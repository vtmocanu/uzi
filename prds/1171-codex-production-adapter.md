# PRD #1171: Complete Codex production adapter and execution safety

**Parent:** [#1106](https://github.com/vtmocanu/uzi/issues/1106), remaining M3 (M3b) only.
**Status (verified 2026-09-10):** PR #1220 contains the completed dark production adapter. The native AMD64 packaged proof passed for pre-hardening base and JVM worker images on an OKD cluster, using synthetic credentials and an internal no-egress network. A current review added fail-closed resume and provider-token-binding fixes plus a stronger Dockerfile guard, so the exact implementation-head `15/15` packaging and `14/14` lifecycle rerun remains in progress before merge. Milestones 1-5 remain complete subject to that revalidation. Maintainer-only real-provider acceptance remains separate and parent #1106 M3 stays unchecked until that acceptance completes.
**Execution:** When authorized, use a gated run so the milestone count scales the budget. Pre-flight must select a worker reporting `docker:true` with this repo allowlisted; block dispatch if none is available. Decide the MR-rework override at dispatch time. Implementation effort: high.

M3a [#1156](https://github.com/vtmocanu/uzi/issues/1156) packages pinned Codex 0.153.2 in both worker images and supplies one isolated, observed-clean supervisor-root launcher. M2 [#1146](https://github.com/vtmocanu/uzi/issues/1146) supplies the neutral run/advice harness boundary while preserving Claude behavior. M1 [#1147](https://github.com/vtmocanu/uzi/issues/1147) supplies named credentials, immutable run binding, per-claim capability authorization and coordinated refresh/recovery. The remaining M3 work is to connect those three foundations into a production Codex run and advice adapter without enabling public Codex routing yet.

Use current `main` and a new working branch; never touch `main`. The accepted [ADR-1106 execution policy](../adr/1106-codex-harness.md#execution-policy), [neutral harness contract](../e2e/codex-m0/harness-contract.md#contracts), [M1 credential contract](done/1147-codex-credentials-foundation.md) and [M3a launcher contract](done/1156-codex-image-launcher.md) are authoritative. Complete only this child and hand off its PR. Do not start M4-M7, enable production routing, deploy, release, close the parent, or claim phase-1 Codex support.

## Problem and outcome

The repository can store Codex credentials and contains the stock app-server transport, callback broker, registry/safety owner, run/advice adapters and renderer delivered by #1188. The live worker still constructs only `ClaudeHarness`/`ClaudeAdviceHarness`: the merged Codex core has not been connected to production credential callbacks, child delegation, executor composition or durability sinks. M4 conformance and M5 routing remain blocked on that integration and acceptance. (AI-synced 2026-09-08)

Deliver the complete **production execution spine** for a Codex-selected, already-bound claim:

- `CodexHarness` and `CodexAdviceHarness` implement the neutral contracts using the pinned stock app-server and M3a launcher;
- worker-owned callbacks, not Codex native tools/roles/hooks/MCP, own shell, file/patch, skills, workflow signals and synchronous delegation;
- one immutable per-run/local-execution-epoch registry owns every provider and command supervisor root, callback and launch reservation;
- the runner holds an unforgeable matching-epoch safety permit across every durability/publication sink;
- provider roots are OS-isolated from model-authorized command/file effects, so “credential-free” means inaccessible rather than merely absent from a command's environment;
- subscription refresh goes through M1's API-owned coordination/recovery state machine, while API-key runs never silently fall back to subscription credentials or vice versa;
- Claude construction, event projection, cleanup timing, prompts and tests retain D0 behavior.

This child may make a **dark internal Codex-bound claim** executable in tests and in the maintainer-only live probe. M5 still owns `runs.harness`, public/manual/schedule selection, old-worker claim compatibility, model/default UI and broad run-kind routing. Saving a Codex credential must remain insufficient to create a Codex run through today's public paths.

## Current implementation facts (base `db1cd4b7`, all offline-verifiable)

These are starting facts, not tasks requiring open-web research:

1. `agent/src/harness.ts` already defines `RunHarness`, `AdviceHarness`, normalized events/reduction and the Claude-preserving lifecycle surface. `agent/src/claude-harness.ts` and `agent/src/claude-advice-harness.ts` are the only production implementations. There is no `CodexHarness` or Codex advice adapter.
2. `agent/src/codex/{config,launcher}.ts` builds the stock native-disabled config and launches exactly one provider/command supervisor root. `agent/codex/supervisor/` and `e2e/codex-m3a/` prove per-root cleanup. M3a explicitly does **not** implement run/epoch reservations, callback settlement, child quiescence, aggregate process evidence or the outer permit.
3. `api/internal/workersvc/claim.go` has an optional secret-bearing `ClaimSecrets.Codex {access_token, capability}` block. `claim_assembly.go` emits it only when `runs.codex_secret_id` is frozen. Ordinary Claude claims omit the key byte-for-byte. In contrast, `agent/src/protocol.ts` does not yet declare or consume the optional Codex claim block.
4. `api/internal/workersvc/codexauthz.go` implements run-bound/current-worker capability scopes and safe access-token release. `codexrefresh.go` implements operation-id/generation-based coordinated subscription refresh, durable commit before token release, stale/idempotent reconciliation, protected recovery and quarantine. These service methods are not yet connected to worker HTTP routes or the app-server refresh callback. Production `workersvc.Service.codexRefresh` is also nil: `api/cmd/server/main.go` does not inject the bounded `codexauth.Client`, so a route calling the current service alone would fail closed without refreshing. The M3 plan must own that injection and reconcile the actual initial-generation/wire data required by `CoordinatedCodexRefresh`; it must not guess that the claim's current two-field Codex block is sufficient.
5. `agent/src/executor.ts` exposes only the legacy optional synchronous `killAgentTree`. `agent/src/runner.ts` calls it around checkpoint, park/shutdown recovery, terminal/finalize and PAT-bearing git paths. The accepted contract instead adds an optional Codex-only `CodexExecutionSafety.withBoundary`; absence keeps Claude/stub behavior literal, while a Codex-selected execution without the facade must fail before model work. The current `ExecutorFactory` receives only `runId` and `main.ts` always constructs `SdkExecutor`/`ClaudeHarness` before claim contents are available, so this child must add a narrow claim-aware dark selection seam rather than proving Codex only through direct adapter tests.
6. M0's app-server fixtures under `e2e/codex-m0/` already measure runtime thread/turn/call identity, worker-created children, code-mode host activity and failure paths. They are characterization evidence, not production code. M3 may promote the measured protocol mechanism without rewriting or weakening the frozen M0 corpus. No `@openai/codex-sdk` dependency is needed for the accepted app-server transport.
7. M3a creates a fresh HOME/CODEX_HOME/XDG tree per launcher root, while pinned Codex sessions live under `CODEX_HOME/sessions`. This child must define stable credential-free per-run session-state ownership across a safe boundary that reaps and recreates provider roots; a bare session id cannot resume from a newly empty home. Auth/cache material must not survive merely because session state does.
8. Preparation-time observation on 2026-09-07: live Uzi was older than these merged foundations (`v0.78.0` server at `a1d3110`; hosted workers reported `v0.77.0`) even though repository `main` had advanced through the `v0.79.0` release commit. This is not dispatch-time authority: recheck the live build then. Regardless of rollout, the uzi worker may build and exercise the new code in isolated Docker images, but must not cite its own installed process as M3 evidence or perform a real credential-backed provider test. The live half below is maintainer-owned.

## Frozen boundaries and non-goals

- Stock app-server only. Disable native environments, native agents, native shell/file tools, native web, MCP, plugins/apps and hooks. Provision the canonical project explicitly `untrusted`, set `project_doc_max_bytes = 0`, and use no hook-trust bypass. Loopback MCP, native role TOML and SDK-only root streaming are rejected designs.
- Runtime callback arguments and model text are untrusted data. Authority comes only from worker-owned immutable run, credential claim epoch, local execution epoch, thread, turn, role, phase, tool/skill and root-registry state. Unknown/stale origins and malformed/unsupported messages fail closed.
- The API `codex_claim_epoch` (credential capability/reclaim authority) and adapter-local execution epoch (callback/root closure and permit matching) are distinct branded domains. Never compare, copy or substitute them. A new claim constructs a new execution registry; a safety permit matches only its held local execution epoch.
- Root-only workflow signals remain root-only. A subagent cannot `submit_plan` or `signal_done`; unknown/nested delegation is denied. Display attribution never grants signal authority.
- Every Codex model effect process is a registered M3a supervisor root. Never infer run safety from app-server exit, process-group kill, one root's `ECHILD`, a callback reply, a name/glob or process inspection outside recorded ownership. While model-authorized command/file callbacks are admitted, they must be unable to read any provider root's argv, environment, descriptors, HOME/CODEX_HOME/XDG/auth/session state, or another run's state through `/proc`, `/data`, guessed paths or enumeration. Same uid plus mode 0700 is not evidence; establish a distinct OS identity/namespace or an equivalently enforced isolation boundary, and stop if it cannot be proven.
- Closing model/callback admission may leave only a distinct trusted boundary-action lane. Every subprocess a permitted checkpoint/git/finalize/recovery action starts reserves owned process state before spawn; the permit remains held until the action and all children actually settle. Timeout/error never returns while an action child runs, and incomplete settlement poisons the epoch. No direct `child_process` bypass is allowed.
- Subscription and API-key credentials are immutable private construction inputs with no fallback. Provider roots receive only their selected credential; command roots are credential-free. No model-visible token, refresh/login blob, run capability, forge PAT, private endpoint or worker environment.
- Credential-free means inaccessible, not merely omitted from an environment. Provider and command roots must be tested concurrently with secret-shaped canaries through guessed and enumerated `/proc` and state paths.
- The shared advice ceiling permits isolated pure in-memory calculation only. Advice exposes no credential operation to the model and receives no workspace or general run callback registry. Its trusted transport may receive only the narrow immutable provider-auth/refresh bridge needed for its selected credential, under the same M1 authority, generation, persistence and disposal rules. If that authority is unavailable, refresh fails closed. Claude advice remains tool-less; this child adds no Claude calculator.
- The production builder fixes the vendor endpoint, provider name, credential env key, binary and supervisor target. None comes from a claim, model, repo, saved label or callback. Localhost/fake-provider injection exists only in a non-exported test/integration composition unreachable from production routing.
- Never log raw app-server frames, auth callback bodies/replies, transport stderr, HTTP bodies, tokens/capabilities or attacker-controlled values in error strings. Diagnostics are static/bounded/redacted. Park/restart/final disposal removes provider auth/cache material while preserving only credential-free session state (or proves pinned Codex wrote none); every recreated root requires a fresh API-authorized committed token/generation release.
- Preserve Claude byte-for-byte at its construction, prompt/render, event/wire, error-ordering, clean-EOF, cleanup and callback boundaries. Do not “improve” inherited Claude behavior in this child.
- No `runs.harness`, `run_usage.harness`, pricing table, public picker/default/schedule/CLI flag, broad run-kind routing, chat-on-Codex, egress/chart, image tag, deployment, release or M4's exhaustive adversarial clause matrix.
- No production `.github/workflows/**` change, including validation. Any repeatable recipe belongs in root `Taskfile.yml`. No real secrets; assemble secret-shaped fixtures at runtime and run `task scan:secrets` with its canaries detected.
- Run `task scan:secrets` with canaries detected before every milestone commit/checkpoint that introduces or changes credential/redaction/capability-shaped fixtures; a clean final tree cannot clear a forbidden literal retained in an earlier commit.
- Test containers use exact invocation-owned names outside `uzi-` (for example `cdr-*` or `aud-*`) and cleanup removes only that exact resource. Never run `docker compose down` from a worktree, never pass `-p uzi` to `down`, never add `-v`, never filter/glob `uzi-*`, and leave unattributed containers/processes untouched.

## Plan gate: settle the production state machines before implementation

The gated plan must name the concrete modules, state transitions and failure precedence for all items below. If it cannot reconcile one with the accepted ADR/contract and current M1/M3a code, revise or block rather than inventing authority.

1. **App-server transport ownership.** Define request/response correlation, notification decoding, startup/handshake, bounded framing, backpressure, cancellation and unexpected-EOF behavior. Preserve raw provider messages inside the adapter and emit only neutral events. Unknown notifications remain liveness data unless protocol safety requires a typed failure. Never fabricate subagent attribution from hook identity. Production construction fixes the vendor endpoint/provider/env key/binaries; the fake endpoint is injected only by a non-exported integration composition.
2. **Registry and outer permit.** Specify the local execution-epoch state machine for open/closing/closed/poisoned/disposed admission; reservations before launch; callback and child settlement; every-root aggregation; serialized overlapping boundaries; matching quiescence/reap epochs; primary-error independence; and permanent disposal. Keep it type/domain-distinct from the API claim epoch. Map every current `runner.ts` sink to `withBoundary`, including both checkpoint forms, park, shutdown recovery, every terminal/finalize branch, PAT-bearing git/base alignment/MR work and best-effort recovery publication. State why ordinary credential-free `reap:false` fetch-back does not itself mint a permit. Define the trusted boundary-action subprocess lane, pre-spawn ownership, full async action lifetime and late-child timeout poisoning.
3. **Worker callback broker and delegation.** Define the worker-owned schemas for shell and file/patch operations, canonical-path/symlink jail, `.git` and secret-path denial, `screenBashCommand` reuse, role/phase/tool/skill admission and root-only signals. Reserve `(registry instance, threadId, turnId, callId)` before an effect; concurrent/replayed duplicates return one bounded cached terminal result or deny, while call-id reuse with changed payload/origin denies and poisons. Model-selected file/patch I/O must use a race-safe fd-anchored/no-symlink mechanism rather than realpath-check-then-pathname-I/O; test symlink swaps and concurrent mutators. Define worker-created child thread start/await/cancel and forbid nested delegation. A callback resolves only after its operation or owned child turn settles; closing admission waits for every reservation and accounts for races already accepted.
4. **Prompt and model construction.** Define the deterministic Claude-tool-vocabulary to Codex callback/role mapping, unknown-tool reporting/stripping, per-role model/effort fallback, sanitized skill delivery and separate run/advice builders. Mutation-test the Claude byte-identity assertion. Every start/resume/turn entry point must go through the lane-specific immutable construction boundary; add a Semgrep rule or extend the existing security rules so an alternate constructor cannot bypass it.
5. **M1 credential bridge and production injection.** Reconcile the API's actual claim payload with the app-server `chatgptAuthTokens` login/refresh flow and `CoordinatedCodexRefresh`'s operation-id plus observed-generation contract. Specify any additive optional claim/result fields, Bearer-only worker routes, status/error mapping and idempotent retry behavior. Wire a bounded production `codexauth.Client` into `workersvc.Service`, retaining an injectable fake, and prove the route through real server construction rather than direct service tests alone. The API is the only holder/user of refresh tokens and the only component performing the provider exchange. The worker-authenticated route path `{id}` is the sole run identity; strict request bodies carry capability and operation/generation data only and must not duplicate or override the run id. Never accept user, secret, account, refresh token, login blob or app-server `previousAccountId` from the path, query or body; callback hints are untrusted liveness, and every secret-bearing response is `Cache-Control: no-store`. Generate one random operation id per logical callback and reuse it after HTTP timeout/lost reply. `observedGeneration` comes only from an authenticated API release/result. Before each provider-root construction, freshly authorize/release a committed access-token/generation pair; capability/ownership loss closes admission and stops/poisons the old root, never maps to a blind refresh/re-login. The entire worker → API → provider/identity → durable commit → callback response is budgeted below pinned app-server's measured 10-second external-auth deadline. Deadline/cancellation returns no stale token, retains the same operation id for reconciliation and never triggers a second exchange. Persist/reconcile latest state before every durability boundary and withhold the permit on failure. Two same-subscription app-server processes converge on one durable generation without a run-long lock; API-key mode never invokes subscription refresh.
6. **Dark selection and secret registration.** Define the narrow claim-aware production discriminator that conveys an internally frozen Codex claim into `RunExecution`/executor construction without adding M5's public `runs.harness` routing. Presence of a complete server-owned Codex binding selects Codex; malformed/incomplete input fails before model work and never falls back. Absence takes the exact Claude factory path. Register Codex access-token/capability canaries with the run redactor before any log/error-producing work, while keeping them out of model-visible data.
7. **Session-state and lifecycle ownership.** Define stable per-run, credential-free session-state ownership across checkpoint/park/shutdown root recreation, including how the fresh M3a launcher receives or adopts it without carrying auth/cache material. Cover session inspection, missing/corrupt state, same-worker preservation, cross-worker resume input, terminal deletion and fresh API-authorized credential release for every new root. Then define plan-to-approval-to-implement/review, local timeout/cancel, provider terminal failure, iterator/transport failure, callback failure, incomplete child/process/tool/action cleanup and poisoned recovery. Preserve neutral run/advice error ordering and Claude compatibility; an incomplete barrier blocks the sink while retaining primary and cleanup evidence separately.

## Remaining execution graph after #1188

**Account-meter coordination (#1209, verified 2026-09-08):** [Codex account limits](1209-codex-account-rate-limits.md) is a separate visibility PRD. This child retains ownership of `codexauth.Client` production injection, worker release/refresh authorization and runtime durability boundaries. #1209 sequences its shared credential integration after this implementation lands, then adds an internal API-side account reader over the same coordinator. Do not add meter polling, UI or a worker callback for it here. #1209 does not depend on M4-M7 or authorize this child's dispatch.

The merged dark core is a prerequisite, not work to reproduce. Preserve its transport, registry/safety owner, broker authority, renderer, run/advice harnesses, fd-anchored session store, file helper and OS identities. The new run implements only the deferred items recorded in the progress log.

| Phase | Milestones | Dependencies | Primary files | Repo |
| --- | --- | --- | --- | --- |
| Phase 1 (parallel) | 1. Credential bridge; 2. Callback/composition prerequisites | Both start from merged #1188 and touch separate ownership seams | `api/internal/{workersvc,handler}/**`, `api/cmd/server/main.go`, `agent/src/{client,protocol}.ts`; separately `agent/src/codex/**`, `agent/codex/supervisor/fileop/**` | `vtmocanu/uzi` |
| Phase 2 (sequential) | 3. Production executor composition; 4. durability boundaries | 3 requires 1+2; 4 requires a real Codex executor from 3 | `agent/src/{main,runner,sdk-executor,executor}.ts`, new Codex composition modules and runner tests | `vtmocanu/uzi` |
| Phase 3 (acceptance) | 5. Packaged proof and maintainer handoff | Requires 1-4 | `Taskfile.yml`, `e2e/codex-m3b/**`, Semgrep rules/tests, both worker Dockerfiles only if the packaged proof requires a production-wiring adjustment | `vtmocanu/uzi` |

## Milestones (remaining work only)

- [x] **1. Phase 1A: connect the M1 credential contract to the worker and pinned app-server bridge.** Extend Go `ClaimCodexSecrets` with the server-owned auth mode and current committed generation while preserving subscription generation zero and keeping generation absent for API-key claims. Ordinary Claude claims must still omit the complete Codex block byte-for-byte. Add the Bearer-only `/worker/runs/{id}/codex/{release,refresh}` handlers and matching TypeScript `WorkerClient` methods. The path `{id}` is the sole authoritative run identity; strict request bodies carry only the run-scoped capability, operation id and observed generation, reject a duplicate/body-supplied run id, and never accept user/secret/account ids or credential data in URL/query/body. Responses release only the selected committed access token plus the minimum mode/generation/outcome facts and set `Cache-Control: no-store`. Wire a fixed-endpoint, deadline-bounded production `codexauth.Client` through real server construction while retaining an injectable fake. Set explicit nested budgets that complete the whole worker/API/provider round trip below pinned app-server's measured 10-second callback deadline. Generate one operation id per logical refresh callback and retain/reuse it after a timeout or lost reply; never begin a second exchange blindly. Router and live-DB tests cover worker Bearer acceptance, cookie/session/CSRF-only rejection, malformed/duplicate bearer, rejected body run identity, wrong worker/path-run/capability/kind/reclaim, production injection, initial generation, revoke/replace/quarantine, provider-success/persistence-failure and the delayed/ambiguous three-case contract. Two stale app-server clients converge on one durable generation; API-key mode performs zero refresh calls. Static error bodies and a non-oracular authorization mapping release no provider text or secrets. Gates: `task gate:api`, `task gate:agent`, `./e2e/run-store-it.sh`, `task test:codex-m3b`, `task scan:secrets`, `task sast:semgrep`.

- [x] **2. Phase 1B: finish the callback and construction prerequisites left as injected seams.** Implement the real worker-created synchronous child-thread delegation runner on top of the delivered broker seam, preserving honest thread/turn/role attribution, root-only workflow signals, nested-delegation denial, cancellation and full child settlement before the parent callback resolves. Add the production file-operation client for the delivered `openat2` helper and parse/apply patch diffs through the fd-anchored no-symlink path; model-selected file effects must never fall back to pathname check-then-use I/O. Complete the deterministic run/advice builders, known-role/tool/skill grants and any harness-neutral construction inputs needed by milestone 3 without wiring a dead branch into `runner.ts`. Mutation-test Claude renderer byte identity and retain the existing replay/stale-origin/symlink-swap controls. Gates: `task gate:agent`, `task test:codex-supervisor`, `task test:codex-m3b`, `task scan:secrets`, `task sast:semgrep`.

- [x] **3. Phase 2A: compose the production Codex executor and claim-aware dark selection path.** Build the `CodexExecutor`/production composition over the neutral run loop, delivered `CodexHarness`/`CodexAdviceHarness`, M3a launcher, registry/safety owner, callback broker, session store and milestone-1 credential bridge. Change the per-run factory only through a narrow claim-aware seam: absence of `secrets.codex` takes the literal current Claude/stub construction path; a complete server-owned block selects Codex; malformed input or missing safety/credential authority fails before model work with no fallback. Register Codex access-token and capability canaries with the run redactor before any log/error-producing construction. Every provider-root start or resume freshly releases the committed credential/generation; preserved state is credential-free only. Wire the milestone-2 child delegate and file/tool handlers plus the advice auth bridge, fixed production endpoint/provider/env-key/binary targets, setgid/umask worktree ownership required by the command identity, and terminal cleanup. Keep public manual/schedule/chat creation dark and confirm the exact provisional rollout file set without deploying it. Gates: `task gate:agent`, `task gate:api`, `task test:codex-m3b`, `task scan:secrets`, `task sast:semgrep`.

- [x] **4. Phase 2B: put every durability and publication sink behind credential authorization plus the matching execution permit.** Map both checkpoint forms, park, graceful-shutdown recovery, terminal/finalize paths, PAT-bearing git/base-alignment/MR work and best-effort recovery publication through the optional Codex `withBoundary` facade. Before each Codex sink, take exactly one auth-mode branch: a subscription run reconciles and durably persists the latest subscription state before `withBoundary`; an API-key run performs zero subscription-refresh/reconciliation calls and proceeds only after fresh API-key authorization. Only then may a matching local-execution-epoch permit invoke the trusted boundary action. Hold it through the full async action and every child. Failed, mismatched, stale or poisoned evidence leaves the sink counter zero, retains primary and cleanup evidence separately, and blocks later publication. Ordinary credential-free `reap:false` fetch-back mints no permit. Claude/stub absence must take the literal legacy `killAgentTree` branches with unchanged ordering, cleanup and errors. Focused `runner-*.test.ts` tests cover every sink, overlapping boundaries, late action children, refresh failure before action, API-key authorization with zero refresh calls and construction-without-safety failure. Gates: `task gate:agent`, `task gate:api`, `./e2e/run-store-it.sh`, `task test:codex-m3b`, `task scan:secrets`, `task sast:semgrep`.

- [x] **5. Phase 3: prove the combined dark production spine in both packaged images and prepare the maintainer-only live handoff.** Add `task test:codex-m3b:packaged`, append it serially to `task test:codex-m3b`, and run image-baked `/app/src` through the real base and jvm entrypoints/confinement with loopback-only fake-provider networking, an outer watchdog and positive per-image test/callback/delegation/root counts. Drive an internally bound claim through plan, approval/new-root resume, implement/review, synchronous subagent, root-only signals, checkpoint, cancel and finalization against real worker routes plus live DB; assert clean/poison outcomes and prove injected credential/capability canaries are absent from public messages, provider-visible prompts/requests, logs and command-root state. Add `e2e/codex-m3b/live.sh` as a secret-injection-only maintainer entrypoint; automated runs exercise only `--dry-run`/fake mode plus no-input refusal, never a real provider credential. Add fixed-constructor, direct-spawn, direct-sink and Bearer-only Semgrep guards with red controls. Mutation evidence covers outer permit, late boundary-action child, stale/replayed callback, subagent signal, refresh-before-sink, Claude renderer, production constructor and worker handler. Before finalize, sweep the tip and every child commit for forbidden `.github/workflows/**` and `e2e/codex-m0/**` authorship, and run `task scan:secrets` before each credential-shaped milestone commit. Final gates, once each to logs: `task gate:agent`, `task gate:api`, `task gate:repo`, `./e2e/run-store-it.sh`, `task test:codex-m3b`. No parent M4 conformance, deployment, release or real-provider call belongs in the uzi run.

## Maintainer-only live acceptance after the implementation PR

This section is deliberately **not** a uzi-worker milestone: restricted workers do not receive a maintainer Codex credential, and the currently deployed worker image predates M3a. Before checking parent #1106 M3 complete, the maintainer must run the child-provided injection-only probe in an isolated current worker image and record only sanitized evidence in the parent progress log:

1. start and complete one production run-harness turn and one advice-harness pass with a Codex subscription login, then repeat startup with an OpenAI API key; no fallback between modes;
2. run two app-server processes against the same subscription state, force an expired generation, observe exactly one coordinated refresh and both processes consuming the one durable generation;
3. recreate the process/root and authenticate from the API's committed state, then exercise cancel and final boundary cleanup with every registered root settled;
4. verify no secret, capability, account id, private endpoint, session/thread id or raw auth artifact appears in logs, git, image layers or the public evidence.

If this live acceptance cannot be performed safely, merge may still close child #1171's implementation, but parent M3 remains unchecked and M4/M5 must not be dispatched as though the production integration were accepted.

## Success criteria

1. Production `CodexHarness` and `CodexAdviceHarness` use stock app-server through the M3a mechanism, fixed production endpoint construction and resumable credential-free session state; no alternate unguarded start/resume/turn constructor exists.
2. Worker-owned callback/delegation authority is immutable, replay-safe, symlink-race-safe, fail-closed and independent of model arguments, repo config, native roles or healthy hooks. A Codex child cannot transition the workflow.
3. Every Codex provider/command root, accepted launch/callback and trusted boundary-action subprocess belongs to one local execution-epoch registry distinct from the API claim epoch. Every durability/publication sink holds a matching joint permit until its action and children settle; incomplete evidence leaves the sink uncalled and poisons later publication.
4. M1's run/current-worker capability and coordinated refresh are exercised through the connected production app-server/client/Bearer-route/server-construction bridge. Concurrent same-subscription sessions converge without serializing whole runs; stale/ambiguous/persistence failures never release or regress credential state.
5. Subscription and API-key modes are separately constructed with no fallback. Provider state is OS-inaccessible to command/file effects; no model-visible credential/capability or unbounded/secret-bearing diagnostic is emitted.
6. Claude execution/advice behavior, prompts, wire projection, error ordering, cleanup timing and existing tests remain unchanged; discriminating mutations prove the new assertions can fail.
7. Negative tests prove public manual/schedule/chat creation remains dark/Claude-compatible, ordinary claims omit `secrets.codex` byte-for-byte, and only the explicit internal bound-claim seam selects Codex; M5 still owns enablement and old-worker compatibility.
8. `task test:codex-m3b:packaged` proves both `base` and `jvm` execute image-baked production code with nonzero expected test/callback/root counts and the dry maintainer probe; the aggregate offline gates pass, then maintainer-only live acceptance passes before parent M3 is checked.
9. The branch diff and history contain no `.github/workflows/**` change, real credential/private deployment data or M0 characterization edit; the new constructor and worker-route Semgrep rules have discriminating red controls.

## Risks and stop conditions

- **Authority drift:** app-server/runtime protocol changes could bypass callback identity or builder restrictions. Pin to 0.153.2 and fail closed on unknown identity/authority-bearing messages; M4 later expands the adversarial matrix.
- **Cross-root credential read:** provider and command roots currently share the runner uid, so sparse command env plus mode-0700 homes is insufficient. M3 must establish and prove an OS boundary before model-authorized command/file callbacks coexist with a credentialed provider root.
- **False cleanup confidence:** a clean provider root can coexist with a live command root or accepted callback. Aggregate recorded ownership only; any incomplete root/reservation/callback poisons the epoch and blocks sinks.
- **Boundary child escape:** the permitted checkpoint/git/finalize action itself can spawn after the pre-action reap. Track that trusted lane and hold the permit through actual child settlement; timeout never means clean.
- **Refresh race or lost provider outcome:** CAS alone is insufficient. Use M1's short operation coordination, durable intent, generation reconciliation and protected recovery. Stop if the real callback cannot supply/retain the required operation/generation state without trusting model/provider hints as authority.
- **Resume state mixes session and auth:** a fresh M3a home cannot find a prior session, while preserving the full home can retain stale credentials. Persist only a proven credential-free session subset and freshly re-authorize auth per root; stop if pinned Codex cannot separate them safely.
- **Claude regression:** sharing policy helpers can accidentally change Claude behavior. Keep construction branches separate and require byte/golden plus mutation evidence before accepting refactors.
- **Scope collapse into M4/M5/M6:** do not add exhaustive conformance, public routing, pricing, UI, egress or deployment merely because the new adapter exposes the seam. Record those dependencies and stop at the dark production spine.
- **Unsafe live validation:** no uzi worker performs real-provider testing. If maintainer isolation or safe secret injection is unavailable, record the exact blocker and leave parent M3 open.

## Progress log

- 2026-09-08 (account-meter conflict check): verified the latest issue body and PR #1200 while preparing #1209. Corrected the stale header requesting new authorization and the present-tense claim that the #1188 core did not exist. The issue already records conditional Auto authorization, with #1197 still pending; #1190 has merged. Added the separate meter PRD's file/authority boundary. Historical baseline facts below retain their original commit/date; no milestone or dispatch state was changed by this documentation update.

- 2026-09-08 (remaining-work redispatch decision): the user chose to continue the existing #1171 child rather than fragment the rationale into another PRD. The milestone list now contains only work deferred by #1188, grouped as Phase 1 parallel credential/callback prerequisites, Phase 2 sequential production composition and sink integration, then Phase 3 packaged proof. The delivered dark core is an explicit prerequisite and must not be reimplemented. Dispatch is gated in Auto mode with MR rework enabled; the updated PRD must land on `main` before the issue is promoted and the new run is created.

- 2026-09-08 (redispatch PRD review): folded both trust-boundary clarifications from PR #1196. The worker route path `{id}` is now the sole run identity and strict bodies reject any duplicate run-id field, removing rather than managing a body/path mismatch state. Durability sinks now state separate subscription and API-key authorization branches: subscription reconciles and persists before the permit; API-key performs zero subscription refresh/reconciliation and proceeds only after fresh API-key authorization.

- 2026-09-07: Child issue and PRD prepared at the user's request, explicitly **not sent to uzi**. Dependencies M1, M2 and M3a are merged; no repository Uzi run is active. The issue carries only the `PRD` label and is intentionally ineligible for unattended pickup until the user authorizes dispatch. Current code/API seams were re-read through `db1cd4b7`; no production implementation or live credential test is claimed.

- 2026-09-07 (implementation run, dark spine): landed the **credential-free Codex adapter core** under `agent/src/codex/` plus its OS-isolation identity and the Go openat2 file helper — all DARK (nothing wired into the live `makeExecutor`/runner path, so Claude behaviour is preserved byte-for-byte). Each unit was committed and passed an independent reviewer+auditor wave; several real defects found by the audit lane were fixed before landing (registry callback-ceiling + sticky-poison, transport inbound-byte ceiling, fileop `.git`-via-`..` bypass + FIFO hang, broker O(n²) output-bounder + base64 corruption + identifier log-injection, harness un-cancellable-callback + origin fail-open, advice-harness unbounded-text OOM, session-store symlinked-`sessions/` confinement escape + unbounded scan). No milestone checkbox is ticked because each still has a deferred sub-item (below). New offline aggregate `task test:codex-m3b` runs all TS unit/state controls; `task test:codex-supervisor` covers the Go helper. Delivered:
  - **M1 (partial):** app-server JSON-RPC transport (`transport.ts`); immutable local-execution-epoch registry + branded outer `CodexExecutionSafety.withBoundary` permit + trusted boundary-action lane (`registry.ts`, `safety.ts`); the neutral safety type surface + optional `Executor.safety` facade (`harness.ts`, `executor.ts`); the credential-free command-root identity `runner-cmd` uid 10003 in both images + parameterized setpriv (`agent/templates/{base,jvm}/Dockerfile`, `runner-uid.ts`); `test:codex-m3b` targets. **Deferred:** wiring `withBoundary` into the 5 runner durability sinks; the packaged both-image isolation canary (`test:codex-m3b:packaged`).
  - **M2 (partial):** worker callback broker with fail-closed capability authority — registry-admitted, args-can't-widen, screening reuse, root-only signals, nested-delegation deny (`broker.ts`); the openat2 `RESOLVE_BENEATH|NO_SYMLINKS|NO_MAGICLINKS` file helper as the command-identity root (`agent/codex/supervisor/fileop/`); deterministic renderer with visible unknown-tool diagnostics (`render.ts`). **Deferred:** the real worker-created child-thread delegation runner (currently an injected seam); the Claude-renderer byte-identity fail-old/pass-fixed mutation (M5); apply_patch-diff parsing in file-write.
  - **M3 (partial):** `CodexHarness` (run) and `CodexAdviceHarness` (tool-less isolated advice) on the transport (`codex-harness.ts`, `codex-advice-harness.ts`); the fail-closed dark selection seam — discriminated `secrets.codex` block + branded validator (`protocol.ts`, `select.ts`); resumable credential-free session-state store (`session-state.ts`). **Deferred:** the CodexExecutor composition + wiring the selector into `makeExecutor`/runner (needs a harness-neutral run loop); redactor canary registration at construction; the both-image packaged integration; the exact provisional rollout file-set confirmation.
  - **M4 (not started):** the Go `ClaimCodexSecrets` wire is still the two-field M1 block — it must gain `auth_mode`/`generation` (the TS discriminated block already added here is intentionally ahead and fails **closed** at the selector until the Go side catches up), plus the Bearer-only `/worker/runs/{id}/codex/{release,refresh}` routes, the `codexauth.Client` injection, per-layer deadlines, and the three-case delayed/ambiguous refresh live-DB test.
  - **M5 (not started):** per-sink proof (`runner-*.test.ts`), the fixed-constructor / direct-spawn / Bearer-only Semgrep guards with red controls, `e2e/codex-m3b/live.sh`, and the mutation-evidence pass.
  - **Sequencing note:** M1's runner-sink wiring, the setgid/umask worktree discipline, and the packaged tests were deliberately deferred to the CodexExecutor/runner integration (M3 live-wiring) where they have a real consumer to test against, rather than landing a dead-branch refactor of the 221 KB `runner.ts`. Parent #1106 M3 stays unchecked and the maintainer-only live acceptance is untouched; the PRD is kept at `prds/` (not archived) because parent #1106 links it by relative path and the child is not fully complete.

- 2026-09-07 (MR-rework, security hardening of the dark M1-M3 core): folded six maintainer review findings onto the branch, all confined to the DARK `agent/src/codex/` surface (still nothing wired into the live `makeExecutor`/runner path, so Claude behaviour stays byte-for-byte). Each fix landed as its own commit and passed an independent reviewer+auditor wave over its immutable range. No milestone checkbox changes; the deferred sub-items above are untouched. Fixes: (1) the run harness now binds every notification/callback to the active `(threadId, turnId)` — a stale/child `turn/completed` no longer ends the root turn, a foreign-thread `item/completed` is liveness not a main frame, and an `item/tool/call` off the active turn is denied fail-closed before the broker, so no stale-turn root-signal latch nor child effect under root grants; (2) the credential-free session copy is now fd-anchored (openat-emulation, `O_NOFOLLOW` on every component, source+dest walked in lockstep on held dir fds) with the read bounded on the opened handle, actual-byte accounting, a bounded recursion-depth axis, and a failed persist removing the partial store so nothing later-adoptable survives; (3) provider terminal fields (status, `turn.error`, usage) are normalized through the new shared `terminal-normalize.ts` to a closed subtype vocabulary, provider-text-free errors and a bounded numeric-only usage subset in both the run and advice decoders, so no raw/secret-bearing/oversize provider data reaches published run messages; (4) a Codex advice root that finishes launching after the settlement grace is now abort-bound and disposed exactly once, so its root/HOME cannot leak on timeout; (5) `ExecutionRegistry.quiesceChildren` closes admission then boundedly settles accepted work up to `deadlineMs`, poisoning only at the deadline or on a real protocol fault instead of instantly poisoning a normal in-flight callback; (6) `registerRoot` returns a checked result and both callers (`CodexHarness.ensureRoot`, the boundary-action lane) tear down and refuse to use a root that failed registry admission.

- 2026-09-08 (MR-rework follow-up): closed the remaining correctness and trust-boundary findings in the delivered dark core. Boundary permits are now bound to one acquisition by object identity, and boundary-action children drain in repeated batches so a child admitted during settlement cannot outlive the permit. Root registration validates both the caller copy and produced root against the stored reservation. The transport installs abort/deadline cleanup before writing and bounds individual plus queued outbound bytes. The run harness preserves an outstanding notification read across cancellation, honors a stop before lazy iteration begins, validates resume IDs, guards undeliverable server replies, and makes transport close permanently reject later turns. Malformed signals deny; delegated-child failure codes/messages are broker-owned, bounded and control-safe. The file helper probes `openat2` before serving and has a direct kernel escape regression. Linux-only copy/supervisor tests now declare that platform constraint, while the ordinary macOS agent gate skips only the Linux fd-walk suite. Review requests for the production fileop client and Go `auth_mode`/`generation` claim wire remain deliberately deferred to their already-recorded integration/M4 work; no public Codex routing or live runner wiring was added.

- 2026-09-08 (fresh-review closure): a rejected boundary-root reap now leaves bounded poison evidence even when the action was fire-and-forget, so the batch drain cannot report a clean boundary around an unreaped root. The file-helper entrypoint now has direct `run` contract coverage for invalid arguments, root-open failure, unavailable `openat2`, response-transport failure, and clean EOF. Both regressions passed their focused TypeScript/Linux suites before push.

- 2026-09-08 (M3 / Phase 2A: production executor + dark selection seam — milestone 3 partial, NOT ticked): composed the production `CodexExecutor` (`agent/src/codex/codex-executor.ts`) over the merged dark core — registry/safety owner, callback broker, real m2 child-delegation runner, `CodexHarness` run loop, credential-free session store, openat2 fileop client and the M3a launcher adapter — and wired it through a narrow, claim-aware DARK selection seam in `makeExecutor` (`agent/src/main.ts`): absence of `secrets.codex` takes the LITERAL current Claude/stub path (byte-for-byte), a complete server-owned block selects `CodexExecutor`, and a present-but-broken block yields a `FailClosedExecutor` that re-throws the bounded, secret-free `CodexSelectionError` message through the existing failed-run catch (never a Claude fallback, never a crash of `executeClaim`). `ExecutorFactory` widened to `(runId, codex?)`; the runner registers the access-token/capability canaries with the redactor BEFORE `makeExecutor` and appends them to `buildFlight`'s redactor set. A FRESH committed credential is released per provider-root start/resume (`releaseCodex`) and rides ONLY into the launcher env; the command + fileop effect surfaces run as the credential-free command identity with a scrubbed replaced env. Delivered the advice auth bridge + `makeCodexAdviceHarness` factory (constructible + unit-tested, deliberately NOT wired into `model-pass.ts` per R3). Edited the dark `codex-harness.ts` (Claude untouched) to add a child-sink demux so a delegated child turn consumes its own frames off the single-consumer transport, plus a `thread/started`→`initialized` guard on the root thread id; the non-delegation root path is proven byte-identical. New `agent/test/codex-executor.test.ts` (22 tests) covers the 19 required scenarios; `agent/test/codex-harness.test.ts` (+all codex suites) stay green. **Deferred / NOT in this unit:** the runner durability sinks through `withBoundary` (milestone 4); the setgid/umask worktree ownership for the command identity and the exact provisional rollout file-set confirmation; the full plan→gate→implement lifecycle, empty-turn retry and summary hooks (milestone 5). **R2 flagged:** the command/fileop effect spawns are still direct `child_process` and are NOT yet registered supervisor roots — m4 must reserve+register a `command` root at the `makeDefaultSpawnCommand` seam so the reap barrier covers them before a credentialed boundary action. Milestone-3 gates (`task gate:agent`, `task gate:api`, `task test:codex-m3b`, `task scan:secrets`, `task sast:semgrep`) run by the lead; this unit ran only `tsc --noEmit`, the focused codex `node --test` suites and `oxlint`/`knip`.

- 2026-09-08 (M3 review rework): folded the reviewer/auditor findings on the dark executor. Bounded the default command-effect `spawnCommand` seam's stdout/stderr capture at 1 MiB with a SIGKILL-at-cap (an untrusted model-steered `Bash` can no longer OOM the worker); the demuxed child-delegation frames now yield a content-free `activity` liveness event so a real subagent turn re-arms the executor idle watchdog instead of falsely tripping `codex run idle timeout`; the freshly-released provider token is evicted from the logger secret set at terminal cleanup (no unbounded growth on a long-lived worker); added fail-old/pass-fixed regressions for the output cap, the child-liveness idle re-arm, and the runner canary-hoist wiring, and tightened two over-claiming test names. **Still deferred (safe while DARK, recorded):** the plan-turn file-write denial — the broker is built once from an implement-phase plan, so `grants.phase === "plan"` is inert during the plan turn; closing it needs per-phase broker/grant construction, folded into the milestone-5 plan→implement lifecycle. Cancelling an in-flight command/child on a per-turn watchdog trip is bounded by the child-turn deadline today and lands with the milestone-4 supervisor-root registration + reap barrier (R2).

- 2026-09-09 (M4 / Phase 2B: durability sinks behind the withBoundary reap facade — milestone 4 NOT ticked, full gate set is the lead's): routed every runner durability/publication sink through the optional Codex `withBoundary` facade with per-sink auth-mode reconciliation, added the pause-sink designation, and relocated the executor reap lifecycle (F1). All DARK — Claude/stub takes the literal legacy `killAgentTree` branch byte-for-byte (the runner branches ONLY on `!!executor.safety`, never reads authMode/credentials, never imports `agent/src/codex/**`). Delivered: two runner helpers `reapForSink`/`withCodexBoundaryOnly` (`agent/src/runner.ts`) + a bounded `RunnerOptions.codexBoundaryDeadlineMs` (default 30s) + a name-based `isCodexBoundaryError` probe. Sinks wired — **park** → `reapForSink(park)`; **graceful-shutdown** durability IIFE → `reapForSink(shutdown, deadline ≤ shutdown publish timeout)` inside the k8s-grace race; **checkpoint reap:true** → extracted `doCheckpointPublish(overlay?)` and routed the milestone path through `reapForSink(checkpoint)` (reap-before-git preserved; overlay built inside the reaped action only when it will be used), **reap:false** calls it directly credential-free (no permit); **phasePublish finalize** → the `phasePublish` CALL is wrapped in `withCodexBoundaryOnly(finalize)` (the whole push/base-align/MR runs under the held permit); **recovery publication** inside `captureRecoveryRestorePoint` → `withCodexBoundaryOnly(shutdown)`. LEFT byte-unchanged: the security-boundary reap and the terminal pause-finalize / ci-fix not_code `killAgentTree` lines (they run inside the outer finalize boundary), and `handlePausePark` (credential-free, mints NO permit — pause-sink designation). Best-effort paths (park/shutdown/recovery) fold a `CodexBoundaryError` into the existing publish-failure outcome; terminal/finalize lets it reach the failed-run report (executeClaim's catch never misclassifies it as limit/pause/shutdown). **Reconcile seam** (`agent/src/codex/safety.ts`): `createCodexExecutionSafety` gained an optional `ReconcileBeforeBoundary`; `runBoundary` runs it AFTER the serialization queue and BEFORE quiesce/reap/mint — `{blocked}` poisons + throws `CodexBoundaryError("reconcile")` with no quiesce/reap/mint/action; the executor-owned closure (exported `buildRunLaneReconcile`) branches subscription (one operation_id per logical refresh, retained across retries, generation advanced on success before the permit) vs api_key (zero refresh, fresh release only); only the neutral `ReconcileOutcome` crosses back. Added `dispose` to the `CodexExecutionSafety` surface. **F1** (`agent/src/codex/codex-executor.ts`): a new `deferRegistryTeardown` dep — under the runner, `run()`'s finally leaves the registry ALIVE for the post-run sinks and only tears down executor-owned harness/fileop/session; the runner's finalize/park/shutdown sink reaps the provider root and `executeClaim`'s finally calls `safety.dispose` once the last sink settles; STANDALONE, `run()`'s finally backstops the reap+dispose. Provider root torn down on every path (normal → sink; error/no-sink → backstop or terminal dispose). **R2 stays DEFERRED to m5** (untouched): the command/fileop effect spawns are still direct `child_process`, not registered supervisor roots. New `agent/test/runner-codex-sinks.test.ts` (10 tests: every sink routes through `withBoundary`; reconcile-blocked-before-action; api_key zero-refresh; pause + reap:false mint no permit; Claude/stub legacy unchanged); extended `codex-safety.test.ts` (reconcile-blocked/ready) and `codex-executor.test.ts` (run-lane⟷advice differential; F1 lifecycle). This unit ran `tsc --noEmit`, the focused codex + runner `node --test` suites (full agent suite green, 2531 pass / 1 skipped), `oxlint` and `knip`; the milestone-4 gate set (`task gate:agent`/`gate:api`, `./e2e/run-store-it.sh`, `task test:codex-m3b`, `task scan:secrets`, `task sast:semgrep`) is the lead's.

- 2026-09-09 (M5 / Phase 3 + milestones 1-4 gate confirmation): milestones **1-4 are ticked** — each was delivered, independently reviewer/auditor-reviewed over its immutable commit range, and confirmed green by the lead's final gate pass at branch tip: `task gate:agent`, `task gate:api`, `task gate:repo` (incl. `scan:secrets` with both canaries detected + `sast:semgrep` with the canary firing), `task test:codex-m3b`, `task test:codex-supervisor`, and the `LiveDB$` suites (`internal/workersvc` + `internal/handler`) against a throwaway Postgres. M5 landed: the plan-turn file-write denial via a per-turn phase-correct broker (`CodexHarness.useBroker`); four Semgrep guards with documented red/green controls (`codex-fixed-constructor`, `codex-no-direct-spawn`, `codex-durability-sink-boundary` — widened to all seven `child_process` forms so an `execSync` git bypass cannot evade it — and `worker_codex.go` confirmed under the Bearer/no-cookie `worker-routes` rule), registered in `docs/security-gate.md`; the `test:codex-m3b:packaged` target + the extended `test:codex-m3b` unit aggregate (now also running the `e2e/codex-m3b` Block-A canary-absence lifecycle proof host-side); `e2e/codex-m3b/` (loopback fake provider with runtime-assembled canaries, lifecycle harness, orchestrators) + `live.sh` (secret-injection-only maintainer entrypoint that refuses without an explicit credential); the openat2 fileop binary built+installed 0555 in both worker images. The mutation-evidence matrix (outer permit, late boundary-action child, stale/replayed callback, subagent signal, refresh-before-sink, Claude renderer, production constructor, worker handler) is covered by the merged fail-old/pass-fixed tests plus the new Semgrep red controls and the plan-write-denied test. The whole-branch authorship sweep is clean (no `.github/workflows/**`, no `e2e/codex-m0/**` in any of the 14 commits). **Milestone 5 is left UNTICKED**: success criterion 8 requires the packaged both-image proof to EXECUTE image-baked `/app/src` through the real base and jvm entrypoints, which is CI/maintainer-only (in-worker Docker builds are storage-flaky, arm64 blocked) — the harness is authored + host-typechecked but the both-image run was not executed here. Two frozen-boundary/scope items remain and are recorded for a follow-up + the maintainer: **(R2)** the command + fileop model-effect spawns are still direct `child_process` (uid-10003 isolated) rather than registered M3a supervisor roots, so the `withBoundary` reap barrier covers only the credentialed provider root and `hasLiveCommandRoot()` is vacuous for those effects — mitigated by the uid-10003/10002/10001 OS split (a surviving command process cannot read the worker PAT across uids) and the 1 MiB output cap-kill + child-turn deadline; wiring them as supervised roots needs a bounded supervisor exit-code change + the launcher's unused `kind:"command"` uid-10003 path + setgid/umask worktree discipline (M3a scaffolded the command-root kind but never wired the uid). **Maintainer-only live acceptance** (the section above) is untouched and remains a prerequisite before parent #1106 M3 is checked. The PRD stays at `prds/` (not archived): parent #1106 links this path and the child is not fully complete.

- 2026-09-09 (PR #1220 R2 review closure): replaced the deferred direct shell/fileop spawns with reservation-before-launch M3a supervisor roots, made command callbacks settle their whole root before replying, registered the persistent fileop root, and made `hasLiveCommandRoot()` a production guard. Permit-held durability Git/gitleaks/pack processes now run through an AsyncLocalStorage-scoped, registry-owned boundary-action lane; a permit deadline aborts those roots and the checkpoint upload, and graceful shutdown awaits actual settlement before terminal disposal. Command roots run as uid 10003 behind a root-owned Landlock wrapper that fail-closes when the kernel facility is unavailable and allowlists only the current worktree, one random private tmp, and read-only system/toolchain paths; `/proc`, sibling `/data`, `/run`, and other `/tmp` state remain unreadable. The split-mode worktree setgid/group-write posture is verified before model work. The supervisor now reports one bounded normalized primary-child exit while retaining ECHILD+`__WALL` as the only whole-root clean authority. Provider stderr is drained without logging. The remaining milestone-5 blocker is still the maintainer-executed packaged proof; this entry closes R2 only and does not claim that proof.

- 2026-09-09 (R2 independent-audit follow-up): worker-PAT supervisor roots now retain uid 10001 but explicitly clear the root-entry controller's SETUID/SETGID before the unchanged zero-cap profile check; permit Git/gitleaks targets resolve to fixed absolute binaries; an unconfirmed launcher `DisposeOutcome` makes `RegisteredRoot.dispose` fail and keeps the registry incomplete; and one absolute boundary deadline now supplies shrinking remaining budgets across serialized queue wait, reconcile (including HTTP cancellation), quiesce, aggregate reap, action, and action-root reap. The real root-start profile and focused mutation controls cover all four. **PR #1220 remains blocked and milestone 5 remains unchecked**: the fresh audit also found that normal `submit_plan`/`signal_done` results do not reach the reducer, production broker grants have no matching MCP/forge/memory/findings/skill `toolHandlers`, and the command environment omits both `/opt/uzi-toolchain/bin` and `RunContext.toolEnv` provisioned paths. Those executor-composition repairs plus the already-recorded both-image packaged proof must land before completion is claimed.

- 2026-09-09 (R2 deadline follow-up): the boundary now rechecks expiry after reconcile, quiesce and reap before minting a permit, so a cleanup stage that reports success after its budget elapsed cannot start a durability action. Finalize and recovery propagate the permit's single AbortSignal through Git/forge retry, MR creation and duplicate lookup, checkpoint upload, message drain and terminal-state HTTP; each waits for its in-flight request to observe cancellation and settle before `withBoundary` returns. This corrects the earlier incomplete claim that shrinking stage budgets alone bounded the action body. **PR #1220 remains blocked and milestone 5 remains unchecked** for the three executor-composition gaps and packaged proof recorded immediately above.

- 2026-09-09 (MR-rework: production integration landed; packaged both-image EXECUTION deferred — milestones 1/3/4/5 stay UNCHECKED): folded the 2026-09-09 top-level handoff's blocking items onto the branch. The production `CodexExecutor` is now re-pointed at the delivered modules and the three previously-recorded executor-composition gaps are CLOSED:
  - **Auth session + refresh bridge:** `run()` and `makeCodexAdviceHarness` now construct `createCodexAppServerAuth` (via `buildCodexProductionConfigToml`, not the transitional env-key builder) and the app-server subscription refresh callback goes through `WorkerClient.refreshCodex` with a retained operation id, one shared per-run committed-generation cell (also read/written by the boundary reconcile), the 8s client cap and caller cancellation, folding a fresh generation into the shared cell only after the logical callback is delivered (a new operation id). The transitional subscription-as-`OPENAI_API_KEY` path is gone; api_key issues zero refresh.
  - **Signal routing + real loop:** a root-origin signal callback (submit_plan/signal_done/checkpoint/progress/questions) now surfaces the broker's scanned `TurnSignals` as a main-origin frame so the reducer's `foldSignals` fires; `run()` drives the real plan→approval→implement/review loop (cooperative checkpoint→`ctx.checkpoint({reap:true})`, done→break, iteration cap). The "produced no plan" tests are replaced by success coverage. Subagent/child-origin signals stay fail-closed.
  - **Production tool handlers:** the broker `toolHandlers` map (forge/memory/findings via the shared Claude factories + a Codex-specific Skill handler) is built once per run and passed to both the root and delegated-child brokers; `render.ts` grants forge+memory to the root vocabulary (memory subagent-forbidden, forge subagent-allowlist-only).
  - **Command lane:** `/opt/uzi-toolchain/bin` plus the safely-filtered provisioned `toolEnv` (via `provisionRunTools`) now ride the credential-free command PATH, with `PATH/TMPDIR/LANG/HOME` and the credential protected-keys shielded from any override and no `process.env` inheritance; Landlock already covers the toolchain + `/nix`.
  - **Session lifecycle + new-root resume:** the executor factors the per-run provider epoch into `startProviderEpoch` and recreates a fresh epoch (new execution epoch, fresh committed-credential release, adopted credential-free session, per-epoch app-server auth) at plan approval and after each cooperative checkpoint reap, hot-swapping `this.safety` (the runner re-reads it per sink); `persistSession` runs before every provider-reaping boundary and at terminal, and the unconditional `sessionStore.remove` is gone (the runner's runHome lifecycle preserves the store on park, removes it on terminal).
  - **Real WorkerClient harness:** a new `api/cmd/codexm3btestserver` mounts the worker Bearer routes on a real socket over a migrated throwaway Postgres; the e2e m3b lifecycle harness was migrated to the app-server-auth design (Block A green host-side) and Block B rewritten with real assertions (`ran===true`, per-category nonzero real-path counts, api_key zero refresh, canary boundary) that drive a real `WorkerClient` against those routes over a docker `--internal` (no-egress) network with the loopback fake provider in-container.

  Each unit was committed and independently reviewer/auditor/tester-reviewed over its immutable `<base>..<sha>` range; the review-driven follow-ups (advice-lane generation-after-delivery parity, eager-token eviction, the canary-boundary tightening, the runner checkpoint-comment correction, a gosec-G117 test suppression surfaced by this clone's whole-files ratchet) landed and were re-reviewed. Final offline gates green once each to logs: `task gate:agent`, `task gate:api`, `task gate:repo`, `./e2e/run-store-it.sh`, `task test:codex-supervisor`, `task test:codex-m3b`.

  **Packaged both-image proof — AUTHORED, EXECUTION DEFERRED (disk stop-condition).** The `test:codex-m3b:packaged` orchestration (build the base+jvm worker images + a go test-server image, run PG + server + worker containers on a docker `--internal` no-egress network, exact-name teardown) and the real Block B assertions are authored and pass `tsc`/`gofmt`/`go vet`/`shellcheck`; host-side `test:codex-m3b` (Block A + the unit aggregate) is green. But the in-worker EXECUTION could **not** run: building the worker image fails with `No space left on device` (~8 GB free on the docker storage filesystem vs the base image's nix/devbox/node/pinned-Codex toolchain), exactly the `run-lifecycle.sh`-documented "in-worker image builds are storage-flaky" stop-condition. The both-image packaged proof therefore remains a **CI/maintainer-run** step (adequate disk), and milestones **1, 3, 4 and 5 stay UNCHECKED** per the handoff's condition (completion requires both the combined path and the packaged proof to pass); milestone 2 stays checked. When a maintainer runs the packaged proof they should also tighten three best-effort Block B assertions the review flagged: the `delegation` count observes the fake *emitting* `spawn_agent` rather than the executor demuxing a child turn; the reaped/disposed counts prove "did not throw" rather than an observed reap; and the real-Codex Responses item framing + refresh replay-dedup semantics the probe relies on are unconfirmed against a live app-server. The Claude path is preserved byte-for-byte; no `.github/workflows/**` or `e2e/codex-m0/**` change; no real credentials; no AI co-author trailers. Maintainer-only live acceptance (above) remains a prerequisite before parent #1106 M3 is checked.

- 2026-09-10 (maintainer native packaged acceptance): the deferred both-image proof is complete on a native AMD64 OKD cluster with synthetic credentials and an internal no-egress test network. The exact base and JVM worker images each passed `13/13` packaging controls and `13/13` lifecycle tests through their real entrypoint, static supervisor, Landlock command wrapper, openat2 file helper, pinned Codex 0.153.2 app-server, real WorkerClient HTTP routes and a migrated throwaway Postgres. Both subscription and API-key paths authenticated and completed; subscription proved one refresh advance plus same-operation replay, while API-key made zero refresh calls. The real run proved plan submission/approval, fresh-root resume, shell and patch callbacks, synchronous child delegation, cooperative checkpoint recreation, completion, registered-root cleanup, nonzero session persistence/adoption and credential/capability canary absence from public, provider, log and command surfaces.

  Packaged execution found and closed the remaining integration defects before this claim: authenticated loopback provider selection had to match both config and `thread/start`; fresh threads needed explicit dynamic tools and `environments: []`; worker callbacks needed collision-free wire aliases for Codex-native names; persistent root threads needed a runner-owned, credential-free session restore path; traverse-only parents required Linux `O_PATH`; and the refresh replay probe needed a valid retained UUID. Final steady-state permissions keep the provider root and `CODEX_HOME` traverse-only for the worker, expose only `sessions/` read-only, restore as the provider runner, and keep `runner-cmd` outside the trusted group. Milestones 1, 3, 4 and 5 are now ticked with milestone 2. The separate maintainer-only real-provider credential acceptance remains separate, so parent #1106 M3 stays unchecked.

- 2026-09-10 (current-head review revalidation): a fresh CodeRabbit review found three merge-blocking gaps and one false positive. Pinned Codex 0.153.2 does not restore the empty thread environment selection on cold resume, so every root `turn/start` now reasserts `environments: []`; the fail-old regression proves the resumed turn cannot regain provider-default native execution. The Dockerfile group guard now parses logical `RUN` commands with quote-aware shell tokenization, so continuation-line group additions, shell-comment imitations and separators inside quoted data cannot bypass or falsely trip it. Block B's loopback provider now accepts only SHA-256 digests recorded from successful WorkerClient release/refresh responses; its fail-old regression proves an unrelated non-empty bearer no longer authenticates and increases the lifecycle suite from 13 to 14 tests. The proposed TLS change is rejected for the documented test-only trust boundary: every credential is runtime-synthetic and scoped to a disposable API/database, the only network peers are owned containers on the script-created internal network, and the same nested-Docker controller can already inspect their environment and logs. The exact implementation-head both-image rerun is in progress; until it passes `15/15` packaging and `14/14` lifecycle per image, the earlier `13/13` evidence above remains pre-hardening evidence rather than a final-head claim.

- 2026-09-10 (native proof thread-affinity correction): the first exact-commit JVM revalidation passed `15/15` packaging and both real provider flows but once exposed sibling directory enumeration/read in the command-root test. The same image then denied the access in a minimal probe, a focused rerun and a complete `14/14` rerun, so a template/binary difference was ruled out, but the original breach remains blocking evidence. The cause is the pre-ABI-8 Landlock thread model: [the kernel contract](https://docs.kernel.org/userspace-api/landlock.html#thread-synchronization-abi-8) restricts only the calling OS thread and its children, while the Go wrapper had not pinned its goroutine between `restrict_self` and child creation. The wrapper now locks its OS thread before any policy work and never unlocks; a baked, world-readable sentinel outside every allowlist must open before restriction and return permission-denied after restriction before any model command can execute. Root `/` is rejected up front. Linux unit tests pin lock-before-run ordering plus readable, denied and unexpected-error probe outcomes. The earlier rerun is not the final claim; both cached images must pass again from the exact post-fix commit.
