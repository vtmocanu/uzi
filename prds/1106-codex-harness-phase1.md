# PRD #1106: Codex harness, phase 1 (whole-run Claude or Codex choice on one worker image)

**Issue**: [#1106](https://github.com/vtmocanu/uzi/issues/1106) · **Label**: PRD · **Priority**: Medium (strategic; phase 1 of #229)
**Status**: Draft, reviewed 2026-09-05 (four blocking findings folded in, see *Progress log*). Feasibility verified 2026-09-04 (see *Verified facts*).
**Parent**: [#229](https://github.com/vtmocanu/uzi/issues/229) (multi-provider agents + judge, `prds/229-multi-provider-agents-judge.md`). This PRD is #229's phase 1 and supersedes its M1/M2/M3 ordering; #229's mixing (D5/D6/D14/M4), local endpoints (D1 probe, native executor, D10 allowlist) and #727 profiles become later phases. #229's D0, D7, D8, D9, D15, D16, D17 carry over unchanged.
**Area**: `agent/src/` (executor port, Codex adapter, hooks, MCP transport, prompts) · `agent/templates/base/Dockerfile` (Codex binary) · `api/internal/store` (harness on runs and usage, new secret kinds) · `api/internal/workersvc` (credential hand-over, refresh write-back) · `api/internal/secretscrub` · `web/src` (credential card, Run Defaults, Start button, schedule modal, badges) · `api/cmd/uzi/` (CLI parity) · `deploy/chart` (egress allowlist) · `docs/`.
**Line references** are against `a1d3110` (main, 2026-09-04).
**Scope guard**: no `.github/workflows/**` change in implementation or validation (`.claude/rules/prds.md`). The image change is the Dockerfile; any CI build-guard step is a separate maintainer-local issue.

## Problem

uzi runs only on a Claude credential. Every lane imports `@anthropic-ai/claude-agent-sdk` and the worker authenticates only with `CLAUDE_CODE_OAUTH_TOKEN` (`agent/src/sdk-env.ts:73-126`). A user who holds a Codex (ChatGPT) seat or an OpenAI API key cannot run agents on it.

#229 designs the full multi-provider system and orders its milestones judge-first because the judge's guardrail surface is vacuous. That defers what users want first: running whole issues on a flat Codex seat. It also assumed the Codex sandbox would replace the deny hook and that provider workers would be a separate fleet; both assumptions were checked on 2026-09-04 and neither holds (see *Verified facts*), which makes a simpler phase 1 possible.

## Solution

A run executes entirely on **one harness**, chosen per run: all agents on Claude (today's path, byte-for-byte unchanged) or all agents on Codex. No agent mixing.

- **One worker image carries both harnesses.** `agent/templates/base` gains the `@openai/codex` binary and `@openai/codex-sdk`. Harness is a property of the **run**, not of the worker; any worker runs either. No new `workertmpl` entry, no per-harness fleet, no claim routing by harness. This keeps the shared worktree, checkpoints and uid split identical for both harnesses, and it is what makes later in-worker mixing (a Claude lead delegating `coder` to a Codex thread on the same tree) cheap instead of a scheduler problem.
- **Credentials drive the UX; there is no admin toggle.** With one credential nothing new appears anywhere. With both, a default harness in Run Defaults, a split Start button for the per-run override, a harness select in the schedule modal, and a harness badge on runs (Claude unmarked). Facts about existing runs and workers are never hidden.
- **The guardrail ports as hooks, not as a sandbox.** The existing Bash screener, path guard and agent guard run as Codex `PreToolUse` command hooks; the uid split, sparse env and credential absence are OS-level and survive the swap; repo-borne Codex config is neutralized by marking the project untrusted. A conformance suite with one adversarial fixture per clause runs against both harnesses and is a first-class deliverable.
- **uzi's tools reach Codex over an out-of-process MCP transport** hosted by the worker on loopback; Claude keeps the in-process form (D0).
- **The tool-less advice lane** (judge, review, summary) runs on either harness so a Codex-only user keeps the judge. Chat stays on Claude.

### Out of scope (later phases of #229)

- Any mixing of agents across harnesses within a run; #727 profiles; sub-runs.
- OpenAI-compatible and local endpoints, the `wire_api` probe, the native chat-completions executor, the base-URL allowlist (D10). Phase 1 talks only to the fixed vendor endpoint.
- Device-code login (`codex login --device-auth`), capability-aware scheduling (#84), chat on Codex, Codex rate-limit meters (Claude's 5h/7d windows have no SDK-visible Codex equivalent in `runStreamed`).

### Later phases this PRD must not preclude (custom OpenAI URL, OpenAI-compatible, local LLM)

Tracked in #229 (D1, D10, R6, Success Criterion 4); listed here so phase 1 leaves the seams open.

- **Custom base URL / OpenAI-compatible endpoint** (OpenRouter, Azure OpenAI, an internal proxy, a self-hosted Responses-API server) is **not a new harness**: it is the Codex harness with a `[model_providers.<id>]` entry (`base_url`, `env_key`, `wire_api = "responses"`) in the provisioned `config.toml`, plus a third credential kind `openai_compatible {base_url, env_key, model}`. What it needs on top of phase 1: #229's admin base-URL allowlist (D10, mirrors `FORGE_ALLOWED_BASE_URLS`, https-only), a per-endpoint egress allowlist entry, and the vault-threat-model-style egress writeup. The project-layer denylist in fact 4 already stops a cloned repo from redirecting `base_url`.
- **Local LLM (Ollama, LM Studio)**: `codex exec` in 0.153.2 exposes `--oss` and `--local-provider lmstudio|ollama` (and an `oss_provider` config key), so a chat-completions-native local runtime may be reachable through the Codex harness directly. If that holds under the conformance suite, #229's third harness (the native chat-completions executor, finding 1) is unnecessary; if not, it stays as designed. Verify in that phase, not here.
- **Phase 1 keeps open**: `runs.harness` is `text` with a CHECK that a later additive migration widens; `user_secrets.kind` is CHECK-constrained (migration `00010`) and each new kind is one CHECK-widening migration plus a `store/enums.go` constant, the same step phase 1 takes for its two kinds; the per-run `CODEX_HOME/config.toml` is provisioned by the worker and is the only place a provider entry would go; the hook, the uid split and the conformance suite are endpoint-agnostic; the credential-card pattern extends to a third card without touching the first two; the credential-driven visibility rule extends unchanged (a user with only a compatible-endpoint credential sees only that).
- **Phase 1 explicitly does not ship**: any user-supplied `base_url`, any `model_providers` entry, the allowlist, or a local-provider flag. The Codex adapter talks only to the fixed vendor endpoint.

## Verified facts (2026-09-04)

Each fact names how it was established so an implementer can re-verify. Facts marked *offline* need no network beyond the package cache.

**Codex side** (openai/codex source at `main`, `@openai/codex@0.153.2`, the Codex config reference and MCP docs):

1. **Hooks.** `$CODEX_HOME/config.toml` accepts `[[hooks.PreToolUse]]` with a `matcher` regex and `type = "command"` handlers. Stdin is JSON with `session_id`, `turn_id`, `cwd`, `tool_name`, `tool_input`, `tool_use_id`, `permission_mode`, plus `agent_id` and `agent_type` when inside a subagent. Deny by exit code 2 with the reason on stderr, or exit 0 with `{"hookSpecificOutput":{"permissionDecision":"deny","permissionDecisionReason":…}}`. `spawn_agent` is a hookable `tool_name`. Async hooks cannot block. Hook `tool_name`s are Claude-style aliases mapped in `codex-rs/core/src/tools/hook_names.rs`: the shell tool is `Bash`, `apply_patch` is `Write`/`Edit`, `spawn_agent` is `Agent` (so the existing matchers port as is; M0(c) confirms the full table). Hooks carry a trust hash; `--dangerously-bypass-hook-trust` (or a persisted `trusted_hash` in `[hooks.state]`) runs provisioned hooks headless. Source: `codex-rs/hooks/src/events/pre_tool_use.rs`, `codex-rs/hooks/src/schema.rs`, `codex-rs/config/src/hooks_tests.rs`.
2. **OS sandbox is inert in a pod** (*offline, measured*). On `node:24-alpine` as uid 10002 with `--cap-drop ALL` and `no-new-privileges`, `codex sandbox linux -- id` fails with `bwrap: No permissions to create a new namespace`. OpenAI's documented answer for containers is host isolation plus `--sandbox danger-full-access`. So the Codex run uses `approval_policy = "never"` and `danger-full-access`, and the deny layer is the hook, exactly the Claude lane's `permissionMode: "bypassPermissions"` plus `PreToolUse` shape (`agent/src/sdk-executor.ts:975-991`).
3. **musl binary, no `ldd` sniff** (*offline, measured*). The npm launcher maps Linux to `x86_64-unknown-linux-musl` and `aarch64-unknown-linux-musl`; those are the only Linux builds. `codex --version` runs on `node:24-alpine` for both arches, also with a fake GNU `ldd` first on PATH (the #1082 trigger). `codex exec --sandbox danger-full-access` under pod conditions reaches `api.openai.com` and fails only with `401 Missing bearer`. `CODEX_HOME` under `/tmp` makes Codex refuse to create its helper-binary PATH aliases (`Refusing to create helper binaries under temporary dir`), so `CODEX_HOME` goes on the data volume.
4. **Repo-borne config is trust-gated.** Config layers load `${PWD}/config.toml`, `./.codex/config.toml` up the tree and `$(git rev-parse --show-toplevel)/.codex/config.toml`, each "loaded but disabled when the directory is untrusted" (`codex-rs/config/src/loader/mod.rs`). Agent roles (`.codex/agents/*.toml`) and exec-policy `.codex/rules/*.rules` are read per layer, so a disabled project layer contributes neither. `model_provider`, `model_providers`, `openai_base_url`, `chatgpt_base_url`, `notify`, `profile(s)` are on `PROJECT_LOCAL_CONFIG_DENYLIST` regardless of trust. `AGENTS.md` **is** trust-gated too: `load_project_instructions` (`codex-rs/core/src/agents_md.rs:60-63`) returns before the project read loop when `active_project.is_untrusted()`, so an untrusted project contributes no `AGENTS.md`; independently, `project_doc_max_bytes = 0` breaks out of the loop at `remaining == 0`, so the cap alone also yields an empty project doc. Both are set (belt and braces) and M4 keeps the fixture. Fact-checked 2026-09-04 against the raw source.
5. **SDK surface.** `@openai/codex-sdk` (`sdk/typescript/src/*.ts`): `CodexOptions {codexPathOverride, baseUrl, apiKey, config, configOverrides, env}`; `ThreadOptions {model, sandboxMode, workingDirectory, skipGitRepoCheck, modelReasoningEffort, networkAccessEnabled, webSearchMode, approvalPolicy, additionalDirectories}`; `startThread` / `resumeThread(id)`; `run` / `runStreamed` yielding `thread.started`, `turn.started`, `item.started|updated|completed`, `turn.completed {usage}`, `turn.failed`, `error`; items `agent_message`, `reasoning`, `command_execution {command, aggregated_output, exit_code, status}`, `file_change`, `mcp_tool_call {server, tool, arguments, result}`, `web_search`, `todo_list`, `error`; `Usage {input_tokens, cached_input_tokens, cache_write_input_tokens, output_tokens, reasoning_output_tokens}`; `outputSchema` on `run`. Sessions persist under `$CODEX_HOME/sessions`.
6. **Subagents.** `$CODEX_HOME/agents/<role>.toml` with `name`, `description`, `developer_instructions`, `model`, `model_reasoning_effort`, per-role `sandbox_mode`, per-role feature switches (`shell_tool = false`, `web_search = "disabled"`); `[agents] max_concurrent_threads_per_session`, `max_depth` (default 1, `DEFAULT_AGENT_MAX_DEPTH` in `codex-rs/core/src/config/mod.rs:242`, so a subagent cannot spawn), `features.multi_agent` (default on; `features.multi_agent_v2` takes precedence when set, `config_toml.rs:684`). Subagents inherit the parent's sandbox and permission mode.
7. **MCP.** `[mcp_servers.<name>]` stdio (`command`, `args`, `env`, `env_vars`, `cwd`, `startup_timeout_sec`, `tool_timeout_sec`) or streamable HTTP (`url`, `bearer_token_env_var`, `http_headers`), with `enabled_tools` / `disabled_tools`.

**uzi side** (researcher inventory at `a1d3110`):

8. Three query sites: run lane `agent/src/sdk-executor.ts` (options at `:927-995`, `driveTurn` `:2470-2545`), advice lane `agent/src/model-pass.ts` (`:135-180`), chat lane `agent/src/chat-executor.ts` (`:237-270`); all funnel through `agent/src/sdk-messages.ts` (`mapSdkMessage` `:243-275`).
9. Only `PreToolUse` hooks are used anywhere; no `canUseTool`; `permissionMode: "bypassPermissions"` + `allowDangerouslySkipPermissions` (`sdk-executor.ts:975-976`); `settingSources: []` (`:933`, semgrep-ratcheted via `semgrep/settings-sources-isolation.yml`); `disallowedTools: [...ASYNC_DEFERRAL_TOOLS]` (`:980`, #34); `spawnClaudeCodeProcess` in its own process group (`:2496-2505`, B1 reap via `runner-uid.ts:133`); `resume` per turn (`:2492`); full-replacement sparse env from `buildSdkEnv` (`sdk-env.ts:73-126`).
10. Control plane is turn-boundary only: cancel is the signal wiring at `sdk-executor.ts:1044-1050` tripping `state.currentAbort?.abort()` in `trip()` (`:2751`), steer and follow-ups are new resumed turns, no `.interrupt()` anywhere. Checkpoints are git refs around the worktree (`runner.ts:506, 606-724`, `git.ts` `checkpointPack`), harness-agnostic by construction.
11. Every uzi tool is an in-process `createSdkMcpServer`: `signals.ts` (`submit_plan`, `signal_done`, `ask_user`, `report_progress`, `checkpoint`), `memory-tools.ts`, `forge-tools.ts`, `findings-tools.ts`, and the chat-only `uzi-tools.ts`. Subagent reach is wired per `AgentDefinition.mcpServers` (`agents.ts:181-198`).
12. Guardrail layers: Bash screener `screenBashCommand` (`guardrails.ts:605`, hook at `:636`; reasons `:140-145`), path guard `buildPathGuardHook` (`:942`, matcher `Read|Edit|Write|MultiEdit|NotebookEdit|Glob|Grep` at `sdk-executor.ts:851`), agent guard `buildAgentGuardHook` (`:696`), SendMessage alias hook (`:757`); uid split `runnerSpawn` (`runner-uid.ts:110-123`); `PROTECTED_ENV_KEYS` (`sdk-env.ts:29-39`).
13. Builtin prompts: frontmatter is `name`, `version`, `description`, `tools`, `model` (`api/internal/agenttmpl/render.go:16-34`); `toDefinition` (`agents.ts:146-228`) adds `disallowedTools: [Agent, mcp__uzi, mcp__memory]` to every subagent (`:166`). Nine builtins name Claude-only tools `SendMessage`, `TaskUpdate`, `TaskList`, `TaskGet` in `tools:`; the SDK really provides them (#210).
14. Web: no start-run dialog exists. `api.createRun(repoId, issueIid, force?)` (`web/src/lib/api.ts:910`) is the whole request; model and effort live in Run Defaults (`web/src/pages/RunDefaults.tsx:658-708`); the schedule modal carries a model override only, no effort (`web/src/components/ScheduleModal.tsx:977-1041`). The credential gate is single-sourced (`web/src/lib/hasToken.ts`, `web/src/lib/runStream.ts:142-150`). Credential card `web/src/components/AnthropicTokens.tsx`; run header badge cluster `web/src/pages/RunView.tsx:1130-1180`; list rows `web/src/pages/RunsList.tsx:465`; usage panel `web/src/components/RunUsage.tsx:177-180`; model aliases `web/src/components/ModelSelect.tsx:8` (`MODEL_ALIASES`), effort `EffortSelect.tsx:14`.
15. Store: `run_usage` PK `(run_id, session_id, model)` with no provider column (`00062_run_usage.sql:25-36`, extended by `00188_run_usage_leg_key.sql`); no `harness`/`provider` column on `runs` or `workers`; the run-to-credential binding for Claude is `runs.anthropic_secret_id` (`00086_run_anthropic_secret.sql`); credentials live in `user_secrets` with `kind TEXT NOT NULL CHECK (kind IN ('anthropic_token'))` (`00010_user_secrets.sql`, never widened) and `store/enums.go` mandates a matching constant per CHECK value and declares each kind's seal path (`SealedWithMaster` vs `SealedWithDEK`, vault AAD bound to `user_id||kind`); capability vocabulary is `{docker, jvm}` (`api/internal/capability/capability.go:19-37`), template registry `{base, jvm}` (`api/internal/workertmpl/workertmpl.go:22`). Migration head is `00189`.
16. 96 test files under `agent/test/*.test.ts` define the D0 invariant.

## Decision log

- **D0 (from #229): the Claude path is unchanged.** A run with no harness selected, or a user with only an Anthropic credential, gets today's code: same options, same in-process tools, same hooks, same tests. The port is an extraction behind an interface, not a modification. No existing-behavior test changes (SC0).
- **D1: one image, harness is a run property.** `runs.harness text NOT NULL DEFAULT 'claude' CHECK (harness IN ('claude','codex'))`, frozen at creation with the value the D11 resolver produced (the column default exists for pre-feature rows; the service always writes the resolved value). Workers do not declare a harness; the claim query gains nothing for routing, only the D5 seat-lock predicate for `codex_auth` runs. Rationale: the trust boundary is the worker, not the harness; a second fleet buys nothing in phase 1 and would make in-worker mixing later impossible.
- **D2: naming.** `harness` in code, API, schema and CLI flags (`--harness claude|codex`); UI labels "Claude" and "Codex"; credential cards "Anthropic" and "OpenAI / Codex". Not "provider", which #229 uses for the wider matrix.
- **D3: credential kinds.** Two new `user_secrets.kind` values, `openai_api_key` and `codex_auth` (the verbatim `auth.json` from `codex login`), added by one migration that widens `00010`'s CHECK, with matching constants and a declared seal path in `store/enums.go` (both `SealedWithDEK` through the per-user vault, like the Anthropic token, so the `user_id||kind` AAD binding holds). Same slot in `CODEX_HOME/auth.json` either way (#229 D9). No device-code flow in phase 1.
- **D4: refresh write-back on every checkpoint, run-scoped, ordered before the terminal transition.** A Codex seat rotates its refresh token and writes the new `auth.json` on refresh, and a seat idle for about eight days is dead. The worker reads back `CODEX_HOME/auth.json` at every checkpoint publish, at park and graceful shutdown, and at run end, and posts it to `POST /api/worker/runs/{id}/codex-auth` with `{expected_version, auth_json}`. The route is join-token-authenticated like every `/api/worker/*` route, and **the run is the only authority on which secret gets replaced**: the api resolves the run through `GetRunOwnedByWorker` (`runtime.sql:561`, the existing worker-endpoint authz, "a worker may only touch a run it currently holds", via `runOwnedByWorker` at `service.go:2627`), takes the target from `run.codex_secret_id`, and checks server-side that the secret's `kind = 'codex_auth'`, that its `user_id` equals the run's, and that the run's status is in the seat-held set (D5). The request never carries a secret id or a user id, and neither is trusted if present. A stale `expected_version` (the secret changed underneath: a re-upload, or another write-back) is rejected 409; every other mismatch (another run, another worker's run, another user's secret, wrong kind, null binding, terminal or queued run) is rejected 4xx, with one negative test each (M1). The api re-seals through the vault with the same `user_id||kind` AAD as a user-side rotate, so a write-back can never move a secret across users or kinds even if a check above were bypassed. Ordering: `GetRunOwnedByWorker` does not filter on status (`worker_id` survives the terminal transition), so the status check above would drop the run-end write-back if it came after the terminal `SetState`; the worker therefore posts the final write-back **before** it reports the terminal state, and a rejected write-back lands in the run's status frame as a warning, never a silent skip. A run requeued onto a fresh pod therefore seeds from the latest written-back state, not from the original upload. The seed is copied in only when the run's `CODEX_HOME/auth.json` is absent.
- **D5: one in-flight run per Codex seat credential, enforced by the database.** Refresh-token rotation semantics are unverified (whether an old refresh token stays valid after rotation). Until M0 settles it, at most one run per `codex_auth` secret may be in the **seat-held set** `('claimed','running','awaiting_approval','awaiting_input','awaiting_followup')` (the busy set `ListAllWorkers` already uses; `limit_wait` and `pool_wait` are Anthropic parks a Codex run never enters in phase 1); API-key credentials are unconstrained. Two things make that hold, neither sufficient alone. First, **`runs.codex_secret_id` is frozen at run creation** by the D11 resolver, with the `00086`-shaped composite FK `(user_id, codex_secret_id)` and `SET NULL (codex_secret_id)`. It is *not* a mirror of `runs.anthropic_secret_id` in write timing: that column is written at claim assembly (`claimSecretID`, `claim_assembly.go:87`) after the claim commits, so a claim-time predicate keyed on it would not see a competing run's binding at all. Second, **the invariant is a partial unique index**, `runs (codex_secret_id) WHERE codex_secret_id IS NOT NULL AND status IN (<seat-held set>)`, not a `NOT EXISTS` in `ClaimRun`: `ClaimRun` is `UPDATE ... WHERE id = (SELECT ... FOR UPDATE SKIP LOCKED)` (`runtime.sql:588`), and a `NOT EXISTS` subquery there reads the statement snapshot under READ COMMITTED while the row lock covers the run, not the seat, so two workers claiming two queued runs on the same seat both pass before either commits. `ClaimRun` still carries the same predicate as a `NOT EXISTS`, so the steady state never selects a seat-busy run and nothing starves behind it; the index catches only the race window, where the claim UPDATE fails with `23505` and `Claim` treats that as "nothing claimed this poll" (no error surfaced to the worker, the next poll sees the committed claim and skips the run). Release is transition-clean with no extra write: the row leaves the index on any terminal state and on requeue to `queued`, so the sweeper's stale-heartbeat requeue frees the seat, which is the accepted "crashed run holds its seat until the timeout" behaviour. Alternative considered: `pg_advisory_xact_lock` keyed on the secret around the claim (precedent `SecretMutationLockClass`, `store/migrate.go:47`); rejected because the seat is not known before the UPDATE's subquery picks the run, which would force a per-user lock over every claim, Claude runs included. The live-DB criterion (SC8) races two claims from separate transactions, not two calls on one connection. If M0 finds rotation is non-invalidating, the index is dropped for a per-seat concurrency cap (#229 Q3), which then needs the advisory lock, since a cap above 1 is not expressible as a unique index.
- **D6: the layer map for Codex.** Clause → layer: `git push` / remote mutation / force ops / `git config --get` / bare `env` / `ps` / `/proc` / secret paths / wrapper depth → the **Bash screener as a `PreToolUse` command hook** matching the shell tool; outside-worktree and `.git` access → the **path guard as a hook** matching the file tools, plus the uid split; subagent allowlist and nested spawn → the **agent guard as a hook** on `spawn_agent` plus `agents.max_depth = 1`; repo-borne config → the project marked **untrusted** in the user layer plus `project_doc_max_bytes = 0` for `AGENTS.md`, verified by fixture; credential absence → the sparse env (`OPENAI_API_KEY`, `CODEX_API_KEY`, global `CODEX_HOME` stripped) and `CODEX_HOME/auth.json` added to the secret-path deny list; process tree → the wrapper at `codexPathOverride` does `setpriv` to `runner` and `setsid` so the existing group kill applies; **synchronous delegation (#34, B1)** → a delegated subagent must complete within the lead's turn, never backgrounded or deferred: Claude enforces it with `run_in_background:false` in the agent guard and `disallowedTools: [...ASYNC_DEFERRAL_TOOLS]`; on Codex M0(b) establishes whether a `spawn_agent` child thread can outlive the parent turn, and if it can, the adapter closes every child thread at each turn boundary before the pre-push reap and the hook denies `spawn_agent` from inside a subagent, with a fixture for both. Codex's own sandbox is configured `danger-full-access` and is not a layer.
- **D7: uzi tools over MCP; signals stay stream-observed.** Tool handlers are defined once as plain async functions; two adapters expose them: the existing `createSdkMcpServer` for Claude, and a worker-hosted streamable-HTTP MCP endpoint bound to `127.0.0.1` with a per-run bearer token (`bearer_token_env_var`) for Codex. Handlers run in the worker's event loop, so the model still blocks on the tool result. The run-control signals (`submit_plan`, `signal_done`, `ask_user`, `checkpoint`) are **not** latched at the HTTP handler: as on Claude (`scanSignals` and the `isSubagentFrame` rejection in `signals.ts`), the worker latches them only from a main-thread `mcp_tool_call` item observed in the `runStreamed` stream, so a subagent can never mark a run done or submit a plan. Two mechanisms, either sufficient for the guarantee: the `PreToolUse` hook denies every `mcp__uzi__*` signal tool whenever the payload carries `agent_type` (fact 1: present only inside subagents), and the worker's latch additionally requires main-thread attribution on the stream item (M0(b)); if an item's origin is ambiguous it is treated as subagent-originated and rejected (fail closed). With the hook deny in place, M0(b) decides feed attribution quality, not whether M3 can proceed. Per-role reach uses `enabled_tools` in the role's config layer, an allowlist, mirroring `agents.ts:181-198`; the main-thread scan is the harness-independent backstop and is reasserted for Codex, not inherited.
- **D8: per-harness prompt rendering, Claude byte-identical.** The builtin bodies in `api/internal/agenttmpl/builtins/*.md` are **not edited** in phase 1, so the Claude rendering, the pristine-refresh path and every Anthropic run stay exactly as today (D0). The Codex render step, applied at provisioning, maps `tools:` to the role TOML (`shell_tool`, `web_search`, MCP `enabled_tools`, the hook enforcing read-only by `agent_type`) and rewrites the Claude-only tool vocabulary in the body (`SendMessage`, `TaskUpdate`, `TaskList`, `TaskGet`, `Agent`, `Read`/`Edit`/`Grep`/`Glob`) to Codex wording through a fixed substitution table committed next to the renderer, with a test that the Claude render of every builtin is unchanged. Making the bodies harness-neutral at the source is a later, separate change with its own prompt review. The `.claude/agents/` roster is untouched (dev roster, decoupled).
- **D9: per-harness model and effort vocabulary.** A server-owned list per harness (Claude: today's aliases; Codex: an explicit initial list settled in M0 from the models the SDK accepts, with reasoning effort mapped from uzi's effort values to `modelReasoningEffort`). The templates editor's per-role `model` is a per-harness alias; a Claude alias is never sent to Codex and vice versa, the render step falls back to the harness default and records it in the run's status frame.
- **D10: advice lane on either harness.** `model-pass.ts` gains a Codex implementation: one `thread.run` with `outputSchema`, a deny-all `PreToolUse` hook, `sandboxMode: "read-only"`, no MCP. The judge, review and summary settings gain no new knobs; they run on the run's harness when the user has no Anthropic credential, else on the configured Claude model as today.
- **D11: explicit selection never falls back; an implicit default resolves.** One resolver, shared by `POST /repos/{id}/runs`, the chat `start_run` and schedule fire, decides the run's harness in this order: (1) a harness named on the request, or a schedule's non-null `harness`, is **explicit**: used as is, and if the user holds no credential of that kind the run fails `no_credential_for_harness` on the run header and schedule row (silent fallback would move cost onto a seat the user did not choose); (2) otherwise `users.default_harness`, when set and its credential exists; (3) otherwise the sole harness the user holds a credential for, when exactly one; (4) otherwise, both credentials and no usable default, `claude` (D0); (5) no credential at all is the existing gate (`hasToken.ts` in the UI, the create-run rejection in the api). `users.default_harness` and `schedules.harness` are both nullable, null meaning "resolve at creation or fire time"; `runs.harness` and `runs.codex_secret_id` record what was resolved, frozen (D1, D5). Without step 3 a Codex-only user, who by SC3 sees no picker, would start `claude` runs that fail step 1 with no control to change it.

  | credentials held | picker (SC3) | `default_harness` null | `default_harness` names a harness with no credential |
  |---|---|---|---|
  | none | none, gate | gate (5) | gate (5) |
  | Anthropic only | none | `claude` (3) | `claude` (3) |
  | Codex only | none | `codex` (3) | `codex` (3) |
  | both | Run Defaults, Start, schedule | `claude` (4) | cannot occur (both exist) |

  Transitions: adding a second credential reveals the picker and leaves `default_harness` null, so runs stay on `claude` until the user picks (D0); removing a credential does **not** rewrite `default_harness` (it is a preference, not a pin), the dangling value is skipped at step 2 and step 3 resolves to the remaining credential; a schedule pinned to the removed harness fails at step 1 on every fire until the user edits it or re-adds a credential of that kind, and the schedule row says so. Re-adding a credential makes the same default and the same pinned schedules work again with no edit.
- **D12: egress widens fleet-wide, recorded against ADR-0285.** Codex runs need `api.openai.com`, `chatgpt.com` and `auth.openai.com`. The restricted tier's allowlist is a namespace-level FQDN policy (`worker-fqdn-egress.yaml`), so the entries apply to every worker in the tier, including workers running only Claude runs; per-run egress is not expressible at that layer and D1 deliberately has no per-harness fleet. This is accepted as symmetric with the existing `*.anthropic.com` entry (an unauthenticated POST to either vendor is the same exfiltration class) and the ADR-0285 tier table gains the three hosts. Chart-level, shipped in phase 1, not a runtime toggle. **Confirmed by the user 2026-09-04**; the alternative, a per-harness worker pool, would reopen #229 D4 and was rejected.
- **D13: Codex isolation is gated, not just tested.** `settingSources: []` is ratcheted by `semgrep/settings-sources-isolation.yml`, which is blind to a new query surface. The Codex adapter gets its own equivalent: a single `codexThreadOptions()` builder is the only call site allowed to construct `CodexOptions`/`ThreadOptions`, and a semgrep rule fails `sast:semgrep` on any other `startThread`/`resumeThread` call or on a builder missing the untrusted-project marking, `project_doc_max_bytes = 0`, `approval_policy = "never"` and the sparse env.

## Milestones

Each milestone ends with its gate green and its evidence recorded: a `report_progress` entry when the milestone runs on a uzi worker, a *Progress log* entry below when it is maintainer-local or on dev-cluster (M0, the live half of M3, M6). Gates: `task gate:agent`, `task gate:api`, `task gate:web`, `task gate:repo` (which includes `scan:secrets` and `sast:semgrep`), per what the milestone touched.

### Suggested split into child issues

The chain M0 → M2 → M3 → M4 → M5 → M6 → M7 is near-sequential and mixes execution loci, so this PRD is better run as an epic (precedent: #915) with children by file ownership. Phases:

| Phase | Children | Depends on | Locus |
|---|---|---|---|
| 1 | M0 spike + ADR | none | maintainer-local (needs a Codex credential and `api.openai.com`) |
| 2 (parallel) | M1 credentials (`api/`, `web/` card, CLI list) · M2 port (`agent/src`, ships dark, SC0) | M0 | uzi-runnable |
| 3 | M3 adapter, split three ways: image + wrapper + `CODEX_HOME` provisioning · MCP transport + render step · run-lane turns + advice lane | M2 (and M1 for the live half) | uzi-runnable except the live half |
| 4 (parallel) | M4 conformance suite · M5 schema + UI + CLI + e2e axis | M3 · M1 + M2 | uzi-runnable |
| 5 | M6 infra + live verification, then M7 docs + #229 repoint | M4, M5, worker image rolled | dev-cluster, maintainer |

- [ ] **M0: Spike and ADR.** `adr/1106-codex-harness.md` <!-- check-docs:ignore-path: created by this milestone --> records, with evidence: (a) whether `runStreamed` can be aborted mid-turn and what `resumeThread` sees afterwards; (b) whether child-thread items reach the parent `runStreamed` stream and how they are attributed; (c) the exact `tool_name` values the hook receives for the shell tool, `apply_patch`, file reads, `spawn_agent` and MCP tools, and that hooks fire under the SDK's non-interactive `codex exec` path with `--dangerously-bypass-hook-trust`; (d) whether `CodexOptions.env` replaces or merges the process env; (e) whether refresh-token rotation invalidates the previous token (drives D5), and a reproducible lever that forces a refresh on demand (candidate: seeding `auth.json` with an expired access token), which SC4 depends on; (f) the initial Codex model list and effort mapping (D9); (g) the harness port interface (`RunHarness` for the run lane, `AdviceHarness` for the advice lane, one message-mapping contract). Needs a Codex credential and `api.openai.com` reachability, so this milestone runs maintainer-local, not on a uzi worker.
- [ ] **M1: Credentials.** Draft migration `00190_user_secrets_codex_kinds.sql` widening `00010`'s CHECK to `('anthropic_token','openai_api_key','codex_auth')` plus the `store/enums.go` constants and seal-path declaration (D3); the kinds end to end: create, rotate, delete, default, list (`/me/secrets/<kind>`), with the DTO triple (Go struct, `fixtures/api-contract/`, `web/src/lib/apiTypes.ts`); the settings card beside Anthropic tokens; the gate copy becomes "add a credential" and `hasToken.ts` becomes harness-aware; claim response hands the run's Codex credential the way `ctx.oauthToken` is handed today (`sdk-executor.ts:656`); the run-scoped refresh write-back endpoint (D4) with its version check and the seven negative tests (another run, another worker's run, another user's secret, wrong kind, null binding, terminal or queued run, stale version), each asserting no secret row changed; `secretscrub` patterns for `sk-` keys and `auth.json` token fields, fixtures assembled at runtime from parts; CLI: the existing read-only credential listing shows `kind` (credential mutation stays web-only by the CLI's own rule in `api/cmd/uzi/token.go`). Gate: `gate:api`, `gate:web`, `scan:secrets`.
- [ ] **M2: Harness port with zero behaviour change.** The run lane, advice lane and message mapping extracted behind the M0 interfaces with the Claude implementation moved, not edited; tool handlers split from their `createSdkMcpServer` registration (D7 first half); the semgrep `settingSources` ratchet still fires on the Claude adapter. Evidence: `git diff --stat` of `agent/test/` is empty and `gate:agent` is green. Gate: `gate:agent`.
- [ ] **M3: Codex adapter.** Dockerfile adds `@openai/codex` and `@openai/codex-sdk` pinned, with a build guard that runs `codex --version` as the runtime uid; the `codexPathOverride` wrapper (setpriv, setsid, `env -i` rebuild); per-run `CODEX_HOME` on the data volume with `auth.json` seed-if-absent, provisioned `config.toml` (untrusted project, `project_doc_max_bytes = 0`, `approval_policy = "never"`, `sandbox_mode = "danger-full-access"`, hooks, `mcp_servers.uzi`, `[agents]`), and `agents/<role>.toml` rendered from the builtins (D8); the loopback MCP endpoint (D7 second half); the run-lane turns (plan → `submit_plan` → approval → implement ⇄ review → finalize) with `resumeThread` across the gate and across a worker restart, cancel via the M0-verified path, checkpoints unchanged; `runStreamed` mapped to `run_messages` kinds with subagent attribution per M0(b) and the main-thread signal scan (D7); per-turn usage folded with `harness`; the advice lane on Codex (D10); the `codexThreadOptions()` builder and its semgrep rule (D13); the test that the Claude render of all twelve builtins is byte-identical to today, mutation-tested by flipping one substitution and watching it fail (D8). Precondition: M0(b) answered; if child items carry no attribution, subagent frames land in the feed as lead-attributed with a status note, while the hook deny in D7 keeps the signal guarantee intact. Gate: `gate:agent`, `sast:semgrep`.
- [ ] **M4: Guardrail conformance suite.** One adversarial fixture per clause in D6 (`git push`, `git -C x push`, `sh -c 'git push'`, `git config --get`, bare `env`, `/proc/<pid>/environ`, `base64 | sh`, wrapper depth, write outside the worktree, `.git` write, unknown subagent, nested spawn, repo `AGENTS.md` injection, repo `.codex/agents` and `.codex/rules`, `cat $CODEX_HOME/auth.json`), asserted as outcomes on both harnesses (pure predicate on Claude, live hook invocation on Codex with recorded stdin/exit), with a per-adapter "clause exercised" assertion and the clause-to-layer map committed next to the suite. Runs in `gate:agent`; the Codex half needs only the binary, not a credential (hooks are exercised by invoking the hook command directly with M0's recorded payloads). Gate: `gate:agent`.
- [ ] **M5: Schema, routing, UI and CLI.** Migrations (draft numbers, renumbered at merge): `00191_runs_harness.sql` (`runs.harness`; `runs.codex_secret_id` with the `00086`-shaped composite FK and `SET NULL (codex_secret_id)`; the D5 partial unique index over the seat-held statuses), `00192_run_usage_harness.sql` (a `harness` column on `run_usage`, additive, default `'claude'`); `users.default_harness` and `schedules.harness`, both nullable (D11); the D11 resolver as one function shared by `POST /repos/{id}/runs`, the chat `start_run` and schedule fire, which all accept `harness` (DTO triple as in M1), with a table-driven test over D11's state table and transitions; the D5 seat lock (index, the matching `NOT EXISTS` in `ClaimRun`, `23505` handled as no-claim), verified by a live-DB test that races two claims from separate transactions on two queued runs of one seat, placed in an existing `*LiveDB` package so no new package registration in `run-store-it.sh` or `ci.yml` is needed (the latter is a workflow file); an e2e phase in the `e2e/` registry exercising a Codex-selected run through the fakes, offline; web: Run Defaults harness default (both-credentials only), split Start button on board card and issue view, schedule modal select, harness badge in run header and list rows next to `RunCredential`, per-harness `ModelSelect` / `EffortSelect`, usage panel caption per cost model, `no_credential_for_harness` surfaced on run header and schedule row (D11), a Codex-only user's Start creating a Codex run with no picker (D11 step 3), a `codex` mock scenario with a Codex credential, run and usage fixtures satisfying `data.realism.test.ts`; CLI: `uzi run create --harness`, `uzi schedule create|edit --harness`, harness shown in `uzi run get` and `uzi run list` (no CLI setter for the user default, matching `default_model` and `default_effort`, which have none). Gate: `gate:api`, `gate:web`, `gate:repo`, `./e2e/run-store-it.sh`.
- [ ] **M6: Infra and live verification.** Chart egress allowlist entries (D12) and the ADR-0285 tier-table update; **precondition**: the worker image carrying the binary is built and `workers.image.tag` has rolled the dev-cluster fleet (the fleet is decoupled from app releases and moves only on that tag; `scripts/worker-tag-autobump.sh` must fire at the release since the agent runtime surface changed); on dev-cluster (primary, per *We mostly test in k8s*): one real issue run on Codex from plan to MR with `main` untouched, one on Claude on the same worker before and after, judge on Codex for a Codex-only user, a seat surviving a forced refresh (write-back observed as a new secret version); compose smoke unchanged. Evidence recorded in the PRD progress log with run ids.
- [ ] **M7: Docs, #229 repoint, ADR promotion.** User docs: a "Connect OpenAI / Codex" page beside `docs/anthropic-token.md`, harness paragraphs in `getting-started`, `worker-model`, `worker-effort`, `run-cost`, `scheduling`, `judge`; `task docs:sync` committed; `docs/cli.md`; `ARCHITECTURE.md` one-liner; `specs/ai.md` entry; #229's milestones repointed (this PRD is phase 1; judge/advice folded in; mixing and local endpoints later); the M0 ADR finalized. Gate: `gate:repo`, `gate:api` (embedded docs equality).

## Success criteria

0. **No regression (D0)**: with no Codex credential, uzi behaves exactly as today; `agent/test/` unchanged through M2; every existing gate green.
1. A user with a Codex seat runs a labeled issue end to end on Codex on dev-cluster: plan, approval, implementation with subagents, MR opened, `main` untouched. The same worker runs a Claude run before and after.
2. The conformance suite is green for both harnesses and every clause in D6 is asserted as exercised on each.
3. UI is credential-driven: an Anthropic-only user sees no Codex control; a Codex-only user sees no Anthropic-only control (rate-limit meters, token pool toggle, credential badge); a user with both sees the harness picker in Run Defaults, the split Start button and the schedule select; a Codex-only user's Start, with no picker, creates a Codex run (D11 step 3), never a failing Claude one.
4. The rotated seat state survives a pod change: on a running Codex-seat run on dev-cluster, force a refresh with the M0(e) lever, observe the write-back persist a newer secret version, requeue the run onto a fresh pod, and the resumed run authenticates from the persisted value, not the original upload. Eight-day reuse across two real runs is post-deployment soak evidence recorded in the progress log when it occurs, not release acceptance.
5. `run_usage` rows carry `harness`; a Codex-seat run reports tokens with no dollar cost and an API-key run reports metered cost.
6. The judge, review and summary lanes produce output on a Codex-only account.
7. Nothing under `.github/workflows/` changes in any commit of this PRD.
8. Two runs created on the same Codex seat and claimed concurrently from separate transactions: exactly one claims, the other stays queued, and it claims after the first run's terminal state or its stale-heartbeat requeue (live-DB test, D5). The test fails on a `NOT EXISTS`-only claim predicate.
9. A `submit_plan` or `signal_done` call emitted by a Codex subagent is rejected and the run does not transition (conformance fixture, D7).
10. Every `startThread`/`resumeThread` in `agent/src` goes through `codexThreadOptions()`, enforced by `sast:semgrep` (D13).

## Risks

- **R1 (from #229): seat terms of service.** Decided 2026-08-04: proceed under the one-human-one-seat model with API keys as the documented fallback; a per-vendor re-check is a GA precondition, not a prototyping one.
- **R2: refresh rotation and concurrency.** If rotation invalidates the old token, two concurrent runs on one seat kill each other. Mitigation: D5 (one run per seat, a database invariant rather than a claim-time check) until M0 answers; the write-back version check catches a lost race loudly.
- **R3: SDK unknowns.** Abort semantics and child-thread visibility could force a different cancel path (kill the process group, then `resumeThread`) or lose subagent attribution in the feed. Mitigation: M0 answers before M2 starts; attribution degrades to lead-only frames, never to dropped frames.
- **R4: `AGENTS.md` neutralization regresses upstream.** Both switches are verified in source today (fact 4), but either could change in a Codex release. Mitigation: the M4 fixture (a repo `AGENTS.md` with a canary instruction, asserted absent from the model's behaviour and from the session transcript) runs in `gate:agent`, so a Codex version bump that reopens the vector reddens the gate.
- **R5: worker egress.** A uzi worker cannot reach `api.openai.com` today, so M0, M3's live path and M6 cannot run inside a uzi run; they are maintainer-local or dev-cluster steps. Everything else (extraction, hooks, suite, schema, UI) is offline and uzi-runnable.
- **R6: image weight.** The Codex binary adds roughly the size of one more Rust CLI to `base`; acceptable, but the build guard must keep the fake-`ldd` case from #1082 in its checklist even though the launcher does not sniff, so a future launcher change is caught.
- **R7: prompt vocabulary under Codex.** Builtin bodies are not edited (D8), so the pristine-refresh path is untouched; but a customized template (`customized = true`) or a repo-provided agent may name Claude-only tools that the substitution table does not know. Mitigation: the Codex render step strips unknown tool names, records them in the run's status frame, and the conformance suite has a fixture for an unknown tool name. Anthropic runs cannot be affected: the Claude render is asserted byte-identical.

## Open questions

- **Q1**: the initial Codex model list and the effort mapping (M0(f)). Proposed: the two most recent GPT models the SDK accepts, effort `low|medium|high` mapped 1:1, `xhigh` where present.
- **Q2**: resolved into D4 (write-back on every checkpoint, park, shutdown and run end), because a mid-run rotation followed by a requeue onto a fresh pod would otherwise resurrect a stale token.
- **Q3**: whether Codex's hook `permission_mode: plan` can replace the phase-marker file for the plan-turn write strip. Nice to have; the marker file works regardless.

## Progress log

- 2026-09-04: issue filed, feasibility verified (facts 1 to 16), PRD drafted. Reviewed by three lenses (architecture, fact-check, milestones): 14 findings folded in (synchronous-delegation clause in D6, stream-observed signals with fail-closed attribution in D7, checkpoint-time write-back in D4, `runs.codex_secret_id` and the seat-lock criterion, `user_secrets` CHECK migration, D13 isolation gate, CLI scope, fleet-roll precondition, e2e axis, split table); two facts corrected (`AGENTS.md` is trust-gated; the schedule modal has no effort field). D12 egress posture confirmed by the user the same day (fleet-wide, symmetric with `*.anthropic.com`).
- 2026-09-05: review on the issue (four blocking findings, each confirmed against the code before folding). D5: the `NOT EXISTS` claim predicate was racy (statement snapshot under READ COMMITTED, row lock on the run not the seat) and its key column, mirrored from `anthropic_secret_id`, is written at claim assembly after the claim; now `codex_secret_id` is frozen at creation and the invariant is a partial unique index, with the predicate kept for steady state and `23505` handled as no-claim. D4: the write-back endpoint is run-scoped (`GetRunOwnedByWorker`, target from `run.codex_secret_id`, kind and owner checked server-side, request carries no secret or user id), and the run-end write-back precedes the terminal transition because ownership does not filter on status. SC4: the eight-day soak could not close the implementation run; replaced with the forced-refresh, fresh-pod criterion, eight-day reuse moved to soak evidence, M0(e) gains the refresh lever. D11: gained the resolver and state table (explicit never falls back, implicit resolves to the sole credential, both default columns nullable). M0(e), M1, M5, SC3, SC8 and R2 updated to match.
- 2026-09-05: M0 partial checkpoint ([ADR-1106](../adr/1106-codex-harness.md)). The pinned `@openai/codex` and `@openai/codex-sdk` 0.153.2 packages were installed only in private temporary storage and their declared surface was inventoried. A credential-free fake-executable harness passed discriminating checks for full-replacement `CodexOptions.env`, stable `resumeThread` identifier plumbing, and prompt `AbortSignal` interruption of the local CLI child. Those observations prove local process mechanics only, not server-backed resume. No pod or deployment was used. The sanitized live harness remains uncommitted under private temporary storage. The credential-backed tool probe did not run: its hook efficacy is itself under test, so allowing a model to operate beside a copied readable auth home would make the unproven hook a circular exfiltration control. Explicit informed approval was requested and remains pending. M0 is not complete: M0(a)'s server half, M0(b), M0(c)'s live half, M0(e), M0(f)'s live half, and the final M0(g) interface decision remain.
