# PRD #1189: Extend a run's wall-clock budget from the web and CLI, and show the budget on the run page

**Issue:** [#1189](https://github.com/vtmocanu/uzi/issues/1189)
**Status:** Ready for planning; no implementation yet. **Blocked on PRD #1170 merging** (this PRD adds a term to the `workersvc.RunDeadline` function and the health arm #1170 introduces; do not start it on a `main` that lacks them).
**Execution:** Queued for the nightly `Planned` sweep once #1170 is on `main` (auto-approve on, MR rework inherited). api (store, workersvc, handler, settings, slacksvc) + web + CLI + docs. One additive migration. Do not touch `.github/workflows/**`.
**Mock:** `prds/mockups/run-budget-extend-pause-mock.html` (web header at three moments, TUI, CLI, summary table). The TUI section of that mock is PRD #1170's and #1190's; this PRD changes nothing in the TUI.

Use current `main` and a new working branch, never write to `main`.

## Problem

A run's wall-clock budget is frozen when its plan is approved: for a milestone-structured issue run it is `RUN_TIMEOUT × min(milestones, 12)` capped at 8h (`milestoneBudgetCap`, `budgetWallCeilingSeconds`, `api/internal/workersvc/budget.go:9-17`), which on the maintainer's 6h cluster means every run with two or more milestones sits at the 8h cap; otherwise it is the global `RUN_TIMEOUT` (2h default). `SweepRunningTimeout` (`api/internal/store/queries/runtime.sql:2082-2136`) fails a `running` run the moment `started_at + COALESCE(budget_wall_seconds, RUN_TIMEOUT) + budget_paused_seconds` passes, with `fail_origin = 'run_timeout'`. Nothing lets the owner grant that run more time: the only levers are `RUN_TIMEOUT` (global, operator-only, a restart) and `run scope` / `run stop` (finalize what is committed). PRD #1170 adds a near-timeout warning at 85% of the budget and shows the time left, so the owner now learns the run will be killed and still cannot act on it from the page or the CLI.

Measured on the maintainer's cluster over 45 days (PRD #1170's window): one run was killed at 480/480 minutes, another reached 95% and then failed on an unrelated push. Rare, but each one loses the tail of an 8h run to a limit the owner would have lifted had they been asked. The run page also never shows the budget at all: `budget_wall_seconds` and `started_at` are on the run DTO (`api/internal/apitypes/run.go:153`, `:294`) and the web uses them only in mock fixtures.

## Solution

- **An additive extension, never a mutated budget.** New `runs.budget_extension_seconds int NOT NULL DEFAULT 0 CHECK (>= 0)`, added to the sweep's interval, to `RunDeadline` and to the health arm's effective timeout. `budget_wall_seconds` stays immutable and capped at 8h exactly as the sweep's comment demands (`runtime.sql:2110-2115`): the ceiling bounds what the lead can buy itself; a human grants the extension.
- **An owner-only `extend` steering input**, audited in `run_user_inputs` like `scope`, with a body of whole seconds. Total extension per run is capped by a new admin setting `run_extension_cap_seconds` (default 16h; `0` disables extending). Allowed on any non-terminal run of a kind the sweep can time out, including a parked one; refused with 409 on `chat`, `judge` and interactive tasks, which never time out.
- **Web**: on every run that can time out, the header's live elapsed text gains the budget, `3h 48m / 8h` (focusable, tooltip names the deadline). While PRD #1170's near-timeout flag is up, an **Extend time…** button appears in the header (the way Expedite appears only on a queued run) and a near-timeout panel modelled on the limit-wait panel explains what will happen. Extend always opens a chooser (+1h / +2h / +4h / +8h / custom, +2h preselected) that shows used, allowance left and the resulting deadline; it never adds time on the first click.
- **CLI**: `uzi run extend <id> --by 2h`; `uzi run get` shows the extension in its `DEADLINE` row.
- **Slack**: #1170's near-timeout DM gains the viewer-local deadline, time left, the age of the last checkpoint and the exact CLI command; the command is omitted when the cap is exhausted.
- **TUI**: unchanged beyond #1170 (read-only; no extend from the TUI, owner decision 2026-09-07).

## Resolved facts (no internet needed; every anchor read on 2026-09-07 at `main` df9c5a4d, before #1170's implementation)

Because this PRD lands after #1170, which deletes `KeyHealthSlowSeconds`, rewrites the health arm and adds `RunDeadline`, the line numbers below in `health.go`, `settings.go`, `settings/keys.go` and `runs_dto.go` will have moved. **Locate every anchor by symbol name, not by line.**

**Sweep and budget.** `SweepRunningTimeout` (`runtime.sql:2082`, body `:2117-2136`, driver `api/internal/workersvc/sweep.go:45-57`) predicate: `status = 'running' AND started_at < now - make_interval(secs => COALESCE(budget_wall_seconds, @global_timeout_seconds) + budget_paused_seconds) AND kind NOT IN ('chat','judge') AND interactive = false`. The comment at `:2110-2115` says the 8h ceiling is enforced by the freeze writers (`SetRunRunning` `:869`, `CreateApprovePlanInput`) and that `budget_wall_seconds` is trusted as "already-capped, server-only, IMMUTABLE"; that is why the extension is a second column. `budget_paused_seconds` (migration `00173_…`, `int NOT NULL DEFAULT 0 CHECK (budget_paused_seconds >= 0)`) is the exact shape to copy. Migrations head: `00202_run_codex_binding.sql`; draft this PRD's as `00203_run_budget_extension.sql` and renumber at landing (root `CLAUDE.md`, Conventions). `scripts/check-migration-additive.sh` does not flag `ADD COLUMN`.

**The clock after #1170.** #1170 exports `workersvc.RunDeadline(...)` (its *The deadline on the wire* section specifies seven scalars and suggests the row form in its out-of-scope note; **take whichever #1170 shipped**: with the row form the extension is one added term, with scalars it is an eighth parameter and every caller, three sites in #1170's design, is updated in M1) and computes `started_at + COALESCE(budget_wall_seconds, globalTimeout) + budget_paused_seconds`, nil for `chat`/`judge`/`interactive`/non-`running`. The health arm's `effTimeout` in `runningTarget` (`api/internal/workersvc/health.go:285`, `effTimeout` at `:343-345` today; #1170's *The arm* rewrites the block to subtract `budget_paused_seconds`) uses the same inputs. `ListActiveRunsForHealth` (`runtime.sql:2932-2956`) selects the health inputs; #1170 adds `budget_paused_seconds` and `interactive`; this PRD adds `budget_extension_seconds` (then `cd api && go run github.com/sqlc-dev/sqlc/cmd/sqlc@v1.31.1 generate`; see #1170's Risks for the offline hand-edit fallback).

**Steering inputs.** Handler allowlist `runInputKinds` (`api/internal/handler/runs_lifecycle.go:30-31`: follow_up, approve_plan, reject_plan, cancel, revise_plan, answer, stop, scope). DB CHECK `run_user_inputs_kind_check`, last rewritten in `00162_run_scope_steering.sql:31` with the same eight values; widen it with `extend` (a CHECK widening is a relaxed constraint, not flagged as destructive). `SubmitInput` (`api/internal/workersvc/submit.go`): owner scope via `GetRun` (404), terminal guard `ErrRunTerminal` (`:67`, 409), then per-kind branches; the `scope` branch (`:148-160`) parses the body with `strconv.Atoi`, clamps, and calls `submitScopeCeiling` (`:631-655`), which writes `runs.scope_ceiling` and the `kind='scope'` audit row in one statement (`CreateScopeCeilingInput`) and returns the applied value on `SubmitInputResult`. The `extend` branch mirrors it. CLI side: `submitInput(env, gf, c, cmd, runID, kind, body, nil)` (`api/cmd/uzi/run_steer.go:38`), kind consts at `api/cmd/uzi/run.go:20-29`, `run scope` verb `run_steer.go:19-54` is the template (flag validation, `uzicli.Exitf(uzicli.ExitUsage, …)`, stderr hint under `!gf.quiet`).

**Run DTO and its builders.** `apitypes.RunDTO` (`api/internal/apitypes/run.go:73`): `BudgetWallSeconds *int` `:153`, `StartedAt` `:294`, health trio `:238-240`; `RunListItemDTO` embeds it (`:489-490`). Mapping in `api/internal/handler/runs_dto.go` (`StartedAt` `:119`, budget block `:247-249`, `ScopeCeiling` `:252-256`). The board card's `latestRunDTO` is in `api/internal/handler/board.go:89`, `:120-145`, mapped at `:176`, fed by three SELECTs (`:574`, `:975`, `:1079`). Per-run toggle endpoints for precedent: `SetRunWaitOnLimit` (`handler/waitonlimit.go:86`), `SetRunMrReworkEnabled` (`handler/mrrework_run.go:35`), `SetRunPriority` (`PATCH /runs/{id}/priority`, web `api.expediteRun` at `web/src/lib/api.ts:1088`). `store.Run` fields: `StartedAt` `models.go:420`, `BudgetWallSeconds` `:470`, `Interactive` `:496`, `BudgetPausedSeconds` `:502`. The handler holds `cfg config.Config` (`handler/handler.go:45`); `cfg.RunTimeout` is parsed at `api/internal/config/config.go:837`.

**Settings.** Key pattern `KeyHealthStallSeconds = "health_stall_seconds"` (`api/internal/settings/keys.go:83`), default const `:287`, `Defaults` map entry `:401`; validation routing `settings.go:404-406` (`case KeyHealthStallSeconds, …: return validateHealthSeconds(value)`, `{0} ∪ [60, 86400]`). `Known()` (`keys.go:493`) makes unknown keys a 400. Admin card `web/src/pages/adminSettings/HealthSettingsCard.tsx` (`HEALTH_FIELDS` `:12-26`, per-field validator #1170 adds); mock-mode settings default/shape guard/write validator `web/src/mocks/mockApi/settings.ts:72`, `:149`, `:606-614`; `AppSettings` type `web/src/lib/apiTypes.ts:755-762`. Every settings reader iterates `Defaults`, so a new key appears in `GET /api/admin/settings` with no handler change.

**Web run page.** Header meta row `web/src/pages/RunView.tsx:1050-1085`: `StatusPill`, `RunPriorityBadge`, the queued-only Expedite button (`run.status === "queued" && (canSteer ? <Button variant="secondary" size="sm" …>Expedite</Button> : <span className="text-xs text-muted">Only the run's owner can expedite it.</span>)`), autopilot/PRD badges, the live-stage chip, `HealthFlag`. The live elapsed token renders `formatDuration(now - start)` at `:106` (import from `../components/RunEvent` `:49`); the terminal duration at `:1005`. `LimitWaitPanel` (`:489-660`) is the model for the near-timeout panel: prose stating what happens and that nothing is lost, then one row of controls with the non-owner inert text (`:640-657`). `SteerQueueCard` (`web/src/components/SteerQueueCard.tsx:209-210`) hosts the running run's **Stop run** button. Health badge suffix rule and `deadline_at` on `HealthFlaggable` / `Run` / `LatestRun`: #1170 M3. `formatCountdown` (`web/src/lib/limitWait.ts:149`) renders `1h 5m`. Fixtures with `budget_wall_seconds`: `web/src/mocks/data/runs.ts:434`, `:540`, `:816`, `:1080`, `:1164`.

**Slack.** `healthNudgeBlocks(health, reason, base string, runID uuid.UUID)` (`api/internal/slacksvc/health.go:177-195`): one section block (head + reason) and a `🔗` context block with `runLink(base, runID)`. Its caller holds a `store.GetSlackRunContextRow` (`healthContextLabel(rc)` at `:115`); widen `GetSlackRunContext` (`api/internal/store/queries/slack.sql`) with the deadline inputs if it does not already select them. Slack renders `<!date^<unix>^{time}|<fallback>>` in each viewer's own time zone, so the DM needs no user time zone. Thread-reply precedent: `handleLimitResume` (`slacksvc/notifier_state.go:147-200`).

**Checkpoint age.** `runs.checkpoint_tip` (migration `00185`, `TEXT`) is written by the `UPDATE runs SET checkpoint_tip = @checkpoint_tip WHERE id = @id` at `runtime.sql:2924-2928` (deliberately without `updated_at`). It has no timestamp, so "last checkpoint pushed 9 minutes ago" needs `checkpoint_tip_at timestamptz` stamped in that same UPDATE. PRD #1190 needs the same column; both migrations use `ADD COLUMN IF NOT EXISTS checkpoint_tip_at timestamptz`, so whichever lands second is a no-op.

**Docs** (mirrored into `api/internal/uzidocs/embed/` by `task docs:sync`; `TestEmbeddedDocsMatchSource` reddens `gate:api` without it): `docs/run-health.md` (`## What the flags mean` `:14`, `## This is an early-warning aid, not a guardrail` `:51`, `## Tuning or turning a flag off` `:66`), `docs/configuration.md:224` (`RUN_TIMEOUT` row; #1170 rewrites its clamp sentence, this PRD adds the per-run extension sentence after that), `docs/admin-settings.md` Run health table (`:186-199`) and validation bullet (`:256-266`), `docs/cli.md` verb list (`:95-96`) and explanations (`:196-218`). `CHANGELOG.md` `[Unreleased]` uses one bold-title line per entry. `specs/ai.md` last section is `## 623.` (`:24056`); #1170 takes the next; re-check at landing across sibling worktrees. `specs/human.md` lines are one terse sentence tagged `[user, #N]` (e.g. `:717`).

**Kinds.** `api/internal/runkind/runkind.go:28-35`: issue, ci_fix, chat, judge, self_improve, prompt, task, mr_rework. The sweep exempts `chat`, `judge` and `interactive = true` (a task); everything else can time out and can be extended.

## Design

### Store

- Migration `00203_run_budget_extension.sql` (draft number): `ALTER TABLE runs ADD COLUMN budget_extension_seconds int NOT NULL DEFAULT 0 CHECK (budget_extension_seconds >= 0); ALTER TABLE runs ADD COLUMN IF NOT EXISTS checkpoint_tip_at timestamptz;` Down drops `budget_extension_seconds` and, **only if this PRD lands before #1190**, `checkpoint_tip_at`; whichever of the two PRDs merges second removes both the `ADD … IF NOT EXISTS` and the matching `DROP` for that column at landing, so no Down ever drops a column the other migration owns. Additive only. Heads-up: none of the 115 existing `ADD COLUMN` migrations uses `IF NOT EXISTS`; it is plain Postgres and goose runs raw SQL, so it works, but check the migration applies twice cleanly on a scratch database (`goose up`, `goose down`, `goose up`) as part of M1.
- `ConsumeRunInputs` (`runtime.sql:2780`, `… AND p.kind <> 'scope'`) becomes `p.kind NOT IN ('scope', 'extend')`. The exclusion exists because a server-only directive drained by the worker hits `SteeringChannel.route`'s default arm and logs `steering: ignoring unknown input kind` (`agent/src/steering.ts:544-545`); `extend` is server-only exactly like `scope` (D6). Update the comment at `:2777-2779` to name both.
- `SweepRunningTimeout`: the interval becomes `COALESCE(budget_wall_seconds, @global_timeout_seconds) + budget_paused_seconds + budget_extension_seconds`. Nothing else in the statement moves; the comment gains one paragraph naming this PRD and restating that the ceiling still lives at the freeze writers.
- `SetRunCheckpointTip` (`:2924-2928`) also sets `checkpoint_tip_at = now()`; still no `updated_at` bump, for the reason its comment gives.
- New `CreateExtendInput :one` CTE, modelled on `CreateScopeCeilingInput`: `UPDATE runs SET budget_extension_seconds = budget_extension_seconds + @secs, updated_at = now() WHERE id = @id AND status NOT IN ('completed','failed','cancelled') AND kind NOT IN ('chat','judge') AND interactive = false AND budget_extension_seconds + @secs <= @cap RETURNING budget_extension_seconds`, then `INSERT INTO run_user_inputs (run_id, kind, body, disposition) SELECT @id, 'extend', @body, 'applied' …` from the CTE. `disposition = 'applied'` at insert (the CHECK from `00162_run_scope_steering.sql:16-22` admits it) because an extension takes effect in the same statement; a NULL would leave the row "pending" in `uzi run inputs` forever, since nothing settles it the way `SettleScopeInputDisposition` settles a scope row. Zero rows means one of the guards failed; Go distinguishes cap (`ErrExtensionCapExceeded`, 409 with the remaining allowance in the message) from untimed kind (`ErrExtendNotTimed`, 409) from terminal (`ErrRunTerminal`, already 409) by reading the run row it already fetched. That read is the same TOCTOU `scope` accepts: a run that goes terminal between `GetRun` and the CTE is reported as the guard the stale row suggests; harmless (the run is dead either way) and noted here so nobody adds a second query to close it.
- `ListActiveRunsForHealth` gains `budget_extension_seconds`. `GetSlackRunContext` gains whatever `RunDeadline` needs plus `checkpoint_tip_at` and `budget_extension_seconds`.

### The clock

`RunDeadline` adds `budget_extension_seconds` to its sum; `runningTarget`'s `effTimeout` becomes `COALESCE(budget_wall_seconds, RunTimeout) + budget_extension_seconds` so the 85% test moves with the extension and the flag clears on the next sweep tick (the detector re-evaluates every pass and writes `ok` when the arm no longer fires). No other health change; a run that later crosses 85% of the extended budget is flagged and nudged again, which is the D8 behaviour #1170 accepted.

### The input

- `runInputKinds` gains `"extend"`; the CHECK gains `'extend'`.
- `SubmitInput` branch, after the existing guards and before the `stop` branch: body is a base-10 integer of seconds, `>= 60`, else `ErrInvalidExtension` (400). Cap from `settings.RunExtensionCapSeconds(ctx)`; `0` means extending is disabled (`ErrExtendDisabled`, 409, message says the admin has turned extensions off). Audit body: `extend +2h → total extension 2h of 16h; times out 17:20 UTC`, computed **before** the CTE from the fetched row (projected total = the row's `budget_extension_seconds` + `@secs`; projected deadline = `RunDeadline` over the fetched row with the projected total), so the write stays one statement and needs no read-back. UTC in the audit row; the UI localizes. `SubmitInputResult` gains `ExtensionSeconds *int` (the new total) and `DeadlineAt *time.Time` so the CLI can print the outcome without a read-back.
- No worker involvement: the sweep and the health arm read the column; the worker's own soft wall reference (`state.wallRemainingMs`, `agent/src/sdk-executor.ts:1697-1712`) is advisory and already tolerates the server being more generous, so it is left alone. Record in D6.

### The setting

`KeyRunExtensionCapSeconds = "run_extension_cap_seconds"`, `DefaultRunExtensionCapSeconds = "57600"`, in `Defaults`; accessor `RunExtensionCapSeconds(ctx) (int, error)`; validator `validateExtensionCapSeconds`: `0` (disabled) or `[3600, 604800]` (1h to 7d), error text `must be 0 (disabled) or between 3600 and 604800 seconds`. Admin card: a field in the Run health card under the near-timeout percent, label `Extension allowance per run (seconds)`, hint `Total extra wall-clock time an owner may grant a run through Extend; 57600 = 16h. 0 turns extending off.`, with its own client-side validator (the seconds fields keep theirs, the percent field keeps #1170's).

### DTO

`RunDTO` (and so `RunListItemDTO`) gains `budget_extension_seconds int`, `budget_extension_cap_seconds int` (the effective admin cap, so the chooser and the CLI need no second request), `budget_total_seconds *int` (`COALESCE(budget_wall_seconds, RUN_TIMEOUT) + budget_extension_seconds`, nil for kinds that never time out), `budget_used_seconds *int` (**active** time so far: `now - started_at - budget_paused_seconds`, clamped at 0, nil when `started_at` is null; valid in every status, so a paused run reports a frozen figure) and `checkpoint_tip_at *time.Time`. One clock: the header's "used", the chooser's facts and #1170's `deadline_at` are all paused-aware and agree; the raw wall elapsed at `RunView.tsx:106` is **not** what `/ 8h` is measured against, because on a run that sat 32m at its plan gate raw elapsed would disagree with the deadline in the same header. `latestRunDTO` gains none of these: the board card shows the badge only, and the badge's countdown comes from #1170's `deadline_at`.

### Web

- **Elapsed text**: the header's live token becomes `3h 48m / 8h` when `budget_total_seconds` is non-null, where the left side is `budget_used_seconds` aged client-side (not raw wall elapsed) and the right side is rendered from `budget_wall_seconds ?? (budget_total_seconds - budget_extension_seconds)` plus `+<extension>` when `budget_extension_seconds > 0`, so an extended 8h run reads `/ 8h+2h`, never `/ 10h`; `budget_total_seconds` is for arithmetic (time left when resumed, the chooser's facts), not for the label. The element is focusable (`tabIndex=0`, dotted underline) and its tooltip names the deadline in local time; a healthy run gets nothing else. Kinds with a null budget show today's plain elapsed.
- **Conditional header action**: when `shouldShowHealthFlag(run.health, run.status) && run.health === "slow"`, an `Extend time…` secondary-size button renders in the meta row for the owner, with the same inert non-owner text pattern as Expedite (`Only the run's owner can extend it.`). It opens `ExtendTimePopover` (new component under `web/src/components/`): quick picks `+1h +2h +4h +8h`, a custom input accepting `2h`, `90m`, `1h30m`, +2h preselected; facts: `Used 6h 55m of 8h`, `Allowance left 16h of 16h`, `After +2h: 3h 05m left, stops at 17:20`; a pick above the remaining allowance disables the confirm button and says why; confirm posts `submit("extend", String(seconds))` and refreshes the run. When `budget_extension_cap_seconds` is `0` the button does not render and the panel says extensions are turned off.
- **Near-timeout panel**: rendered under the header while the flag is up, modelled on `LimitWaitPanel`: `⚠ This run is close to its time limit`; `It has used 6h 55m of its 8h budget and will be stopped at 15:20 unless you extend it.`; `Work already committed is safe: the last checkpoint was pushed 9 minutes ago (agent/issue-1183 @ 7d21c0a).` (the checkpoint sentence is omitted when `checkpoint_tip_at` is null); one row: `Extend time…`, `Stop run`, and the owner-only note. PRD #1190 adds its `Pause after M5` button to this row when it lands.
- **Chooser also while paused** (#1190): `Extend time…` renders in that PRD's paused panel; nothing to do here beyond exporting the popover.
- Mock mode: `mockApi/runs.ts` accepts the `extend` input, mutates the fixture's `budget_extension_seconds`, recomputes `deadline_at`; settings mock gains the key and its validator branch. Fixtures carrying `budget_wall_seconds` gain `budget_total_seconds` and `budget_extension_cap_seconds`.

### CLI

- `uzi run extend <run-id> --by <duration>` (`run_steer.go`, beside `run scope`): parses `--by` with `time.ParseDuration` plus a `d` suffix (`1d` = 24h) into whole seconds, requires the flag (`uzicli.ExitUsage`), posts `extend`, prints `Extended <id> by 2h. 3h05m left, times out 17:20. Extensions on this run: 2h of 16h allowed.` from the response (times in local zone via the existing CLI time formatting). A 409 with the cap message maps to exit 5 like the other steering 409s and prints the server's message verbatim (`20h exceeds the remaining extension cap (14h left of 16h)`).
- `uzi run get` `DEADLINE` row (#1170) gains `· +2h extended` when `budget_extension_seconds > 0`; `--json` carries the four new fields for free.
- `docs/cli.md` verb list and explanation; the embedded skill (`api/internal/uzicli/skill/SKILL.md`) if it enumerates run verbs.

### Slack

`healthNudgeBlocks` for `slow` gains, after the head: `Stops at <!date^<deadline unix>^{time}|HH:MM UTC> (1h 05m left). Last checkpoint pushed 9m ago.` (checkpoint clause omitted when null) and a second context line ``Extend: `uzi run extend <id> --by 2h` `` omitted when `budget_extension_seconds >= cap` or the cap is `0`. The fallback string stays the head sentence alone. The other four flags' DMs are unchanged.

## Milestones

- [ ] **M1: store, clock, input, setting** (the long pole; everything in phase 2 waits on it). Migration as designed; `SweepRunningTimeout` interval; `SetRunCheckpointTip` stamps `checkpoint_tip_at`; `CreateExtendInput` CTE with `disposition = 'applied'`; `ConsumeRunInputs` excludes `extend`; `ListActiveRunsForHealth` and `GetSlackRunContext` widened; `sqlc generate`. `RunDeadline` and `runningTarget` add the extension. `extend` in `runInputKinds` and the CHECK; the `SubmitInput` branch with its four errors; `SubmitInputResult` fields. Settings key, default, accessor, validator, `Known()` coverage. DTO fields (`budget_extension_seconds`, `budget_extension_cap_seconds`, `budget_total_seconds`, `budget_used_seconds`, `checkpoint_tip_at`). Tests: live-DB (`.claude/rules/go.md`): a running run with an 8h budget and a 2h extension is not swept at 8h01m active and is swept at 10h01m; the CTE refuses over-cap (0 rows) and terminal and `chat`; the audit row lands in the same statement with `disposition = 'applied'` (fail the INSERT, see no UPDATE); an `extend` row is never returned by `ConsumeRunInputs` while a `follow_up` row beside it is; `SetRunCheckpointTip` stamps the time. Unit: `RunDeadline` adds the extension; the arm does not flag at 7h active of 8h+2h and flags at 8h30m; extend on a `queued` run succeeds; `60` accepted, `59` rejected; cap `0` yields `ErrExtendDisabled`; settings validator bounds (`3599`, `604801`, `-1`, `abc` rejected; `0`, `3600`, `604800` accepted); a handler test that `GET /api/runs/{id}` serves the four fields with `budget_total_seconds` null for a chat run. Mutation check: drop `+ budget_extension_seconds` from the sweep interval and watch the 8h01m test redden; drop it from `RunDeadline` and watch its test redden. Gate: `task gate:api`.
- [ ] **M2: web.** Elapsed text with budget and focus behaviour; conditional `Extend time…` header action; `ExtendTimePopover`; near-timeout panel; `api.ts` uses the existing steering submit path; `Run` type fields; admin card field and validator; mock mode; fixtures. Tests: the header shows `/ 8h` for a running issue fixture and plain elapsed for a chat fixture; the button renders only when `health === "slow"` on a flaggable status and only for the owner (non-owner sees the inert text); the popover's confirm is disabled when the pick exceeds `cap - extension` and the reason names the allowance; confirm posts `extend` with the seconds body; after a mocked extension the header reads `/ 8h+2h` and the flag chip is gone once the mock recomputes health; the panel omits the checkpoint sentence when `checkpoint_tip_at` is null; the admin card rejects `3599` and accepts `0`. Gate: `task gate:web`.
- [ ] **M3: CLI.** `run extend` verb, duration parsing (`2h`, `90m`, `1h30m`, `1d`; `0`, negative and `abc` are usage errors), output line, 409 mapping; `run get` `DEADLINE` row extension clause. Tests in `api/cmd/uzi`: parse table; the verb posts kind `extend` with the seconds body; the success line names the new deadline and the allowance; a 409 body is printed verbatim with exit 5. Gate: `task gate:api`.
- [ ] **M4: Slack.** The two added lines in `healthNudgeBlocks` for `slow`, fed from the widened run context; tests in `slacksvc`: the deadline uses the `<!date^…|…>` form with a UTC fallback; the checkpoint clause is omitted when null; the command line is omitted at cap and when the cap is `0`; the other flags' blocks are byte-identical to before. Gate: `task gate:api`.
- [ ] **M5: docs, specs, changelog.** `docs/run-health.md`: a `## Giving a run more time` section (what Extend does, the cap, who may do it, the CLI form) and one sentence in the near-timeout row; `docs/configuration.md:224` gains the per-run extension sentence; `docs/admin-settings.md` gains the setting row and its validation bullet; `docs/cli.md` the verb. `task docs:sync` and commit the mirror. `specs/human.md`: `A run's owner can extend its wall-clock budget from the web or CLI, up to an admin-set allowance; the frozen budget itself never changes. [user, #1189]`. `specs/ai.md`: a section (next free number at landing) recording the additive column, the cap, the kinds rule and why the ceiling stays at the writers. `CHANGELOG.md` `[Unreleased]` → `### Added`. Gate: `task gate:repo` plus the `gate:api` rerun for the embed mirror.
- [ ] **M6: whole-tree validation.** `task gate` once, to a log, read from the log (root `CLAUDE.md`, Run economy). `git diff --name-only <base>..HEAD` shows nothing under `.github/workflows/`; `task check:migration-numbering` passes; the migration number is the next free above the live head at landing.
- [ ] **M7 (maintainer, post-merge, not for the worker):** after the next release reaches the cluster, extend a live near-timeout run by 1h from the web, confirm the flag clears within one sweep tick, `uzi run get` shows `+1h extended`, the Slack DM carried the command, and the run is swept only past the extended deadline.

### Execution plan

| phase | milestones | files | notes |
|---|---|---|---|
| 1 | M1 | `store/migrations`, `store/queries/runtime.sql`, `slack.sql` + generated, `workersvc/{health,submit,budget}.go`, `handler/{runs_lifecycle,runs_dto}.go`, `apitypes/run.go`, `settings/*` | the wire contract everything else consumes |
| 2 (parallel) | M2, M3, M4 | M2 `web/src/**`; M3 `api/cmd/uzi/run_steer.go`, `run_render.go`; M4 `api/internal/slacksvc/health.go` + `store/queries/slack.sql` if not widened in M1 | disjoint files once M1's DTO and errors exist |
| 3 | M5 | `docs/*.md`, embed mirror, `specs/*.md`, `CHANGELOG.md` | after wording is final |
| 4 | M6 | none | one full gate |

## Decision log

- **D1: an additive column, the frozen budget stays immutable.** The sweep trusts `budget_wall_seconds` as an already-capped server-only value and says the cap must stay at every writer; the 8h ceiling exists so a lead cannot buy itself budget through milestone count. A human-granted extension is a different trust and gets its own column, so neither invariant moves. Rejected: `UPDATE runs SET budget_wall_seconds = …` from the handler.
- **D2: a steering input, not a PATCH endpoint.** `scope` shows the shape: one CTE writes the control and the audit row, the owner and terminal guards are `SubmitInput`'s, and the CLI reuses `submitInput`. The audit row is the point: an extension changes when a run dies, and `uzi run inputs` should show who granted it and when. Rejected: `PATCH /runs/{id}/budget` like priority (no audit trail).
- **D3: the cap is a per-run total from an admin setting, `0` disables.** A per-extension cap is bypassed by repeating; a hard-coded cap would need a release to change. 16h default: two full ceilings, generous for a human decision and still finite. Bounds `[1h, 7d]` because a cap under an hour is noise and over a week is no cap.
- **D4: extend is allowed on any non-terminal, timed run, including a parked one.** The column is additive and inert until the run is `running`; refusing it on a gated run would make an owner wait for the run to resume before granting time it may need immediately. Kinds the sweep never times out (`chat`, `judge`, interactive tasks) are refused with 409 rather than silently accepted, so a script cannot believe it did something.
- **D5: Extend always opens the chooser; there is no one-click add.** From the 2026-09-07 UX review of the mock: a state-changing action with a hidden magnitude is the wrong default; the chooser preselects +2h so the common case is still two clicks.
- **D6: the worker is not told.** Its wall reference (`wallRemainingMs`) is a soft self-check that is already looser than the server's sweep for scaled runs; making it authoritative would be a second clock. If a worker ever self-trips `REASON_WALL` on an extended run, the fix is to serve the extension on the running-report ACK the way `wallSeconds` is served today, as a follow-up.
- **D7: healthy runs show text, not controls.** Mirrors the #1170 TUI decision and the Expedite precedent: the header carries state, and grows an action only when the state calls for one. The 85% flag fired on 2 runs in 45 days; a permanent button is clutter.
- **D8: server-computed `budget_total_seconds` and `budget_extension_cap_seconds` on the DTO.** Clients never add `RUN_TIMEOUT` (which they do not know) to anything, and the chooser needs the cap without an admin-only settings call.
- **D9: Slack uses `<!date^…>`.** Viewer-local time with no stored time zone; the fallback is UTC. The command is omitted at cap so the DM never tells someone to run a command that will 409.
- **D10: `checkpoint_tip_at` lands here with `IF NOT EXISTS`.** #1190 needs it too; whichever merges second is a no-op. The panel and the DM both need "how old is the last checkpoint", which the tip SHA alone cannot answer.
- **D11: no TUI change.** Read-only surface; #1170 already shows the countdown there. Owner decision 2026-09-07.

## Success criteria

- A running issue run with an 8h budget extended by 2h is failed by the sweep only after 10h active; `deadline_at`, the header, `uzi run get` and the Slack DM all name the same new deadline.
- Extending clears the near-timeout flag on the next sweep tick and the run page loses the chip and the panel; a later crossing of 85% of the extended budget re-flags it.
- The cap is enforced server-side (409 with the remaining allowance), reflected in the chooser (confirm disabled) and the CLI (message printed verbatim); `0` turns the feature off everywhere at once.
- A healthy run's header differs from today only by the ` / 8h` text; a chat run's header is unchanged.
- `task gate` green; one additive migration; no workflow file in the diff.

## Risks

- **Rollout skew.** A new SPA against an api pod without the four DTO fields sees `undefined`: the header falls back to plain elapsed and the button does not render (`budget_extension_cap_seconds` undefined is treated as "unknown", not `0`). Keep every new web field optional; the M2 tests pin the fallback.
- **Two drafts numbered 00203.** #1190 drafts `00204`; at landing renumber to the live head. `check:migration-numbering` in `gate:repo` catches a duplicate prefix.
- **`sqlc generate` needs the module proxy** (see #1170's Risks for the hand-edit fallback shape).
- **A worker's soft wall.** D6 accepts that a very long extension could see the worker self-trip its advisory wall; measured today the server's scaled budget already exceeds the worker's initial reference and runs do not self-trip, because `wallSeconds` on the ACK lifts it. If M7 shows otherwise, serve the extension on the ACK.
- **Health nudge repeat.** After an extension the run may re-cross 85% and DM again; accepted per #1170 D8.

## Out of scope

- Pausing and resuming a run (PRD #1190), the `paused` status, and the TUI vocabulary for it.
- Any TUI change; any change to `RUN_TIMEOUT`, the freeze or the 8h ceiling.
- Auto-extension policies (an autopilot run extends itself). The point of the ceiling is that only a human grants time.
- A per-user or per-repo cap; the cap is instance-wide.
