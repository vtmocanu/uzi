# PRD #103 M3 — Linting: golangci-lint + oxlint, each ratcheted its own way

**Branch**: `feature/prd-103-m3-m6`
**Worktree**: `/home/user/repos/myorg/vtmocanu/uzi/prd-103-m3-m6`
**Base**: `main` @ `31a36412`
**Spec of record**: `prds/103-dev-loop-quality-gates.md` — the M3 bullet, plus Decisions 1, 2, 3, 4, 8, 9, 10, 11 and Success Criteria 1, 2, 3, 4, 8. **Read those before anything here.** This brief carries only the deltas, the open design points, and the things a cold-starting teammate cannot infer.

**Amendments are dated inline.** Re-read this file before each commit; the lead amends it here rather than sending requirements by message.

---

## What M3 ships

1. `.golangci.yml` (v2 schema) covering **both** Go modules — `api/` and `controller/`.
2. `web/.oxlintrc.json` + `agent/.oxlintrc.json`, oxlint as a devDependency in both `package.json`s.
3. `Taskfile.yml`: `lint:api`, `lint:controller`, `lint:web`, `lint:agent`, composite `lint`, wired into the existing `gate:*` targets.
4. `.gitlab-ci.yml`: jobs in the (currently empty) `lint` stage that invoke those same targets.
5. Doc updates closing the `lint none (gap)` slot in **two** places (Success Criterion 4).
6. A `CHANGELOG.md` `[Unreleased]` line — **M3 owns its own entry**; the `publish:assert-changelog` gate is satisfied by any `#103` already present, so it cannot catch a missing one.

## Non-negotiable traps (all three are silent-green shapes)

- **🔴 `oxlint src test` EXITS 0 WHILE REPORTING FINDINGS.** `correctness` defaults to `warning` severity and warnings do not set the exit code. A target without `-D correctness` (or `--deny-warnings`) is a lint job that can never fail. This is the whole reason the calibration below must assert a **nonzero exit**, not merely that output appeared.
- **🔴 The oxlint `react` plugin is OFF BY DEFAULT**, and **`rules-of-hooks` is `pedantic`, not `correctness`.** A config that enables the plugin and stops there still never runs the rule this milestone exists for. Both must be in the shipped config; neither is discoverable from a clean run.
- **🔴 Do not gate with `test -z "$(cmd)"`.** M2 proved that shape fails OPEN when the tool exits nonzero and writes to stderr. Where a target must inspect output, use the assignment-plus-explicit-`|| exit N` form already shipped in `Taskfile.yml`'s `fmt-check:api` — read that recipe, do not restate one. Where the tool sets its own exit code correctly (golangci-lint always; oxlint **only** with `-D correctness`), let the exit code do the work and say so in a comment.

## Taskfile constraints (from `Taskfile.yml`'s own header — read it)

No `sources:` / `generates:` / `status:` on any target. No `dotenv:`. No `includes:`. No dynamic root `vars:`. Never splice a variable unquoted into a `cmds:` line. Serial, not concurrent. Every load-bearing flag gets an inline comment naming why it is there.

## CI constraints

- `task` is installed by the shared `.task_setup` fragment; reuse it, do not re-derive it.
- **The CI job must provide the tool — never devbox** (Decision 7). For golangci-lint prefer the official digest-pinned `golangci/golangci-lint` image over installing into `golang:1.26`. oxlint arrives via the existing `npm ci` as a devDependency. Anything fetched in a `before_script` must be version- **and** sha256-pinned, matching `.task_setup` and `e2e:kind-smoke`.
- The `lint` stage entry already exists in `stages:` (M1 added it, deliberately empty). Do not add it again.
- Every existing gate job sets `needs: []`; keep that property so the new jobs start at T+0 and the stage stays organisational, not ordering.

## Measurements this milestone must take and record (Decision 10)

Every figure below is stale by construction — the PRD's own numbers were measured on a different tree. **Re-measure on this branch, record the tool version and the exact invocation beside each number, and put them in the MR description.**

- oxlint findings for `web/` and `agent/` at the shipped config (`--react-plugin` tier). PRD's stale reading: 10 agent / 6 web / 16 combined. M3 **fixes** these rather than baselining them.
- The **unfiltered** golangci-lint finding count for each Go module. `--new-from-merge-base` reports nothing on `main` pipelines, so this debt is otherwise uncountable. Go burn-down gets its own issue — M3 does not fix Go findings.
- The pedantic-tier total, only if you touch it: the PRD records **two conflicting figures** (3,015 vs 2,976) and explicitly says do not adopt either. Out of scope for M3 anyway (scope ruling: correctness-only).

## Scope ruling — correctness-only

Type-aware linting (`oxlint-tsgolint`) is **its own issue**, not M3. The PRD's reason is behavioural, not budgetary: 119 of web's type-aware findings are `onClick={asyncHandler}`, and wrapping a promise-returning handler changes what happens to a **rejection**. That is 119 chances to change error behaviour under cover of a lint fix. Do not let it ride in.

The React Compiler rule family is likewise excluded — this repo is on React 18.3.1 and does not run the compiler.

## Calibration (Success Criteria 2 and 8) — the acceptance bar

Two mutations, each proving its own gate is live, **each asserting a nonzero exit**, both recorded in the MR description with the exact command and its output:

1. Add an unchecked error return in a scratch Go file → `errcheck` fires through `task lint:api` → **nonzero exit** → remove it → green.
2. Add a violating hook call in a scratch web file → `rules-of-hooks` fires through `task lint:web` → **nonzero exit** → remove it → green.

`CLAUDE.md`'s rule applies verbatim: *a control that produces no output is not a control.* A first run reporting nothing is indistinguishable from a linter that is not wired up. Restore with a `cp`-based backup, **never `git checkout --`** (it reverts to HEAD and silently wipes uncommitted work).

Do the same for `lint:controller` and `lint:agent` if it is cheap; if you skip either, say which and why in the report rather than leaving it implied.

## Documentation edits (Success Criteria 3 and 4)

- **`.claude/agent-team.md`** — the `## Quality gates` paste-block. Its `lint` slot currently reads `none (gap)` with an explanatory comment. Replace with real **targets** (never recipes, per Decision 9). The `lint` slot carries **no** `noted` marker, so there is none to remove there.
- **`.claude/agents/tester.md`** — a **second, full copy** of the slot table in its `## For this repo` tail. It must get the same change. **Asymmetry warning from the PRD:** `agent-team.md` carries four `noted` markers (`dead code`, `coverage`, `security scan`, `pre-commit`); `tester.md` carries none and never has. A pass told to "remove the markers in both" will find only one file has any — that is expected, not a sign of editing the wrong file.
- **`.claude/agents/coder.md`** — its tail names the gate slots the coder must run before reporting done. Add the lint slot; the coder's generic body says "every slot named in your tail", so an unnamed slot has no owner.
- **`CLAUDE.md`** §Commands — add the new targets. **Command lines only.** Every measurement paragraph stays byte-identical: a command written as an *instruction* is a recipe and becomes a target; a command written as an *observation* is evidence, and rewriting it makes the paragraph describe a run nobody performed. Sweep the whole file, not the line someone names.
- **`docs/dev-conventions.md`** — M1 added the gate; M3 adds its check.

## Decision 11 — citation discipline

**No line anchor into any file this PRD's milestones edit.** That set includes `.gitlab-ci.yml`, `Taskfile.yml`, `.claude/agent-team.md`, `.claude/agents/*.md`, `CLAUDE.md`, `.gitignore`, `web/vite.config.ts`, `web/scripts/check-docs.mjs`, and both `package.json` files. Cite the **job name**, the **target name**, the **slot name**, or an **exact quoted string**. A paraphrased cite fails silently where a quoted one greps.

## Open design points — SETTLE THESE IN THE DESIGN WAVE, BEFORE ANY CODE

1. **Where does the `--new-from-merge-base=main --whole-files` ratchet live — the Taskfile target, or the CI job only?** Success Criterion 1 says `task gate` reproduces every toolchain check CI runs *and CI runs no toolchain check `task gate` does not*. Putting the ratchet only in CI breaks that symmetry; putting it in the shared target means a contributor on `main` lints nothing. Both readings are defensible. Pick one, state the cost, and write the reason beside the target.
2. **One root `.golangci.yml` or one per module?** golangci-lint v2 walks up from the working directory for config, so a single root file may cover both modules given the `dir:` each target uses — **verify by running it, do not reason about it.** Per-linter test-file exclusions differ between the modules in principle; check whether they do in fact.
3. **Does the ratchet need `GIT_DEPTH: "0"` alone, or also an explicit `git fetch origin main`?** The PRD says merge-base needs the *ref*, not merely the depth, and MR pipelines do not fetch `origin/main` by default. Establish which, by reading the CI config rather than assuming.
4. **`goconst` on a repo of this size** — git-manager's own CLAUDE.md records 136 goconst warnings accumulating unnoticed. Confirm it is worth enabling here given the ratchet, or justify leaving it off with a one-line note in the config (that file's convention: every *disabled* linter carries a justification).

## Definition of done

- `task lint`, `task lint:api`, `task lint:controller`, `task lint:web`, `task lint:agent` all exist and run.
- `task gate:<component>` includes the component's lint step; `task gate` still passes.
- Both calibrations recorded with commands, output and **nonzero exits**.
- oxlint findings fixed (not baselined); unfiltered Go count recorded, not fixed.
- Both slot-table copies updated; `CLAUDE.md` and `docs/dev-conventions.md` updated; measurement paragraphs untouched.
- `CHANGELOG.md` `[Unreleased]` carries an M3 line.
- Full `task gate` green on the final tip before handoff.

---

# AMENDMENT 1 — 2026-08-02, after the design-critique wave. THE DESIGN IS NOW FROZEN.

The wave (reviewer + auditor, both read at `06404d51`) produced two HIGH findings, four MEDIUMs and a documentation-deletion sweep. **Everything below supersedes the "Open design points" section above, which is now closed.** Where this amendment and the body disagree, this amendment wins.

Two claims below were re-derived by the lead rather than relayed: the `.gate_needs`/`.publish_needs` enumeration, and the golangci-lint v2 config schema accepting the ratchet keys.

## R1 — EXTEND THE EXISTING `validate:*` JOBS. DO NOT OPEN NEW `lint`-STAGE JOBS.

`.gate_needs` and `.publish_needs` enumerate gate jobs **by name** (lead-verified at `06404d51`: nine names in the first, eleven in the second). `*gate_needs` is consumed by the kaniko build template and `e2e:kind-smoke`; `*publish_needs` by the publish-image template and `publish:chart`. **A new lint job absent from both means a `v*` tag pushes every image and the OCI chart to Harbor while the lint job is failing.** The pipeline reddens afterwards; the artifacts are already out.

This is not inference. `validate:api`'s own script comment already records it as the reason M2 did not open a new job: *"No new job, because `.gate_needs` and `.publish_needs` enumerate jobs BY NAME. A new lint-stage job absent from both produces this repo's signature failure shape: a red job nothing `needs:`, with the images and chart publishing anyway."*

**The PRD contradicts itself here and the contradiction is resolved in favour of the later, more specific statement.** Decision 3 prescribes new jobs (`lint:api`, `lint:controller`, …). M1's own Files section prescribes the opposite for web: *"at M1 that is `task check-docs:web` + `task typecheck:web` for `validate:web` … **M3 adds `task lint:web` to the first**"*. M1's wins. Extending removes the failure mode instead of mitigating it, adds no `.task_setup` fetch and no new `cache:` cycle, and follows M2's precedent.

**Record the deviation from Decision 3 in the PRD as part of this milestone** — a spec that disagrees with the shipped code is the failure this PRD spends its length on.

**Consequence M5 inherits, and it is not optional:** M5's checks are repo-wide (`shellcheck`, `yamllint`, `gitleaks`, `govulncheck`), so they genuinely cannot fold into a per-toolchain `validate:*` job and M5 **will** open lint-stage jobs. Adding them to **both** anchor lists is M5's definition of done, and the two lists become a second single-hot-line contention that the PRD's Parallelization section does not list (it names only `stages:` and the two `package.json`s). Note this in the PRD too.

## R2 — RATCHET IN `.golangci.yml`, REF IS `origin/main`, WITH A PRE-FLIGHT ASSERTION

```yaml
issues:
  new-from-merge-base: origin/main
  whole-files: true
```

- **Config, not a command-line flag.** Lead-verified, golangci-lint 2.12.2, 2026-08-02: `config verify` exits **0** on those keys and **3** on a bogus sibling key, naming it (`additional properties 'no-such-key' not allowed`) — so the positive control fires and the green means something. One recipe, byte-identical locally and in CI, Success Criterion 1's symmetry exact, and no variable spliced into a `cmds:` line.
- **`origin/main`, never bare `main`.** A bare `main` resolves in this bare-clone-with-worktrees layout and does **not** resolve in GitLab's detached-HEAD checkout, where no local branch exists. That is a check that works on your machine and degrades in CI — precisely the divergence SC1 exists to prevent.
- **🔴 THE FAILURE MODE IS FAIL-OPEN-INTO-NOISE, AND IT IS WHY THE PRE-FLIGHT IS MANDATORY.** Measured by the auditor: an unresolvable ref does not skip the ratchet and does not error — golangci-lint logs a single `level=warning msg="[runner] Can't process results by diff processor…"` and reports the **entire unfiltered finding set**. The first MR then reddens with the whole pre-existing debt and the only explanation is one warning line buried in a `prefixed` log.

  So `lint:api` and `lint:controller` open with a **literal** pre-flight, in the Taskfile so local and CI both get it:
  `git rev-parse --verify -q 'origin/main^{commit}' >/dev/null || { echo "lint: origin/main is unresolvable — the ratchet would silently report the whole backlog; fetch it (GIT_DEPTH=0) before linting"; exit 2; }`
  Literal ref, no splice. **`exit 2` not `exit 1`**, matching `fmt-check:api`'s convention: 2 = the instrument is broken, 1 = there are findings. `task`'s own rc is 201 for both, so the distinction has to live in the recipe.
- **CI needs `GIT_DEPTH: "0"` AND the ref actually fetched.** Depth alone is not sufficient and the repo already knows it: `publish:assert-changelog` is the only job setting `GIT_DEPTH: 0` today, it also sets `GIT_STRATEGY: clone`, **and it still runs an explicit `git fetch`**. Whether `origin/main` exists after a fetch is a property of the runner's refspec, not of `.gitlab-ci.yml` — so this is settled by running a pipeline, not by reading config. The pre-flight is what makes a wrong guess loud.
- **Rejected — `CI_MERGE_REQUEST_DIFF_BASE_SHA`.** It sidesteps the ref question and the auditor is right that it is available on `merge_request_event` pipelines. It is also **CI-only**, so it forces either a banned Go-template splice or a shell env reference that is empty locally and then **silently lints everything**. That trades a loud CI problem for a silent local one, in the milestone about silent-green gates.

## R3 — INVOKE golangci-lint VIA PINNED `go run`, INSIDE THE ALREADY-PINNED `golang:1.26`. VERIFY BEFORE COMMITTING.

The official `golangci/golangci-lint` image is a **third image family** and the auditor measured why it is the wrong choice: it ships Go **1.26.2** with **`GOTOOLCHAIN=auto`**, while both `go.mod` files require **1.26.4** — so `go: downloading go1.26.4` happens on every lint run. The pinned `golang:1.26` this pipeline already uses is 1.26.4 with **`GOTOOLCHAIN=local`**, so no existing job fetches a toolchain and switching is disabled outright. Adopting that image would make lint the first job whose effective Go toolchain is fetched rather than pinned.

**Preferred:** `go run github.com/golangci/golangci-lint/v2/cmd/golangci-lint@v2.12.2 run …`, exactly matching the `sqlc@v1.30.0` precedent already living in `validate:api`. Sumdb-verified, version pinned identically local and CI (which also kills the unpinned-local-binary divergence), no new image, no `.task_setup` change.

**Do NOT use `go get -tool`.** That writes golangci-lint's whole dependency tree into `api/go.mod`, and this repo keeps that file deliberately clean — the separate `controller` module exists precisely so `k8s.io/client-go` stays out of it. A module-versioned `go run pkg@version` builds in its own context and does not touch `go.mod`; confirm that is still true when you run it.

**MEASURE IT BEFORE COMMITTING.** Real risks: upstream discourages `go install` of golangci-lint, cold-`GOCACHE` build time, and version-info embedding. If it proves unworkable, fall back to the digest-pinned image **with `GOTOOLCHAIN: local` set explicitly in the job's `variables:`** so a version mismatch fails loudly instead of fetching — and say in the MR which route you took and what the measurement was. The auditor derived the digest two independent ways (Docker Hub tags API and the registry `docker-content-digest` header, agreeing); derive it yourself rather than copying, and note the image runs as uid 0, has apk but no curl (so `.task_setup`'s apk branch fires), and needs no `entrypoint: [""]` override.

## R4 — PIN oxlint SEVERITY IN THE CONFIG. THERE ARE FOUR HOLES, NOT ONE.

The brief above named one. The reviewer measured four, on a fixture carrying one conditional `useState` and one duplicate object key:

| invocation | exit | `rules-of-hooks` fired |
|---|---|---|
| `oxlint src` | **0** | no |
| `oxlint -D correctness src` | 1 | **no** — exits 1 for the *other* finding |
| `oxlint --react-plugin src` | **0** | **no** |
| `oxlint -D react-hooks/rules-of-hooks src` (no plugin) | **0** | **no** |
| config `plugins:[react]` + rule at `"error"` | 1 | yes |
| same config, rule at **`"warn"`** | **0** | yes — *printed, exit 0* |

Three of those are what a careful reader reaches for after being told "the plugin is off by default" or "`rules-of-hooks` is pedantic". So:

- The shipped `.oxlintrc.json` lists `react` in `plugins` **and** sets `react-hooks/rules-of-hooks` to `"error"` explicitly. Severity lives in the config, not in a flag.
- `-D correctness` still goes on the target for the correctness tier, with a comment saying it **cannot** reach `rules-of-hooks` (not a correctness rule) so it is not the guard for it.
- A rule left at `"warn"` prints its violation and exits 0. Nothing in this milestone ships at `warn`.

## R5 — BOTH CALIBRATIONS ASSERT THE RULE NAME, NOT ONLY A NONZERO EXIT

The bar written above ("→ nonzero exit") is satisfiable by row 2 of that table with the rule never firing. Assert the emitted rule name in the output **and** rc≠0:
- Go: the string `(errcheck)` appears.
- Web: the string `react-hooks(rules-of-hooks)` appears.
Restore with a `cp`-based backup, never `git checkout --`, and verify the restore with `git status` rather than a grep count.

## R6 — THE UNFILTERED RECORDING RUN CARRIES `--max-issues-per-linter=0 --max-same-issues=0`

Defaults are 50 and 3, so the number you would otherwise record is truncated. Measured by the reviewer on the standard tier: `api` reports **52** capped against **103** uncapped (errcheck 36→79, one repeated message folded by `--max-same-issues`), while `controller` reports **5** either way. **The smaller module agreeing exactly is the camouflage** — a coder who sanity-checks "do the caps matter?" on the cheap module concludes no.

For scale under M3's actual linter set, the auditor measured (golangci-lint 2.12.2, go 1.26.5 darwin/arm64, `--default=none --enable=errcheck,staticcheck,ineffassign,unused,unparam,goconst`, 2026-08-02): **api 124, controller 41**; of api's 124, **85 are in `_test.go` files**. Treat as the starting point, not the recorded figure — re-measure on the final tree with the shipped config.

## R7 — ONE ROOT `.golangci.yml`

Both validators measured the walk-up: `golangci-lint config path` from a module subdirectory resolves the root file, and a root-level `default: none` + `enable:` list is honoured from inside the module. Control run: from `api/` and `controller/` today the same command prints `No config file detected`, so nothing stray is being picked up from outside the repo.

**One artefact to check on the first real run:** with the config passed by absolute path from outside the tree, findings printed as `../../../../../../Users/…/api/internal/…`. An in-repo root config reached by walk-up from `dir: api` should print module-relative paths. **Verify that it does** — a gate printing six levels of `../` is unreadable and breaks the names-the-file property `fmt-check` was built for.

## R8 — `goconst` ON, WITH THE PER-LINTER TEST-FILE EXCLUSIONS THE M3 BULLET ALREADY REQUIRES

`goconst` is **83 of the 165 combined findings**, and **40 of api's 50 are in `_test.go` files**. With the test-file exclusions it drops to a handful; without them it dominates both the debt and the `--whole-files` blast radius. Wall time is not an argument either way (5.3s for the whole api module on a warm cache, against `gate:api`'s recorded 43-66s). Every **disabled** linter still owes a one-line justification in the config, per git-manager's convention.

## R9 — BAN THE PLUGIN ESCAPE HATCHES IN EACH CONFIG'S OWN HEADER

Both config formats can name an executable that the runner then loads: golangci-lint's `linters.settings.custom.<name>.path` (a `.so` Go plugin) and oxlint's `jsPlugins` (`"Path or package name of the plugin"`) and `extends` (indirects the effective ruleset out of the reviewed file). A branch author gains no *new* capability on MR refs — an MR pipeline already runs the MR's own `.gitlab-ci.yml`, and Harbor creds are protected+masked and absent there — but two more files now decide what executes, and reviewers do not read config files the way they read code.

- State in each config's header that `custom:` / `jsPlugins` / `extends` are forbidden here, so a diff adding one reads as a policy violation.
- Put **`golangci-lint config verify`** in the Taskfile target ahead of the run. It exists in v2, exits 0 silently on a valid config, and 3 with a named error otherwise.
- The weaker sibling has no CI answer: a branch author can *neuter* the gate from the same files (`linters.default: none`, `ignorePatterns`). CODEOWNERS on the two configs is the only structural fix — out of scope here, worth one line in the MR.

## R10 — THE DOCUMENTATION EDIT LIST GROWS BY ONE FILE AND SEVERAL PROSE BLOCKS

The body's list above is incomplete. Both validators found the same gap independently.

- **ADD `.claude/agents/reviewer.md`** — its tail asserts *"Dead-code slot: `none (gap)` — there is no `deadcode`, `knip`, or `golangci-lint unused` here yet (PRD #103 M4 adds them)"*. M3's linter set includes `unused`, so after M3 that sentence is false on **both** halves: the tool exists, and it did not arrive with M4. The PRD listed `reviewer.md` as "already satisfied" — that was an M1 statement and does not survive M3.
- **`.claude/agents/coder.md`** — not just the slot value. *"this repo has no linter yet (PRD #103 M3 builds one)"* and *"Do not go hunting for a lint command; there isn't one"* both go. The second is the operative damage: the coder is the role that must run the new gate.
- **`docs/dev-conventions.md`** — *"There is no linter, no dead-code check and no coverage signal yet"* must be **split**, not appended to. Lint becomes true; dead code and coverage stay false until M4/M6.
- **DEAD-CODE SLOT RULING.** M3 enables `unused`, which **partially** closes a slot M4 owns. Do not silently close it and do not leave it unqualified. Both copies keep `none (gap, noted 2026-07-26)` and gain one clause: `unused` (via the lint targets) finds unused unexported symbols within a package; it does **not** do `deadcode`'s cross-package reachability analysis, and it says nothing about unused TS exports. M4 still owns the slot.
- **GATE-TIMING PROSE, BOTH COPIES.** `task gate` at `~8m30s` and the per-component figures `api 43-66s, controller ~10s, web 23s, agent 34s` are used *prescriptively* ("gets a complete answer in well under a minute") and are invalidated by wiring lint into `gate:*`. Either re-measure and re-date them, or add one clause saying they predate the lint step. Do not leave them bare.
- **DO NOT TOUCH `api/internal/agenttmpl/builtins/reviewer.md`.** Its wording is conditional generic product text. `CLAUDE.md` makes `builtins/` decoupled from `.claude/agents/`, and product changes must never touch the dev roster or vice versa.
- **DO NOT "FIX" the PRD's Problem section** (*"There is no `.golangci.yml`, no `eslint.config.*`…"*). It is a historical record of the pre-PRD state.

**`CLAUDE.md` — the exact sentences.** The reviewer enumerated which command strings in `CLAUDE.md` are recipes (editable) and which are observations (byte-frozen), including the trap that `cd api && go test ./internal/forge -run TestName   # single test — no target` is prose, not a recipe, and that several fenced blocks are transcripts of runs nobody can re-perform. **Ask the lead for that enumeration before editing `CLAUDE.md`** rather than re-deriving it. The discriminator: *if the line has a `#` comment telling you what to do, it is a recipe; if it sits in a block with columns, an arrow, or an exit code beside it, it is evidence.*

## R11 — VERIFY THE musl BINDING LANDS IN BOTH LOCKFILES

Both lockfiles are `lockfileVersion: 3` with complete integrity coverage (432/432 web, 142/142 agent), so `npm ci` is already fully checksum-pinned and oxlint inherits that. oxlint itself has **no install scripts**; its binary arrives as napi `optionalDependencies`. But this host is darwin/arm64 and CI is alpine: **confirm `@oxlint/binding-linux-x64-musl` is present in both lockfiles after `npm install`.** That is the classic place a platform binding goes missing, and its absence would be discovered in CI, not here. Node engine is satisfied (`^20.19.0 || >=22.12.0` against the pinned image's 22.23.1). Worker images run `npm ci --omit=dev`, so a devDependency does not reach a shipped worker container.

## R12 — TWO SMALLER BINDING RULES

- **The ratchet ref stays a literal.** `git check-ref-format --branch` accepts `a;b`, `a$b`, `` a`b` ``, `a|b`, `a&b`, `a(b)` as branch names; only a space is rejected. Anyone who can open an MR chooses the target branch, so `$CI_COMMIT_BRANCH` / `$CI_MERGE_REQUEST_TARGET_BRANCH_NAME` are attacker-chosen strings with live metacharacters. Never a `{{.VAR}}` splice; quote even in `script:`.
- **`--whole-files` makes pre-existing findings in a touched file blocking** — measured 2→4 findings when added, the two extra being old findings in a file whose only change was an added comment. That is the flag working as intended and it is also the adoption cost. One sentence in the config header so the first person it hits does not file it as a bug. (Note `--help` says it "requires new-from-rev or new-from-patch" and omits `new-from-merge-base`; the combination nevertheless works, measured twice independently. Someone reading `--help` will otherwise "correct" the invocation.)

## Two hazards RULED OUT — do not spend time on them

- **Untracked scratch files ARE linted under the merge-base ratchet** (rc=1, finding reported), so a `cp`-backup calibration on an uncommitted scratch file works.
- **`--whole-files` does work with `--new-from-merge-base`** despite the help text.

## Where the Taskfile calls oxlint from

No `"lint"` script exists in either `package.json` today, so nothing binds this. **Call `node_modules/.bin/oxlint` directly from the Taskfile**, so the load-bearing flags sit under an inline comment in the file whose header mandates one — rather than in `package.json`, which is both a Decision 11 file and the three-way merge contention (M3 oxlint, M4 knip, M6 `@vitest/coverage-v8`). **Do not ship a bare `npx oxlint`**: it fetches from the network when the dep is missing, which is not something a gate may do.
