// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: users.sql

package store

import (
	"context"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const bumpTokenVersion = `-- name: BumpTokenVersion :one
UPDATE users SET token_version = token_version + 1 WHERE id = $1
RETURNING token_version
`

func (q *Queries) BumpTokenVersion(ctx context.Context, id uuid.UUID) (int32, error) {
	row := q.db.QueryRow(ctx, bumpTokenVersion, id)
	var token_version int32
	err := row.Scan(&token_version)
	return token_version, err
}

const countUsers = `-- name: CountUsers :one
SELECT count(*) FROM users
`

func (q *Queries) CountUsers(ctx context.Context) (int64, error) {
	row := q.db.QueryRow(ctx, countUsers)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const createUser = `-- name: CreateUser :one
INSERT INTO users (email, password_hash, display_name, is_admin)
VALUES ($1, $2, $3, $4)
RETURNING id, email, password_hash, display_name, is_admin, is_active, token_version, created_at, last_login, default_model, autopilot_enabled, theme, slack_member_id, slack_notify, slack_resolved_id, slack_link_confirmed_at, oidc_issuer, oidc_subject, judge_enabled, judge_anthropic_secret_id, wait_on_limit, ci_autofix_enabled, sidebar_token_ids, judge_model, summary_model, ephemeral_workers_enabled, default_effort, mr_rework_enabled
`

type CreateUserParams struct {
	Email        string      `json:"email"`
	PasswordHash pgtype.Text `json:"password_hash"`
	DisplayName  pgtype.Text `json:"display_name"`
	IsAdmin      bool        `json:"is_admin"`
}

func (q *Queries) CreateUser(ctx context.Context, arg CreateUserParams) (User, error) {
	row := q.db.QueryRow(ctx, createUser,
		arg.Email,
		arg.PasswordHash,
		arg.DisplayName,
		arg.IsAdmin,
	)
	var i User
	err := row.Scan(
		&i.ID,
		&i.Email,
		&i.PasswordHash,
		&i.DisplayName,
		&i.IsAdmin,
		&i.IsActive,
		&i.TokenVersion,
		&i.CreatedAt,
		&i.LastLogin,
		&i.DefaultModel,
		&i.AutopilotEnabled,
		&i.Theme,
		&i.SlackMemberID,
		&i.SlackNotify,
		&i.SlackResolvedID,
		&i.SlackLinkConfirmedAt,
		&i.OidcIssuer,
		&i.OidcSubject,
		&i.JudgeEnabled,
		&i.JudgeAnthropicSecretID,
		&i.WaitOnLimit,
		&i.CiAutofixEnabled,
		&i.SidebarTokenIds,
		&i.JudgeModel,
		&i.SummaryModel,
		&i.EphemeralWorkersEnabled,
		&i.DefaultEffort,
		&i.MrReworkEnabled,
	)
	return i, err
}

const createUserOIDC = `-- name: CreateUserOIDC :one
INSERT INTO users (email, password_hash, display_name, is_admin, oidc_issuer, oidc_subject)
VALUES ($1, NULL, $2, $3, $4, $5)
RETURNING id, email, password_hash, display_name, is_admin, is_active, token_version, created_at, last_login, default_model, autopilot_enabled, theme, slack_member_id, slack_notify, slack_resolved_id, slack_link_confirmed_at, oidc_issuer, oidc_subject, judge_enabled, judge_anthropic_secret_id, wait_on_limit, ci_autofix_enabled, sidebar_token_ids, judge_model, summary_model, ephemeral_workers_enabled, default_effort, mr_rework_enabled
`

type CreateUserOIDCParams struct {
	Email       string      `json:"email"`
	DisplayName pgtype.Text `json:"display_name"`
	IsAdmin     bool        `json:"is_admin"`
	OidcIssuer  pgtype.Text `json:"oidc_issuer"`
	OidcSubject pgtype.Text `json:"oidc_subject"`
}

// JIT-provision a passwordless OIDC user (PRD #45, Decision 7): password_hash is
// NULL so password login always fails constant-time. is_admin follows the
// first-user rule, decided by the caller under the advisory lock.
func (q *Queries) CreateUserOIDC(ctx context.Context, arg CreateUserOIDCParams) (User, error) {
	row := q.db.QueryRow(ctx, createUserOIDC,
		arg.Email,
		arg.DisplayName,
		arg.IsAdmin,
		arg.OidcIssuer,
		arg.OidcSubject,
	)
	var i User
	err := row.Scan(
		&i.ID,
		&i.Email,
		&i.PasswordHash,
		&i.DisplayName,
		&i.IsAdmin,
		&i.IsActive,
		&i.TokenVersion,
		&i.CreatedAt,
		&i.LastLogin,
		&i.DefaultModel,
		&i.AutopilotEnabled,
		&i.Theme,
		&i.SlackMemberID,
		&i.SlackNotify,
		&i.SlackResolvedID,
		&i.SlackLinkConfirmedAt,
		&i.OidcIssuer,
		&i.OidcSubject,
		&i.JudgeEnabled,
		&i.JudgeAnthropicSecretID,
		&i.WaitOnLimit,
		&i.CiAutofixEnabled,
		&i.SidebarTokenIds,
		&i.JudgeModel,
		&i.SummaryModel,
		&i.EphemeralWorkersEnabled,
		&i.DefaultEffort,
		&i.MrReworkEnabled,
	)
	return i, err
}

const getUserByEmail = `-- name: GetUserByEmail :one
SELECT id, email, password_hash, display_name, is_admin, is_active, token_version, created_at, last_login, default_model, autopilot_enabled, theme, slack_member_id, slack_notify, slack_resolved_id, slack_link_confirmed_at, oidc_issuer, oidc_subject, judge_enabled, judge_anthropic_secret_id, wait_on_limit, ci_autofix_enabled, sidebar_token_ids, judge_model, summary_model, ephemeral_workers_enabled, default_effort, mr_rework_enabled FROM users WHERE email = $1
`

func (q *Queries) GetUserByEmail(ctx context.Context, email string) (User, error) {
	row := q.db.QueryRow(ctx, getUserByEmail, email)
	var i User
	err := row.Scan(
		&i.ID,
		&i.Email,
		&i.PasswordHash,
		&i.DisplayName,
		&i.IsAdmin,
		&i.IsActive,
		&i.TokenVersion,
		&i.CreatedAt,
		&i.LastLogin,
		&i.DefaultModel,
		&i.AutopilotEnabled,
		&i.Theme,
		&i.SlackMemberID,
		&i.SlackNotify,
		&i.SlackResolvedID,
		&i.SlackLinkConfirmedAt,
		&i.OidcIssuer,
		&i.OidcSubject,
		&i.JudgeEnabled,
		&i.JudgeAnthropicSecretID,
		&i.WaitOnLimit,
		&i.CiAutofixEnabled,
		&i.SidebarTokenIds,
		&i.JudgeModel,
		&i.SummaryModel,
		&i.EphemeralWorkersEnabled,
		&i.DefaultEffort,
		&i.MrReworkEnabled,
	)
	return i, err
}

const getUserByID = `-- name: GetUserByID :one
SELECT id, email, password_hash, display_name, is_admin, is_active, token_version, created_at, last_login, default_model, autopilot_enabled, theme, slack_member_id, slack_notify, slack_resolved_id, slack_link_confirmed_at, oidc_issuer, oidc_subject, judge_enabled, judge_anthropic_secret_id, wait_on_limit, ci_autofix_enabled, sidebar_token_ids, judge_model, summary_model, ephemeral_workers_enabled, default_effort, mr_rework_enabled FROM users WHERE id = $1
`

func (q *Queries) GetUserByID(ctx context.Context, id uuid.UUID) (User, error) {
	row := q.db.QueryRow(ctx, getUserByID, id)
	var i User
	err := row.Scan(
		&i.ID,
		&i.Email,
		&i.PasswordHash,
		&i.DisplayName,
		&i.IsAdmin,
		&i.IsActive,
		&i.TokenVersion,
		&i.CreatedAt,
		&i.LastLogin,
		&i.DefaultModel,
		&i.AutopilotEnabled,
		&i.Theme,
		&i.SlackMemberID,
		&i.SlackNotify,
		&i.SlackResolvedID,
		&i.SlackLinkConfirmedAt,
		&i.OidcIssuer,
		&i.OidcSubject,
		&i.JudgeEnabled,
		&i.JudgeAnthropicSecretID,
		&i.WaitOnLimit,
		&i.CiAutofixEnabled,
		&i.SidebarTokenIds,
		&i.JudgeModel,
		&i.SummaryModel,
		&i.EphemeralWorkersEnabled,
		&i.DefaultEffort,
		&i.MrReworkEnabled,
	)
	return i, err
}

const getUserByOIDCSubject = `-- name: GetUserByOIDCSubject :one
SELECT id, email, password_hash, display_name, is_admin, is_active, token_version, created_at, last_login, default_model, autopilot_enabled, theme, slack_member_id, slack_notify, slack_resolved_id, slack_link_confirmed_at, oidc_issuer, oidc_subject, judge_enabled, judge_anthropic_secret_id, wait_on_limit, ci_autofix_enabled, sidebar_token_ids, judge_model, summary_model, ephemeral_workers_enabled, default_effort, mr_rework_enabled FROM users WHERE oidc_issuer = $1 AND oidc_subject = $2
`

type GetUserByOIDCSubjectParams struct {
	OidcIssuer  pgtype.Text `json:"oidc_issuer"`
	OidcSubject pgtype.Text `json:"oidc_subject"`
}

// Primary OIDC login lookup (PRD #45): match the stable (issuer, subject) identity.
func (q *Queries) GetUserByOIDCSubject(ctx context.Context, arg GetUserByOIDCSubjectParams) (User, error) {
	row := q.db.QueryRow(ctx, getUserByOIDCSubject, arg.OidcIssuer, arg.OidcSubject)
	var i User
	err := row.Scan(
		&i.ID,
		&i.Email,
		&i.PasswordHash,
		&i.DisplayName,
		&i.IsAdmin,
		&i.IsActive,
		&i.TokenVersion,
		&i.CreatedAt,
		&i.LastLogin,
		&i.DefaultModel,
		&i.AutopilotEnabled,
		&i.Theme,
		&i.SlackMemberID,
		&i.SlackNotify,
		&i.SlackResolvedID,
		&i.SlackLinkConfirmedAt,
		&i.OidcIssuer,
		&i.OidcSubject,
		&i.JudgeEnabled,
		&i.JudgeAnthropicSecretID,
		&i.WaitOnLimit,
		&i.CiAutofixEnabled,
		&i.SidebarTokenIds,
		&i.JudgeModel,
		&i.SummaryModel,
		&i.EphemeralWorkersEnabled,
		&i.DefaultEffort,
		&i.MrReworkEnabled,
	)
	return i, err
}

const getUserDefaultEffort = `-- name: GetUserDefaultEffort :one
SELECT default_effort FROM users WHERE id = $1
`

// The current user's per-user default reasoning effort (PRD #617); NULL = inherit
// (we omit the SDK effort key, so the SDK default `high` applies). Read at issue-
// and chat-run claim assembly, keyed on the run owner. Selects only the column.
func (q *Queries) GetUserDefaultEffort(ctx context.Context, id uuid.UUID) (pgtype.Text, error) {
	row := q.db.QueryRow(ctx, getUserDefaultEffort, id)
	var default_effort pgtype.Text
	err := row.Scan(&default_effort)
	return default_effort, err
}

const getUserDefaultModel = `-- name: GetUserDefaultModel :one
SELECT default_model FROM users WHERE id = $1
`

// The current user's per-user default worker model (PRD #17); NULL = inherit.
func (q *Queries) GetUserDefaultModel(ctx context.Context, id uuid.UUID) (pgtype.Text, error) {
	row := q.db.QueryRow(ctx, getUserDefaultModel, id)
	var default_model pgtype.Text
	err := row.Scan(&default_model)
	return default_model, err
}

const getUserJudgeAnthropicSecret = `-- name: GetUserJudgeAnthropicSecret :one
SELECT judge_anthropic_secret_id FROM users WHERE id = $1
`

// The user's judge-lane binding, read at judge-claim time. NULL ⇒ resolve their
// default token. Selects only the binding: the judge claim needs no other user
// column, and a narrow read keeps it obvious that nothing else about the user
// influences which credential a retrospective spends.
func (q *Queries) GetUserJudgeAnthropicSecret(ctx context.Context, id uuid.UUID) (pgtype.UUID, error) {
	row := q.db.QueryRow(ctx, getUserJudgeAnthropicSecret, id)
	var judge_anthropic_secret_id pgtype.UUID
	err := row.Scan(&judge_anthropic_secret_id)
	return judge_anthropic_secret_id, err
}

const getUserJudgeModel = `-- name: GetUserJudgeModel :one
SELECT judge_model FROM users WHERE id = $1
`

// The current user's per-user judge model override (PRD #69 M2); NULL = inherit
// the instance judge_model. Read at judge-claim assembly, keyed on the run owner.
// Selects only the column: the judge claim needs no other user field, and a
// narrow read keeps resolution self-contained and cheap (Decision 5).
func (q *Queries) GetUserJudgeModel(ctx context.Context, id uuid.UUID) (pgtype.Text, error) {
	row := q.db.QueryRow(ctx, getUserJudgeModel, id)
	var judge_model pgtype.Text
	err := row.Scan(&judge_model)
	return judge_model, err
}

const getUserSettings = `-- name: GetUserSettings :one
SELECT default_model, default_effort, judge_model, summary_model, theme, sidebar_token_ids, mr_rework_enabled FROM users WHERE id = $1
`

type GetUserSettingsRow struct {
	DefaultModel    pgtype.Text `json:"default_model"`
	DefaultEffort   pgtype.Text `json:"default_effort"`
	JudgeModel      pgtype.Text `json:"judge_model"`
	SummaryModel    pgtype.Text `json:"summary_model"`
	Theme           pgtype.Text `json:"theme"`
	SidebarTokenIds []uuid.UUID `json:"sidebar_token_ids"`
	MrReworkEnabled pgtype.Bool `json:"mr_rework_enabled"`
}

// The current user's own (non-secret) settings surface: default worker model
// (PRD #17), per-user default reasoning effort (PRD #617), per-user judge model
// override (PRD #69), per-user run-summary model override (PRD #362 M2), UI theme
// override (PRD #21), and the sidebar token-meter choice (00123). NULL = inherit /
// use the instance default / default-token-only. Own-user only; the caller passes
// the session user's id. summary_model rides this one-row read for the settings
// surface (GetUserSummaryModel stays the narrow read for issue-run claim
// assembly), so the settings response needs no second query. mr_rework_enabled
// (PRD #700 M5) rides it too; NULL there means the default-ON per-user opt-in.
func (q *Queries) GetUserSettings(ctx context.Context, id uuid.UUID) (GetUserSettingsRow, error) {
	row := q.db.QueryRow(ctx, getUserSettings, id)
	var i GetUserSettingsRow
	err := row.Scan(
		&i.DefaultModel,
		&i.DefaultEffort,
		&i.JudgeModel,
		&i.SummaryModel,
		&i.Theme,
		&i.SidebarTokenIds,
		&i.MrReworkEnabled,
	)
	return i, err
}

const getUserSummaryModel = `-- name: GetUserSummaryModel :one
SELECT summary_model FROM users WHERE id = $1
`

// The current user's per-user run-summary model override (PRD #362 M2); NULL =
// inherit the instance summary_model. Read at ISSUE-run claim assembly, keyed on
// the run owner. Selects only the column: the claim needs no other user field, and
// a narrow read keeps resolution self-contained and cheap (Decision 8). Mirrors
// GetUserJudgeModel but rides the issue-run claim rather than the judge claim.
func (q *Queries) GetUserSummaryModel(ctx context.Context, id uuid.UUID) (pgtype.Text, error) {
	row := q.db.QueryRow(ctx, getUserSummaryModel, id)
	var summary_model pgtype.Text
	err := row.Scan(&summary_model)
	return summary_model, err
}

const linkUserOIDC = `-- name: LinkUserOIDC :one
UPDATE users SET oidc_issuer = $2, oidc_subject = $3
WHERE id = $1 AND oidc_subject IS NULL
RETURNING id, email, password_hash, display_name, is_admin, is_active, token_version, created_at, last_login, default_model, autopilot_enabled, theme, slack_member_id, slack_notify, slack_resolved_id, slack_link_confirmed_at, oidc_issuer, oidc_subject, judge_enabled, judge_anthropic_secret_id, wait_on_limit, ci_autofix_enabled, sidebar_token_ids, judge_model, summary_model, ephemeral_workers_enabled, default_effort, mr_rework_enabled
`

type LinkUserOIDCParams struct {
	ID          uuid.UUID   `json:"id"`
	OidcIssuer  pgtype.Text `json:"oidc_issuer"`
	OidcSubject pgtype.Text `json:"oidc_subject"`
}

// Attach an IdP identity to an existing (verified-email-matched) account, but ONLY
// if the row is not already bound to a subject (audit H1): the WHERE oidc_subject IS
// NULL guard makes a re-link a no-op that returns no row, so the caller can reject an
// email match against a row already bound to a DIFFERENT subject instead of
// overwriting it. The caller asserts exactly one row was returned.
func (q *Queries) LinkUserOIDC(ctx context.Context, arg LinkUserOIDCParams) (User, error) {
	row := q.db.QueryRow(ctx, linkUserOIDC, arg.ID, arg.OidcIssuer, arg.OidcSubject)
	var i User
	err := row.Scan(
		&i.ID,
		&i.Email,
		&i.PasswordHash,
		&i.DisplayName,
		&i.IsAdmin,
		&i.IsActive,
		&i.TokenVersion,
		&i.CreatedAt,
		&i.LastLogin,
		&i.DefaultModel,
		&i.AutopilotEnabled,
		&i.Theme,
		&i.SlackMemberID,
		&i.SlackNotify,
		&i.SlackResolvedID,
		&i.SlackLinkConfirmedAt,
		&i.OidcIssuer,
		&i.OidcSubject,
		&i.JudgeEnabled,
		&i.JudgeAnthropicSecretID,
		&i.WaitOnLimit,
		&i.CiAutofixEnabled,
		&i.SidebarTokenIds,
		&i.JudgeModel,
		&i.SummaryModel,
		&i.EphemeralWorkersEnabled,
		&i.DefaultEffort,
		&i.MrReworkEnabled,
	)
	return i, err
}

const listUsers = `-- name: ListUsers :many
SELECT id, email, password_hash, display_name, is_admin, is_active, token_version, created_at, last_login, default_model, autopilot_enabled, theme, slack_member_id, slack_notify, slack_resolved_id, slack_link_confirmed_at, oidc_issuer, oidc_subject, judge_enabled, judge_anthropic_secret_id, wait_on_limit, ci_autofix_enabled, sidebar_token_ids, judge_model, summary_model, ephemeral_workers_enabled, default_effort, mr_rework_enabled FROM users ORDER BY created_at ASC
`

func (q *Queries) ListUsers(ctx context.Context) ([]User, error) {
	rows, err := q.db.Query(ctx, listUsers)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []User{}
	for rows.Next() {
		var i User
		if err := rows.Scan(
			&i.ID,
			&i.Email,
			&i.PasswordHash,
			&i.DisplayName,
			&i.IsAdmin,
			&i.IsActive,
			&i.TokenVersion,
			&i.CreatedAt,
			&i.LastLogin,
			&i.DefaultModel,
			&i.AutopilotEnabled,
			&i.Theme,
			&i.SlackMemberID,
			&i.SlackNotify,
			&i.SlackResolvedID,
			&i.SlackLinkConfirmedAt,
			&i.OidcIssuer,
			&i.OidcSubject,
			&i.JudgeEnabled,
			&i.JudgeAnthropicSecretID,
			&i.WaitOnLimit,
			&i.CiAutofixEnabled,
			&i.SidebarTokenIds,
			&i.JudgeModel,
			&i.SummaryModel,
			&i.EphemeralWorkersEnabled,
			&i.DefaultEffort,
			&i.MrReworkEnabled,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const setLastLogin = `-- name: SetLastLogin :exec
UPDATE users SET last_login = now() WHERE id = $1
`

func (q *Queries) SetLastLogin(ctx context.Context, id uuid.UUID) error {
	_, err := q.db.Exec(ctx, setLastLogin, id)
	return err
}

const setUserActive = `-- name: SetUserActive :one
UPDATE users
SET is_active = $1,
    -- Deactivation revokes all live sessions by bumping token_version;
    -- reactivation leaves it untouched.
    token_version = CASE WHEN $1 THEN token_version ELSE token_version + 1 END
WHERE id = $2
RETURNING id, email, password_hash, display_name, is_admin, is_active, token_version, created_at, last_login, default_model, autopilot_enabled, theme, slack_member_id, slack_notify, slack_resolved_id, slack_link_confirmed_at, oidc_issuer, oidc_subject, judge_enabled, judge_anthropic_secret_id, wait_on_limit, ci_autofix_enabled, sidebar_token_ids, judge_model, summary_model, ephemeral_workers_enabled, default_effort, mr_rework_enabled
`

type SetUserActiveParams struct {
	IsActive bool      `json:"is_active"`
	ID       uuid.UUID `json:"id"`
}

func (q *Queries) SetUserActive(ctx context.Context, arg SetUserActiveParams) (User, error) {
	row := q.db.QueryRow(ctx, setUserActive, arg.IsActive, arg.ID)
	var i User
	err := row.Scan(
		&i.ID,
		&i.Email,
		&i.PasswordHash,
		&i.DisplayName,
		&i.IsAdmin,
		&i.IsActive,
		&i.TokenVersion,
		&i.CreatedAt,
		&i.LastLogin,
		&i.DefaultModel,
		&i.AutopilotEnabled,
		&i.Theme,
		&i.SlackMemberID,
		&i.SlackNotify,
		&i.SlackResolvedID,
		&i.SlackLinkConfirmedAt,
		&i.OidcIssuer,
		&i.OidcSubject,
		&i.JudgeEnabled,
		&i.JudgeAnthropicSecretID,
		&i.WaitOnLimit,
		&i.CiAutofixEnabled,
		&i.SidebarTokenIds,
		&i.JudgeModel,
		&i.SummaryModel,
		&i.EphemeralWorkersEnabled,
		&i.DefaultEffort,
		&i.MrReworkEnabled,
	)
	return i, err
}

const setUserAdmin = `-- name: SetUserAdmin :one
UPDATE users SET is_admin = $1 WHERE id = $2
RETURNING id, email, password_hash, display_name, is_admin, is_active, token_version, created_at, last_login, default_model, autopilot_enabled, theme, slack_member_id, slack_notify, slack_resolved_id, slack_link_confirmed_at, oidc_issuer, oidc_subject, judge_enabled, judge_anthropic_secret_id, wait_on_limit, ci_autofix_enabled, sidebar_token_ids, judge_model, summary_model, ephemeral_workers_enabled, default_effort, mr_rework_enabled
`

type SetUserAdminParams struct {
	IsAdmin bool      `json:"is_admin"`
	ID      uuid.UUID `json:"id"`
}

// Authoritative sync of is_admin from OIDC group membership (PRD #55): membership in
// an UZI_OIDC_ADMIN_GROUPS group grants, leaving the group demotes, on the user's
// next OIDC login. is_admin is reloaded per-request by RequireAuth (not carried in
// the JWT), so a flip propagates to live sessions without a token_version bump. The
// seed-admin demotion exemption is enforced in the caller, not here.
func (q *Queries) SetUserAdmin(ctx context.Context, arg SetUserAdminParams) (User, error) {
	row := q.db.QueryRow(ctx, setUserAdmin, arg.IsAdmin, arg.ID)
	var i User
	err := row.Scan(
		&i.ID,
		&i.Email,
		&i.PasswordHash,
		&i.DisplayName,
		&i.IsAdmin,
		&i.IsActive,
		&i.TokenVersion,
		&i.CreatedAt,
		&i.LastLogin,
		&i.DefaultModel,
		&i.AutopilotEnabled,
		&i.Theme,
		&i.SlackMemberID,
		&i.SlackNotify,
		&i.SlackResolvedID,
		&i.SlackLinkConfirmedAt,
		&i.OidcIssuer,
		&i.OidcSubject,
		&i.JudgeEnabled,
		&i.JudgeAnthropicSecretID,
		&i.WaitOnLimit,
		&i.CiAutofixEnabled,
		&i.SidebarTokenIds,
		&i.JudgeModel,
		&i.SummaryModel,
		&i.EphemeralWorkersEnabled,
		&i.DefaultEffort,
		&i.MrReworkEnabled,
	)
	return i, err
}

const setUserAutopilotEnabled = `-- name: SetUserAutopilotEnabled :one
UPDATE users SET autopilot_enabled = $2 WHERE id = $1
RETURNING id, email, password_hash, display_name, is_admin, is_active, token_version, created_at, last_login, default_model, autopilot_enabled, theme, slack_member_id, slack_notify, slack_resolved_id, slack_link_confirmed_at, oidc_issuer, oidc_subject, judge_enabled, judge_anthropic_secret_id, wait_on_limit, ci_autofix_enabled, sidebar_token_ids, judge_model, summary_model, ephemeral_workers_enabled, default_effort, mr_rework_enabled
`

type SetUserAutopilotEnabledParams struct {
	ID               uuid.UUID `json:"id"`
	AutopilotEnabled bool      `json:"autopilot_enabled"`
}

// Flip a user's autopilot opt-in (PRD #19 M3, Decision 4). Per-user consent to
// unattended runs; default false, set from the user's own Settings page.
func (q *Queries) SetUserAutopilotEnabled(ctx context.Context, arg SetUserAutopilotEnabledParams) (User, error) {
	row := q.db.QueryRow(ctx, setUserAutopilotEnabled, arg.ID, arg.AutopilotEnabled)
	var i User
	err := row.Scan(
		&i.ID,
		&i.Email,
		&i.PasswordHash,
		&i.DisplayName,
		&i.IsAdmin,
		&i.IsActive,
		&i.TokenVersion,
		&i.CreatedAt,
		&i.LastLogin,
		&i.DefaultModel,
		&i.AutopilotEnabled,
		&i.Theme,
		&i.SlackMemberID,
		&i.SlackNotify,
		&i.SlackResolvedID,
		&i.SlackLinkConfirmedAt,
		&i.OidcIssuer,
		&i.OidcSubject,
		&i.JudgeEnabled,
		&i.JudgeAnthropicSecretID,
		&i.WaitOnLimit,
		&i.CiAutofixEnabled,
		&i.SidebarTokenIds,
		&i.JudgeModel,
		&i.SummaryModel,
		&i.EphemeralWorkersEnabled,
		&i.DefaultEffort,
		&i.MrReworkEnabled,
	)
	return i, err
}

const setUserCIAutofixEnabled = `-- name: SetUserCIAutofixEnabled :one
UPDATE users SET ci_autofix_enabled = $2 WHERE id = $1
RETURNING id, email, password_hash, display_name, is_admin, is_active, token_version, created_at, last_login, default_model, autopilot_enabled, theme, slack_member_id, slack_notify, slack_resolved_id, slack_link_confirmed_at, oidc_issuer, oidc_subject, judge_enabled, judge_anthropic_secret_id, wait_on_limit, ci_autofix_enabled, sidebar_token_ids, judge_model, summary_model, ephemeral_workers_enabled, default_effort, mr_rework_enabled
`

type SetUserCIAutofixEnabledParams struct {
	ID               uuid.UUID `json:"id"`
	CiAutofixEnabled bool      `json:"ci_autofix_enabled"`
}

// Flip a user's automatic CI-fix opt-in (PRD #71). Per-user consent to spend the
// user's own Anthropic tokens auto-fixing failed pipelines on their agent MR
// branches; default false. The caller passes the target id: the session user for
// PUT /api/me/ci-autofix, or an admin-chosen id (from the path) for the admin toggle.
func (q *Queries) SetUserCIAutofixEnabled(ctx context.Context, arg SetUserCIAutofixEnabledParams) (User, error) {
	row := q.db.QueryRow(ctx, setUserCIAutofixEnabled, arg.ID, arg.CiAutofixEnabled)
	var i User
	err := row.Scan(
		&i.ID,
		&i.Email,
		&i.PasswordHash,
		&i.DisplayName,
		&i.IsAdmin,
		&i.IsActive,
		&i.TokenVersion,
		&i.CreatedAt,
		&i.LastLogin,
		&i.DefaultModel,
		&i.AutopilotEnabled,
		&i.Theme,
		&i.SlackMemberID,
		&i.SlackNotify,
		&i.SlackResolvedID,
		&i.SlackLinkConfirmedAt,
		&i.OidcIssuer,
		&i.OidcSubject,
		&i.JudgeEnabled,
		&i.JudgeAnthropicSecretID,
		&i.WaitOnLimit,
		&i.CiAutofixEnabled,
		&i.SidebarTokenIds,
		&i.JudgeModel,
		&i.SummaryModel,
		&i.EphemeralWorkersEnabled,
		&i.DefaultEffort,
		&i.MrReworkEnabled,
	)
	return i, err
}

const setUserDefaultEffort = `-- name: SetUserDefaultEffort :one
UPDATE users SET default_effort = $1 WHERE id = $2
RETURNING default_effort
`

type SetUserDefaultEffortParams struct {
	DefaultEffort pgtype.Text `json:"default_effort"`
	ID            uuid.UUID   `json:"id"`
}

// Sets (or clears, when @default_effort is NULL) the current user's default
// reasoning effort. Own-user only; the caller passes the session user's id.
func (q *Queries) SetUserDefaultEffort(ctx context.Context, arg SetUserDefaultEffortParams) (pgtype.Text, error) {
	row := q.db.QueryRow(ctx, setUserDefaultEffort, arg.DefaultEffort, arg.ID)
	var default_effort pgtype.Text
	err := row.Scan(&default_effort)
	return default_effort, err
}

const setUserDefaultModel = `-- name: SetUserDefaultModel :one
UPDATE users SET default_model = $1 WHERE id = $2
RETURNING default_model
`

type SetUserDefaultModelParams struct {
	DefaultModel pgtype.Text `json:"default_model"`
	ID           uuid.UUID   `json:"id"`
}

// Sets (or clears, when @default_model is NULL) the current user's default
// worker model. Own-user only; the caller passes the session user's id.
func (q *Queries) SetUserDefaultModel(ctx context.Context, arg SetUserDefaultModelParams) (pgtype.Text, error) {
	row := q.db.QueryRow(ctx, setUserDefaultModel, arg.DefaultModel, arg.ID)
	var default_model pgtype.Text
	err := row.Scan(&default_model)
	return default_model, err
}

const setUserEphemeralWorkersEnabled = `-- name: SetUserEphemeralWorkersEnabled :one
UPDATE users SET ephemeral_workers_enabled = $2 WHERE id = $1
RETURNING id, email, password_hash, display_name, is_admin, is_active, token_version, created_at, last_login, default_model, autopilot_enabled, theme, slack_member_id, slack_notify, slack_resolved_id, slack_link_confirmed_at, oidc_issuer, oidc_subject, judge_enabled, judge_anthropic_secret_id, wait_on_limit, ci_autofix_enabled, sidebar_token_ids, judge_model, summary_model, ephemeral_workers_enabled, default_effort, mr_rework_enabled
`

type SetUserEphemeralWorkersEnabledParams struct {
	ID                      uuid.UUID `json:"id"`
	EphemeralWorkersEnabled bool      `json:"ephemeral_workers_enabled"`
}

// Flip a user's ephemeral worker auto-provisioning opt-in (PRD #529 M2). Per-user
// consent to have the api spin a run-bound throwaway hosted worker when one of the
// user's runs is unplaceable for a capability; default false. Mirrors
// SetUserJudgeEnabled: the caller passes the target id (the session user for the
// self-toggle, or an admin-chosen id for the admin toggle). Used by tests now and the
// CLI/handler wiring later.
func (q *Queries) SetUserEphemeralWorkersEnabled(ctx context.Context, arg SetUserEphemeralWorkersEnabledParams) (User, error) {
	row := q.db.QueryRow(ctx, setUserEphemeralWorkersEnabled, arg.ID, arg.EphemeralWorkersEnabled)
	var i User
	err := row.Scan(
		&i.ID,
		&i.Email,
		&i.PasswordHash,
		&i.DisplayName,
		&i.IsAdmin,
		&i.IsActive,
		&i.TokenVersion,
		&i.CreatedAt,
		&i.LastLogin,
		&i.DefaultModel,
		&i.AutopilotEnabled,
		&i.Theme,
		&i.SlackMemberID,
		&i.SlackNotify,
		&i.SlackResolvedID,
		&i.SlackLinkConfirmedAt,
		&i.OidcIssuer,
		&i.OidcSubject,
		&i.JudgeEnabled,
		&i.JudgeAnthropicSecretID,
		&i.WaitOnLimit,
		&i.CiAutofixEnabled,
		&i.SidebarTokenIds,
		&i.JudgeModel,
		&i.SummaryModel,
		&i.EphemeralWorkersEnabled,
		&i.DefaultEffort,
		&i.MrReworkEnabled,
	)
	return i, err
}

const setUserJudgeAnthropicSecret = `-- name: SetUserJudgeAnthropicSecret :one
UPDATE users SET judge_anthropic_secret_id = $1 WHERE id = $2
RETURNING id, email, password_hash, display_name, is_admin, is_active, token_version, created_at, last_login, default_model, autopilot_enabled, theme, slack_member_id, slack_notify, slack_resolved_id, slack_link_confirmed_at, oidc_issuer, oidc_subject, judge_enabled, judge_anthropic_secret_id, wait_on_limit, ci_autofix_enabled, sidebar_token_ids, judge_model, summary_model, ephemeral_workers_enabled, default_effort, mr_rework_enabled
`

type SetUserJudgeAnthropicSecretParams struct {
	JudgeAnthropicSecretID pgtype.UUID `json:"judge_anthropic_secret_id"`
	ID                     uuid.UUID   `json:"id"`
}

// Point a user's JUDGE lane at one of their own Anthropic credentials, or clear the
// binding back to their default (PRD #104 M4, D1). Per-user, not per-worker: which
// credential reviews your work is a property of you, not of whichever worker
// claimed the retrospective.
//
// Nothing here validates ownership of @judge_anthropic_secret_id — the composite FK
// from 00079 does, in the database. A foreign secret id has no (users.id, id) pair
// in user_secrets and this UPDATE raises a foreign_key_violation. The handler
// checks too, so the caller gets a 404 rather than a constraint violation, but the
// FK is the layer that holds when the handler check does not.
func (q *Queries) SetUserJudgeAnthropicSecret(ctx context.Context, arg SetUserJudgeAnthropicSecretParams) (User, error) {
	row := q.db.QueryRow(ctx, setUserJudgeAnthropicSecret, arg.JudgeAnthropicSecretID, arg.ID)
	var i User
	err := row.Scan(
		&i.ID,
		&i.Email,
		&i.PasswordHash,
		&i.DisplayName,
		&i.IsAdmin,
		&i.IsActive,
		&i.TokenVersion,
		&i.CreatedAt,
		&i.LastLogin,
		&i.DefaultModel,
		&i.AutopilotEnabled,
		&i.Theme,
		&i.SlackMemberID,
		&i.SlackNotify,
		&i.SlackResolvedID,
		&i.SlackLinkConfirmedAt,
		&i.OidcIssuer,
		&i.OidcSubject,
		&i.JudgeEnabled,
		&i.JudgeAnthropicSecretID,
		&i.WaitOnLimit,
		&i.CiAutofixEnabled,
		&i.SidebarTokenIds,
		&i.JudgeModel,
		&i.SummaryModel,
		&i.EphemeralWorkersEnabled,
		&i.DefaultEffort,
		&i.MrReworkEnabled,
	)
	return i, err
}

const setUserJudgeEnabled = `-- name: SetUserJudgeEnabled :one
UPDATE users SET judge_enabled = $2 WHERE id = $1
RETURNING id, email, password_hash, display_name, is_admin, is_active, token_version, created_at, last_login, default_model, autopilot_enabled, theme, slack_member_id, slack_notify, slack_resolved_id, slack_link_confirmed_at, oidc_issuer, oidc_subject, judge_enabled, judge_anthropic_secret_id, wait_on_limit, ci_autofix_enabled, sidebar_token_ids, judge_model, summary_model, ephemeral_workers_enabled, default_effort, mr_rework_enabled
`

type SetUserJudgeEnabledParams struct {
	ID           uuid.UUID `json:"id"`
	JudgeEnabled bool      `json:"judge_enabled"`
}

// Flip a user's run-retrospective opt-in (PRD #46 Decision 7). Per-user consent to
// spend the user's own Anthropic tokens judging every finished run; default false.
// The caller passes the target id: the session user for PUT /api/me/judge, or an
// admin-chosen id (from the path) for the admin per-user toggle.
func (q *Queries) SetUserJudgeEnabled(ctx context.Context, arg SetUserJudgeEnabledParams) (User, error) {
	row := q.db.QueryRow(ctx, setUserJudgeEnabled, arg.ID, arg.JudgeEnabled)
	var i User
	err := row.Scan(
		&i.ID,
		&i.Email,
		&i.PasswordHash,
		&i.DisplayName,
		&i.IsAdmin,
		&i.IsActive,
		&i.TokenVersion,
		&i.CreatedAt,
		&i.LastLogin,
		&i.DefaultModel,
		&i.AutopilotEnabled,
		&i.Theme,
		&i.SlackMemberID,
		&i.SlackNotify,
		&i.SlackResolvedID,
		&i.SlackLinkConfirmedAt,
		&i.OidcIssuer,
		&i.OidcSubject,
		&i.JudgeEnabled,
		&i.JudgeAnthropicSecretID,
		&i.WaitOnLimit,
		&i.CiAutofixEnabled,
		&i.SidebarTokenIds,
		&i.JudgeModel,
		&i.SummaryModel,
		&i.EphemeralWorkersEnabled,
		&i.DefaultEffort,
		&i.MrReworkEnabled,
	)
	return i, err
}

const setUserJudgeModel = `-- name: SetUserJudgeModel :one
UPDATE users SET judge_model = $1 WHERE id = $2
RETURNING judge_model
`

type SetUserJudgeModelParams struct {
	JudgeModel pgtype.Text `json:"judge_model"`
	ID         uuid.UUID   `json:"id"`
}

// Sets (or clears, when @judge_model is NULL) the current user's per-user judge
// model. NULL inherits the instance judge_model. Own-user only; caller passes the
// session user's id.
func (q *Queries) SetUserJudgeModel(ctx context.Context, arg SetUserJudgeModelParams) (pgtype.Text, error) {
	row := q.db.QueryRow(ctx, setUserJudgeModel, arg.JudgeModel, arg.ID)
	var judge_model pgtype.Text
	err := row.Scan(&judge_model)
	return judge_model, err
}

const setUserMrReworkEnabled = `-- name: SetUserMrReworkEnabled :one
UPDATE users SET mr_rework_enabled = $1 WHERE id = $2
RETURNING mr_rework_enabled
`

type SetUserMrReworkEnabledParams struct {
	MrReworkEnabled pgtype.Bool `json:"mr_rework_enabled"`
	ID              uuid.UUID   `json:"id"`
}

// Set (or clear, when @mr_rework_enabled is NULL) the current user's MR review-watcher
// opt-in (PRD #700 M5, Decision 5). This feature ships ON, so the column is nullable
// and a NULL/absent value reads as ENABLED: clearing back to NULL restores the
// default-on state, and a user opts OUT by setting it explicitly to false. Own-user
// only for PUT /api/me/settings; the caller passes the session user's id.
func (q *Queries) SetUserMrReworkEnabled(ctx context.Context, arg SetUserMrReworkEnabledParams) (pgtype.Bool, error) {
	row := q.db.QueryRow(ctx, setUserMrReworkEnabled, arg.MrReworkEnabled, arg.ID)
	var mr_rework_enabled pgtype.Bool
	err := row.Scan(&mr_rework_enabled)
	return mr_rework_enabled, err
}

const setUserSidebarTokens = `-- name: SetUserSidebarTokens :one
UPDATE users SET sidebar_token_ids = $1::uuid[] WHERE id = $2
RETURNING sidebar_token_ids
`

type SetUserSidebarTokensParams struct {
	SidebarTokenIds []uuid.UUID `json:"sidebar_token_ids"`
	ID              uuid.UUID   `json:"id"`
}

// Replaces the user's whole sidebar token-meter set (00123): the non-default
// Anthropic tokens whose rate meters ride the sidebar rail. The handler has
// already filtered the ids to the caller's own anthropic_token secrets; an
// empty array is a valid "default-only" choice. Own-user only.
func (q *Queries) SetUserSidebarTokens(ctx context.Context, arg SetUserSidebarTokensParams) ([]uuid.UUID, error) {
	row := q.db.QueryRow(ctx, setUserSidebarTokens, arg.SidebarTokenIds, arg.ID)
	var sidebar_token_ids []uuid.UUID
	err := row.Scan(&sidebar_token_ids)
	return sidebar_token_ids, err
}

const setUserSummaryModel = `-- name: SetUserSummaryModel :one
UPDATE users SET summary_model = $1 WHERE id = $2
RETURNING summary_model
`

type SetUserSummaryModelParams struct {
	SummaryModel pgtype.Text `json:"summary_model"`
	ID           uuid.UUID   `json:"id"`
}

// Sets (or clears, when @summary_model is NULL) the current user's per-user
// run-summary model. NULL inherits the instance summary_model. Own-user only;
// caller passes the session user's id.
func (q *Queries) SetUserSummaryModel(ctx context.Context, arg SetUserSummaryModelParams) (pgtype.Text, error) {
	row := q.db.QueryRow(ctx, setUserSummaryModel, arg.SummaryModel, arg.ID)
	var summary_model pgtype.Text
	err := row.Scan(&summary_model)
	return summary_model, err
}

const setUserTheme = `-- name: SetUserTheme :one
UPDATE users SET theme = $1 WHERE id = $2
RETURNING theme
`

type SetUserThemeParams struct {
	Theme pgtype.Text `json:"theme"`
	ID    uuid.UUID   `json:"id"`
}

// Sets (or clears, when @theme is NULL) the current user's theme override.
// NULL falls the user back to the instance default. Own-user only.
func (q *Queries) SetUserTheme(ctx context.Context, arg SetUserThemeParams) (pgtype.Text, error) {
	row := q.db.QueryRow(ctx, setUserTheme, arg.Theme, arg.ID)
	var theme pgtype.Text
	err := row.Scan(&theme)
	return theme, err
}

const setUserWaitOnLimit = `-- name: SetUserWaitOnLimit :one
UPDATE users SET wait_on_limit = $2 WHERE id = $1
RETURNING id, email, password_hash, display_name, is_admin, is_active, token_version, created_at, last_login, default_model, autopilot_enabled, theme, slack_member_id, slack_notify, slack_resolved_id, slack_link_confirmed_at, oidc_issuer, oidc_subject, judge_enabled, judge_anthropic_secret_id, wait_on_limit, ci_autofix_enabled, sidebar_token_ids, judge_model, summary_model, ephemeral_workers_enabled, default_effort, mr_rework_enabled
`

type SetUserWaitOnLimitParams struct {
	ID          uuid.UUID `json:"id"`
	WaitOnLimit bool      `json:"wait_on_limit"`
}

// Flip a user's usage-limit park default (PRD #35 Decision 7). Per-user consent to
// a run PARKING rather than failing when the owner's Anthropic window is exhausted;
// default false, set from the user's own Settings page.
//
// It is the DEFAULT a new run inherits, never a retroactive switch: every run
// carries its own runs.wait_on_limit, stamped at creation, and flipping this changes
// nothing about a run that already exists (parked or otherwise). That is the same
// shape as autopilot_enabled above and is what makes the per-run toggle
// (SetRunWaitOnLimit) a separate write rather than a filter over this one.
func (q *Queries) SetUserWaitOnLimit(ctx context.Context, arg SetUserWaitOnLimitParams) (User, error) {
	row := q.db.QueryRow(ctx, setUserWaitOnLimit, arg.ID, arg.WaitOnLimit)
	var i User
	err := row.Scan(
		&i.ID,
		&i.Email,
		&i.PasswordHash,
		&i.DisplayName,
		&i.IsAdmin,
		&i.IsActive,
		&i.TokenVersion,
		&i.CreatedAt,
		&i.LastLogin,
		&i.DefaultModel,
		&i.AutopilotEnabled,
		&i.Theme,
		&i.SlackMemberID,
		&i.SlackNotify,
		&i.SlackResolvedID,
		&i.SlackLinkConfirmedAt,
		&i.OidcIssuer,
		&i.OidcSubject,
		&i.JudgeEnabled,
		&i.JudgeAnthropicSecretID,
		&i.WaitOnLimit,
		&i.CiAutofixEnabled,
		&i.SidebarTokenIds,
		&i.JudgeModel,
		&i.SummaryModel,
		&i.EphemeralWorkersEnabled,
		&i.DefaultEffort,
		&i.MrReworkEnabled,
	)
	return i, err
}

const updatePassword = `-- name: UpdatePassword :exec
UPDATE users
SET password_hash = $2, token_version = token_version + 1
WHERE id = $1
`

type UpdatePasswordParams struct {
	ID           uuid.UUID   `json:"id"`
	PasswordHash pgtype.Text `json:"password_hash"`
}

func (q *Queries) UpdatePassword(ctx context.Context, arg UpdatePasswordParams) error {
	_, err := q.db.Exec(ctx, updatePassword, arg.ID, arg.PasswordHash)
	return err
}
